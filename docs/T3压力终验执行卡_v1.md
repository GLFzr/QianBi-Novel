# T3 60 章压力终验 · 执行卡 v1（2026-09-25 预注册）

> L5-C2 的收口件。预注册判据后才开跑（防"先跑后定及格线"）。
> 渠道裁决依据：`docs/0.20.0_收口裁决记录_v1.md` Q5②——99% 命中率与终验一律在**生产主渠道
> DeepSeek 官方**验收（0.19.x 成本战役的 OpenCode Go 渠道本机无 Key，且跨渠道同码差 4-6pp，
> 混跑只会让口径不可比）。

## 规格

- **项目**：`examples/改命笔记` 复制到 `tests_output/t3_改命笔记`，断点续跑。
- **驱动**：`tests/long_run.py <项目> <本轮章数>`（每轮 4 章，重复调用续跑），连接/模型经
  环境变量 `QIANBI_TEST_BASE / QIANBI_TEST_KEY / QIANBI_TEST_MODEL` 覆盖为
  `https://api.deepseek.com` + keyring 运行时取钥 + `deepseek-chat`（官方 flash 档）。
- **目标**：累计 **60 章**（台帐 T3 原判 60~100 章的下限；预算 ¥10-25 用户已批，
  按 v0.19.8 真跑口径 ¥0.25-0.29/章 估 ¥15-18）。
- **全程开对话落盘**（WP-27 默认开），账本即证据。

## 判据（跑前钉死，Q5③ 口径=整卷均值+重复测量）

| 维度 | 判据 | 取数处 |
|---|---|---|
| 成本上翘 | 第 41-60 章单章均价 ≤ 第 1-20 章 × **1.5**（直跑上翘容差）；整卷均值单列 | usage 台账 / .dialogue usage |
| 命中率 | 混合命中率整卷均值 ≥ **90%**（0.19.5 实测 93.8% 同架构，留 3.8pp 衰减余量） | prefix_lint + 账本 cached_tokens |
| 输出膨胀 | 追踪/摘要单章输出第 41-60 章均值不高于第 1-20 章均值 × 1.2（delta 协议有效性） | 账本 completion 逐笔 |
| 质量斜率 | （T3 跑完后的盲评，独立小预算）D2 斜率 ≥ **-0.02 分/章**（L5-B6 同口径） | 盲评 |

## 渠道替代记录（2026-09-25 实跑）

- DeepSeek 官方：**余额不足（API 402）**，58 轮全败——验收口径保留该渠道，充值后可
  用同一启动器一键重跑；
- OpenRouter stealth/union-alpha：**已下架（404）**（即 09-18 Wave C 走查 404 的根因）；
- 改用**百炼 qwen-flash**（200 可用，返回 cached_tokens 可测命中率，费用远低于预算上限）。
  成本判据的绝对值随渠道重定基，命中率判据（整卷均值 ≥90%）不变。

## 执行状态

- [x] 预注册（本文件）
- [x] 渠道替代（本节）
- [x] **v1 跑完（69 章 / 22.4 万字，2026-09-25 06:14→23:41，实付 ≈¥2.30）**——但 v1 是
  **基线架构口径**（long_run 极简配置未开 0.19.5 成本栈旗标，四项旗标均为选配缺省关），
  命中率判据按出货架构预注册 ⇒ v1 只作参考与缺陷挖掘，判据核分以 v2 为准
- [ ] **v2 重跑中**（成本栈全开 volume_session/s4_static_freeze/tracking_delta/review_in_system，
  全新项目 t3_改命笔记_v2，2026-09-25 23:53 启动）——跑完后按四项判据核分回填
- [ ] 盲评斜率取证（L5-B6 联动）

## v1 记分卡（基线架构口径，2026-09-26 核）

| 判据 | 实测 | 裁定 |
|---|---|---|
| 成本上翘 ≤1.5× | 1.18×（ch41-60 vs ch1-20，相对成本单位） | 数字达标但**被下述相位停摆污染**（后 30 章缺记账相位，天然更便宜），不作数 |
| 命中率整卷 ≥90% | **45.9%**（prose 相位仅 3%） | 基线架构无会话栈，判据**不适用**；出货架构数值待 v2 |
| 输出膨胀 ≤1.2× | 不可评（ch41-60 追踪输出=0，见下） | 待 v2 |
| 盲评斜率 ≥-0.02 | 未做 | 待 v2 后统一排 |

- 数据源：`~/.qianbi_novel/usage/usage.jsonl`（546 笔带章号，in 16.9M / out 1.14M / 命中 7.7M）

## 🔴 T3-1（v1 挖出的真发现，待归因）

**qwen-flash 直跑约 40 章后质量塌方，且失败被 fail-open 静默吞掉**：

1. 自 ch41 起草稿长度失控：ch41 首稿 **21377 字**（目标 2000，10 倍超标），压缩两轮压不动
   （21376），此后章章如此；
2. 去味阻断 67-68 处/章（ch1-40 每章个位数）、审校三票 REJECT-HARD 连挂 → 章章标记「待修」
   继续硬写（mark_continue 策略下不阻断）；
3. **追踪/章摘要/全局摘要/清算四个记账相位只在定稿后执行 ⇒ 自 ch41 起整段停摆 30 章**，
   日志零告警（`追踪更新失败（不阻断）` 一条都没有——因为调用根本没发生），只能从
   usage 台账的相位分布看出。台账/摘要随章数增长的长程承诺在此场景下失效。

**候选归因**（v2 跑完对照）：① qwen-flash 在长会话历史上模仿自身膨胀文风（草稿 10 倍化）；
② 细纲质量随章衰减诱导注水；③ 10 倍长度 × 密度型检测器 = 阻断数爆炸（检测口径按章不按
千字的阈值在天文长度下失真）。
**候选修法方向**：草稿长度硬上限前置（`prose.length_budget` 已有，确认 T3 场景是否生效）、
「待修」章跳过时给一条显式 warn（记账停摆必须可观测）、检测阈值按千字归一。


> 运行方式与监控：`while` 循环调用 `tests/long_run.py tests_output/t3_改命笔记 4` 直到达 60 章；
> 中断后重跑自动续。**停止方法**：删除 `tests_output/t3_改命笔记/pipeline_state.json` 前先停进程
> （Ctrl+C / 结束 python 进程），断点机制保证已完章节不重写、不重计费。
