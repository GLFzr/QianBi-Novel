# WP-19 / L3 执行计划（先交计划不动手——需用户在场窗口）

> 状态：**待用户批准排期**。本计划不执行任何真机动作；批准后逐例执行、逐例勾，
> 绝不批量打勾。`docs/release_checklist.md` 当前 58 框 0 勾保持不变。
> 前置：WP-20 CI 推送授权先行（矩阵要在 CI 绿的提交上装）。

## 一、L3-01 §5b 真机升级矩阵 8 例

统一环境：一台真实 Windows 机器（非虚拟机优先）、装好当前 Release 安装包、
屏幕点亮（截图需要真实合成）。每例证据统一落 `docs/release_evidence/L3_01/`。

| # | 用例 | 怎么验 | 留什么证 |
|---|------|--------|----------|
| 1 | 安装版+空闲：下载并校验→立即安装全流程，装完自动重开、书架与配置在 | 正常网络下点「检查更新」→「下载并校验」→「立即安装」；装完等自动重启 | ①重启后书架截图；②`~/.qianbi_novel/` 配置 md5 前后对比输出；③更新面板全程录屏或 3 张关键帧截图 |
| 2 | 安装版+脏稿：编辑器有未保存内容时一键升级 | 正文编辑器打字不保存→直接升级；重启后检查草稿恢复提示 | ①升级前编辑器截图（可见未保存标记）；②重启后草稿恢复 toast 截图；③恢复的正文内容 diff 截图 |
| 3 | 便携版：面板只给链接与哈希，不出现「立即安装」 | 便携 zip 解压运行→检查更新面板 | 面板整屏截图（证明无「立即安装」按钮，只有链接+哈希） |
| 4 | 旧进程开着时覆盖安装（v0.17→本版） | 保持旧版进程运行→直接双击新版 setup.exe | ①Restart Manager 询问对话框截图；②进程列表截图（tasklist 输出存 txt）证明没有静默排重启 |
| 5 | 断网离线路径：导入拷来的 latest.json → 选本地 setup.exe | 禁用网卡（截图网络面板）→导入清单→选本地包 | ①禁网截图；②导入后面板截图；③哈希命中日志（update 面板哈希行截图）；④安装完成截图 |
| 6 | SmartScreen：真实下载后双击，走「更多信息→仍要运行」 | 浏览器真实下载 setup.exe（不经代理缓存）→双击 | ①蓝底 SmartScreen 截图；②「更多信息」展开截图；③「仍要运行」后安装界面截图 |
| 7 | 陈旧代理：系统代理设死地址→检查仍成功 | 设置系统代理为 127.0.0.1:9（截图）→应用内检查更新 | ①死代理设置截图；②「无代理第二遍」成功的面板截图 |
| 8 | 未验签清单：自签清单导入后无可执行按钮 | `sign_manifest.py` 签一份→手改一个字节破坏签名→导入 | ①导入后清单详情截图（说明+哈希可见）；②操作区截图证明无安装/下载按钮 |

每例完成后在 `release_checklist.md` 对应框打勾，并附证据文件相对路径；
失败/跳过如实写「未通过/未执行+原因」，不许空勾也不许替勾。

## 二、L3-03 干净机/新用户安装向导

- 环境：独立 Windows 账户（或干净虚拟机快照），无 `~/.qianbi_novel/`。
- 步骤：双击安装包→向导逐步→首次启动→（向导界面逐屏截图）→建一本测试书
  →打开设置看默认槽位（应为 ds-official-flash，A14 出厂零 Pro）→退出重开确认
  配置持久。
- 证据：`docs/release_evidence/L3_03/` 全程逐屏编号截图 + 重启后书架截图 +
  `config.json` 快照（脱敏：Key 字段必须为空串或删除后存档）。
- 额外确认项：新账户下首次启动无任何报错弹窗；卸载后 `~/.qianbi_novel/`
  去留与文档承诺一致（截图）。

## 三、§7.4-7 视觉回归 before/after 成套截图

现状核对：`tests/shot_ui.py` 已在 `windows` 平台（真合成），且 show 后多次
`processEvents()`。计划里补一道保险：每屏截图前做一次
`hide→show→processEvents×3`（显隐强制合成），防「显示器关闭抓上一帧」——
批准后在 shot_ui.py 加这 3 行，属于截图工具不算改产品。

- 执行：改前基线提交（当前 HEAD 打 tag 或记 sha）→产出 before 套图；
  WP-16 的 QML 改动（StepGateBar 详情块/G8 TextArea、强锁对话框违规列表）
  逐屏 after 套图。
- 产出：`docs/release_evidence/visual/` 每屏一对 before/after PNG，文件名
  `面板名_before.png / 面板名_after.png`，外加一张「逐屏差异说明.md」
  （人眼可见差异逐条列：预期/意外）。
- 交用户逐屏点头后，才在 checklist 视觉项打勾。

### 三-b、README 现役截图盘点（2026-09-18 预检，B-1「截图同步换」并入本节执行）

7 张引用截图全部停在 09-04/09-06（`b7ebc56`/`72ec144`），落后于其后 UI 变更：
PipelinePanel 对话落盘开关+info 行+打开目录按钮（WP-27）、StepGateBar 闸门详情、
导出对话框三按钮、设置页启动档默认值（B-1）。逐张判定：

| 文件 | 首次盘点判定 | 处置 |
|---|---|---|
| hero.gif（:20 首视觉） | 演自动档流水线点亮；紧邻文字已宣布共写为默认档——首屏观感错位 | **需用户裁决**：换共写首屏动图 / 保留自动档 hero（批量路径卖点）|
| shot_co_writing.png（:46/:484） | 共写主推图，主体无标签性错误 | 随套图刷新（新 UI 控件入镜）|
| shot_pipeline.png（:175） | 标注「自动档驾驶舱」，无标签性矛盾 | 随套图刷新 |
| shot_shelf/chapters/library/settings | 与默认档无矛盾 | 随套图刷新 |

> B-1 的「README 头条与截图一起换」：头条文案已换（README:36），截图部分
> 依赖真机合成截图窗口，并入本节（Wave D.2）逐屏点头执行——不在 Wave B 独立重拍。

## 四、排期与依赖

1. 用户先批 WP-20 推送 → CI 绿。
2. 本计划获批 → 约定窗口（预计半天的真机连续时间，需要用户在旁提供
   SmartScreen 确认与网络环境切换）。
3. 执行顺序：三（视觉回归，零风险）→ 二（干净机）→ 一（升级矩阵，有破坏性
   操作放最后）。
4. 窗口内每完成一例即提交勾选+证据，中断也可断点续作。
