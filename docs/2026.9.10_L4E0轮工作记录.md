# 2026.9.10 L4/E0 轮工作记录（zcode 台前主代理）

> **性质**：工作记录（协同协议第 4 条）。覆盖：W 轮交接吸收 → E0 探针 →
> L4/L3 实现 → 冻结待测。实验结论看《下一步深化计划_v2_20260910.md》（W 轮代理著）
> 与看板 §5；本记录只记动作与判断。
> **时间跨度**：2026-09-10 00:05 → 01:10。用户指令：「按照计划全量实现，只不进行
> 真机测试」。

---

## 1. 交接吸收（W 轮代理 → zcode，经用户转交 outbox）

- W-1（卷号解析「第14-25章」误累加 → 真卷界永不换栈）解释了本代理 V6/V7 测得的
  +34k/章无界斜率的主要成分——两轮结论在根因上汇合。
- W-8（固化 clean_llm_output 字节 ≠ 服务商缓存的「模型输出末端」原始字节）与 E0
  实测的「写延迟一个发次」是**两个独立机制**，L3 与写延迟各自对应。
- 基线核实：本地 = 远端（同工作区同克隆），977 passed 复跑确认。

## 2. 动作与结果

| 动作 | 结果 |
|---|---|
| `replay_request_probe.py` 加 `--truncate-tok`（han 口径估算截前缀，精度以 API 实报为准） | cd3fe84 |
| **E0 探针实测**（t3e 卷3 栈 263k tok，8k→256k→全量逐级，bailian，花费 ≈¥0.5） | hit 随前缀长度**连续增长**（10,240→…→263,168），**无 32k 整数倍台阶** ⇒ 按预注册判据 **L4 存活**，备择 B' 不触发 |
| **附带发现**：256k 与全量同字节相隔 5 秒，hit 171,008→263,168 | **缓存写延迟 ≈ 一个发次（5-15s/20-90k tok）**——「首达深度窗口」的定量形态，与 W-8 清洗字节机制独立并存 |
| **L4 落地**：`writing.head_rebuild`（缺省关）——每章 fresh VolumeSession（persist=False 不落盘），system=冻结稳定头（volume_system_text + review 尾段随 review_in_system），开幕轮带**完整八节**（volume_mode=False：无跨章历史时上一章结尾/文风样本是连续性生命线）；S4-c 追踪引用行同口径扩展 | stages 获取分支 + 开幕分支 + 追踪分支三处；test_head_rebuild 5 件 |
| **L3 落地**：`writing.commit_raw`（缺省关）——清洗改字节时栈里固化**原始字节**（缓存对齐），清洗稿仍返回调用方（工作副本分离） | VolumeSession persist/commit_raw 两旗标；test_head_rebuild 覆盖 |
| L6 | 机制已存在（`think_budget` stage_param 软指令，E1.2/O2）；**preset 值未加**——服从度门槛（实测 ≤0.8×预算）属真机测试，按用户指令冻结 |
| L1 泛化（deslop/review_fix 不整章输出） | **设计冲突上报**：span 机制被 T4b E1 否证（输出 5× 反向）；补尾只适用 enrich；事件溯源只适用台账。三机制皆不适配散文中段修复——**待裁决**（见 §4） |
| L2 卷内变体（audit 指令入冻结头等） | **被 L4 掩盖**：head_rebuild 下历史不过章，指令不累积。volume_session 模式的变体按计划门槛需真机+盲评（冻结待测） |
| 看板登记 E0 结果 + 分工提议（L1 归 zcode/L2 归 W 轮/shared_prefix 归 W 轮） | 0c838e7 |

回归：**982 passed / 1 skipped**（+5 件），prefix_lint 全绿。

## 3. 明确不做（用户指令：不进行真机测试）

- L4/L3 的真机 A/B 臂（原计划 W1 窗口 ¥3-4 + ¥8-12）——**冻结待用户发令**；
- V7 台账 JSON 协议的跨章质量盲评——同上冻结；
- 深化计划 §5 的 09-10 全天日程（W1-W4 臂）——代码就绪，发令即跑。

## 4. 待用户/对方代理裁决

1. **L4 真机 A/B 何时放行**（代码就绪；判据预注册：每章新增字/章输出字 ≤0.6，
   `stack_redundancy.py` 复量）；
2. **L1 的机制裁决**：span（T4b 否证）/ 补尾（不适用中段）/ 待新机制——三选一或
   维持 enrich-only；
3. 分工提议（看板 00:30 条）对方未回应——默认按提议执行，有异议看板回复。

## 5. 下一位从哪里接

1. 看板 §5 最新条目（E0 结果 + 分工提议）；
2. 真机解冻后第一跑：`v8_stack` preset（head_rebuild+commit_raw 开）3 章冒烟 →
   `stack_redundancy` 复量 → `session_events.turn_cleaned.n` 读数定 L3 启停；
3. 本文件即本轮工作记录（协同协议第 4 条）。


---

## 6. 补录：真机解冻后的 V8 轮（2026-09-10 晚，用户指令"现在可以测试了"）

用户解冻真机测试并指定 **DS 官方 API、模型名 `deepseek-flash`**（新模型，≠百炼
v4-flash-0731）。动作与结果：

1. **渠道**：ds-official-flash 连接登记（Key 复用官方），健康验证 200，渠道链第三位
   ds-v41-flash → ds-official-flash（9 单测同步）；
2. **V8 冒烟（v8_smoke3）暴露并修复真机台账污染事故**：W-5 在 cost_bench 的模块级
   `from app.usage import ...` 在 USERPROFILE 重定向前把真机路径焊死 → 30 行烟测用量
   写进真实台账。修复：延迟到函数内导入（模块级留警示注释）；真机台账备份后剔除
   30 行，**恢复 338 行原点**；修复后重跑（v8_smoke3b）验证落点正确；
3. **V8 十三章对照（v8_long13，13/13 章完成零 429——DS 官方全程稳定）**：
   - **平坦性确证（L4 核心主张）**：逐章 prose 入参 11,230 → 16,231 → 平坦于
     **~18k**（ch13=19,118 ≈ ch8=18,303），对比 v7_long13 同章位 313,543（+30k/章）
     ——**上下文与章数脱钩达成**；
   - 相位输出 vs v7_long13：追踪 **-23%**（无截顶）、enrich **-95%**（仅 3 次）、
     摘要 **-51%**、去味 -23%；
   - 每章 DS 口径 **¥0.323**（注意：deepseek-flash 新模型思考量大，输出 516.6k tok
     中推理占 339.4k=66%——输出侧已成第一大成本桶，prose 思考方差治理=下一杠杆）；
   - **turn_cleaned = 12/13 章**（每章 tracking 相位清洗 -12 字符）→ **L3 闸门满足**，
     commit_raw 已在本跑生效；
   - 正文 29,144 字/13 章（均值 2,242，全部落在目标带宽），0 阻塞；
   - 待查项：canon_audit hit 26.7% 偏低（42-53k/笔，新深度首达写延迟税，非结构性）。
4. **量具口径**：head_rebuild 无跨章栈文件，stack_redundancy 不适用——有界性改由
   usage 逐章 prose in 直接判读（更直接）。

**成本轨迹终局**：0.19.0 基线 ¥0.386（6章 hit 80.4%）→ 卷会话 ¥0.291 →
V6 输出侧前 ¥0.405（13章，含 W-1 前的无界栈）→ **V8 ¥0.323 且平坦**
（deepseek-flash 新模型口径，跨模型不可直比，趋势可信）。


---

## 7. 补录：V9 头 v2 + 输出控制量测（2026-09-10 晚，用户给出新目标）

用户实测读数（V8）：hit 2.86M / miss 1.14M / 输出 0.58M，"综合要达到 95%"。
钱分解（DS 官方离峰）：输出 ¥2.75（59%）＞ miss ¥1.81（38%）＞ hit ¥0.14（3%）。

1. **头 v2 落地**（volume_session.head_rebuild_system_text + build_head_snapshot）：
   稳定头从 8.9k tok 扩到 **25.3k tok**（卷首冻结快照：设定底册/世界书/卷纲入头，
   快照文件保证卷内逐字节稳定）；导入修正（app.core 无 project，改 .. 包级）；
2. **输出控制**：v9 preset = prose `reasoning_effort: low` + `think_budget: 4000`
   （deepseek-flash 思考 66% 占输出的治理；盲评门预注册）；
3. **V9 三章量测（v9_smoke3，DS 官方 deepseek-flash）**：综合命中 **79.8%**
   （V8 同位 71.5%，+8.3pp——头扩容生效）、输出 **-36%**（effort low 生效）、
   3 章 ¥0.464（-28%）、enrich **0 次**（首稿全达标）、冻结头 26,169 chars 逐章复用 ✓；
4. **V9 十三章规模确认发车**（v9_long13）——待收车判定规模轨迹；
5. **95% 的诚实数学**：综合命中 = 头命中/(头命中+每章新料)。25k 头 × ~9 调/章
   ≈ 86-90% 天花板；95% 需 60k 头——本书稳定内容天然上限 ~25k tok，靠注水扩头
   反而增加命中价读量。**建议：接受 86-90% 架构天花板 + 拿输出侧的钱**
   （output 59% 的占比下，effort/思考治理的绝对金额远超命中率最后一个 5pp）。
