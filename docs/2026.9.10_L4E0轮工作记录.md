# 2026.9.10 L4/E0 轮工作记录（zcode 台前主代理）

> **性质**：工作记录（协同协议第 4 条）。覆盖：W 轮交接吸收 → E0 探针 →
> L4/L3 实现 → 冻结待测。实验结论看《下一步深化计划_v2_20260910.md》（W 轮代理著）
> 与看板 §5；本记录只记动作与判断。
> **时间跨度**：2026-09-10 00:05 → 01:10。用户指令：「按照计划全量实现，只不进行
> 真机测试」。

---

## 1. 交接吸收（W 轮代理 → zcode，经用户转交 outbox）

- W-1（卷号解析「第14-25章」误累加 → 真卷界永不换栈）解释了本代理 V6/V7 测得的
  +34k/章无界斜率的主要成分——两轮结论在根因上汇合。
- W-8（固化 clean_llm_output 字节 ≠ 服务商缓存的「模型输出末端」原始字节）与 E0
  实测的「写延迟一个发次」是**两个独立机制**，L3 与写延迟各自对应。
- 基线核实：本地 = 远端（同工作区同克隆），977 passed 复跑确认。

## 2. 动作与结果

| 动作 | 结果 |
|---|---|
| `replay_request_probe.py` 加 `--truncate-tok`（han 口径估算截前缀，精度以 API 实报为准） | cd3fe84 |
| **E0 探针实测**（t3e 卷3 栈 263k tok，8k→256k→全量逐级，bailian，花费 ≈¥0.5） | hit 随前缀长度**连续增长**（10,240→…→263,168），**无 32k 整数倍台阶** ⇒ 按预注册判据 **L4 存活**，备择 B' 不触发 |
| **附带发现**：256k 与全量同字节相隔 5 秒，hit 171,008→263,168 | **缓存写延迟 ≈ 一个发次（5-15s/20-90k tok）**——「首达深度窗口」的定量形态，与 W-8 清洗字节机制独立并存 |
| **L4 落地**：`writing.head_rebuild`（缺省关）——每章 fresh VolumeSession（persist=False 不落盘），system=冻结稳定头（volume_system_text + review 尾段随 review_in_system），开幕轮带**完整八节**（volume_mode=False：无跨章历史时上一章结尾/文风样本是连续性生命线）；S4-c 追踪引用行同口径扩展 | stages 获取分支 + 开幕分支 + 追踪分支三处；test_head_rebuild 5 件 |
| **L3 落地**：`writing.commit_raw`（缺省关）——清洗改字节时栈里固化**原始字节**（缓存对齐），清洗稿仍返回调用方（工作副本分离） | VolumeSession persist/commit_raw 两旗标；test_head_rebuild 覆盖 |
| L6 | 机制已存在（`think_budget` stage_param 软指令，E1.2/O2）；**preset 值未加**——服从度门槛（实测 ≤0.8×预算）属真机测试，按用户指令冻结 |
| L1 泛化（deslop/review_fix 不整章输出） | **设计冲突上报**：span 机制被 T4b E1 否证（输出 5× 反向）；补尾只适用 enrich；事件溯源只适用台账。三机制皆不适配散文中段修复——**待裁决**（见 §4） |
| L2 卷内变体（audit 指令入冻结头等） | **被 L4 掩盖**：head_rebuild 下历史不过章，指令不累积。volume_session 模式的变体按计划门槛需真机+盲评（冻结待测） |
| 看板登记 E0 结果 + 分工提议（L1 归 zcode/L2 归 W 轮/shared_prefix 归 W 轮） | 0c838e7 |

回归：**982 passed / 1 skipped**（+5 件），prefix_lint 全绿。

## 3. 明确不做（用户指令：不进行真机测试）

- L4/L3 的真机 A/B 臂（原计划 W1 窗口 ¥3-4 + ¥8-12）——**冻结待用户发令**；
- V7 台账 JSON 协议的跨章质量盲评——同上冻结；
- 深化计划 §5 的 09-10 全天日程（W1-W4 臂）——代码就绪，发令即跑。

## 4. 待用户/对方代理裁决

1. **L4 真机 A/B 何时放行**（代码就绪；判据预注册：每章新增字/章输出字 ≤0.6，
   `stack_redundancy.py` 复量）；
2. **L1 的机制裁决**：span（T4b 否证）/ 补尾（不适用中段）/ 待新机制——三选一或
   维持 enrich-only；
3. 分工提议（看板 00:30 条）对方未回应——默认按提议执行，有异议看板回复。

## 5. 下一位从哪里接

1. 看板 §5 最新条目（E0 结果 + 分工提议）；
2. 真机解冻后第一跑：`v8_stack` preset（head_rebuild+commit_raw 开）3 章冒烟 →
   `stack_redundancy` 复量 → `session_events.turn_cleaned.n` 读数定 L3 启停；
3. 本文件即本轮工作记录（协同协议第 4 条）。
