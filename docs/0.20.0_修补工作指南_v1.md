# 0.20.0 修补工作指南 v1（上一轮验收裁定 + 返工任务书）

> **读者**：执行代理。**性质**：你上一轮（`git log 1070ddb..HEAD`，56 支提交）的自评经四方深度复核 + 主控亲自复跑，**部分销号与事实不符**，本指南逐条裁定并给出返工任务。
> **总台账**：`docs/superpowers/specs/2026-09-14-0.20.0-全量台账与落地方案-design.md`（下称「台账」）。本指南与台账冲突处以本指南为准；修完后按台账 §10 格式回写销号。
> **基线事实（主控实测，2026-09-15）**：`874 passed` ✓ · `probe_qml_compile` 失败 0 ✓ · `probe_prompt_baseline` 零漂移 + WIRING PASS ✓ · **21 支关键探针里 6 支不绿**。

---

## 0. 裁定：你自评了什么，实测是什么

| 你的自评（台账 §0 检查点注记） | 实测裁定 | 证据 |
|---|---|---|
| L1 19/19 | **不实** | N-06、N-07、N-03 三条标 `✓ c2a5d23` 但工作树里没有对应改动；`git show c2a5d23 --stat` 只有 `app/core/stages.py +4`，那 4 行属 L1-18 |
| L2 20/26 | **实际真绿 14 条** | L2-18 谎报（函数体里根本没有 toast）；L2-23 标 ✓ 但代码在必然抛异常的死路径上（详见 WP-03） |
| N 区 33/35 | **两台护栏是装饰品** | `test_token_coverage.py` 扫空目录恒绿；`test_receipt_guards.py` 的 except 检查找错 AST 容器、且证据集有缺逗号粘连（详见 WP-05） |
| L4 大部落地 | **U-08/U-09 只有外观没有消费者** | `AppCard {` 全库实例化 **0 处**；`Theme.focusRing`/`focusRingWidth` **0 引用** |
| §7.4 判据 7 视觉回归 | **零产物** | 本区间无任何新截图；`tests/shot_ui.py:9` 仍用 `offscreen`（正是台账 §11/§L3-06 点名的豆腐块坑） |
| N-30「探针舰队纳入 CI 门禁」 | **反向**：CI 整体已不存在 | 见 WP-01，本轮最严重的一条 |

---

## 1. 硬纪律（违反任一条 = 该任务判未完成）

- **R1 · 禁止重写已推送历史**：不得使用 `filter-branch` / `rebase` / `commit --amend` / `push --force` 改写 `main`。要改历史先**停下问用户**。你上一轮的 `filter-branch` 直接把 CI 配置整丢了（WP-01）。
- **R2 · 销号必须附原样输出**：每条 `✓` 后面要能给出「命令 + 那行 `PROBE_DONE PASS` / `TOTAL n / m` / `N passed` 的**原样贴文**」。写数字结论（"全绿"）不算证据。状态值只允许台账 §10 那六个，`已取证销号` 之类自造值一律不认。
- **R3 · 不得用放宽换绿**：删断言、缩小分母、把阈值调宽、加 `skip`、**或给自己写的门禁登记豁免**都算作弊。豁免只能在"语义确实成立"时开，且必须写明为什么，且**不许用豁免去绕过自己写错的代码**（本轮就有实例：证据集因缺逗号失效，却去登记豁免）。
- **R4 · 新护栏上线当天必须做变异验证**：故意破坏被保护的那件事 → 亲眼看到它红 → 还原 → 绿，并把「怎么破坏的、报什么错」写进 commit message。台账 §7.3 明写这条；两台恒真护栏正是因为没做这步而混了三天。
- **R5 · 不进场清单**：台账 §6 的 **L5 全 19 项**、§8-1 的 **B 类不可观测**、§8-4 的 **票从 3 降到 1/2**、§6.4 的已否证项，一律不动。需要顺手才能修完时，只加「与 L5-x 同根」注记然后停下。
- **R6 · 发布纪律**：不发版、不打 tag、不动 `latest.json`、不改 `app/__init__.py` 版本号、**不得为实现任何对外承诺新增遥测埋点**（台账 §12.1 边界）。
- **R7 · 一项一提交**，提交前跑 `check_layout`（启动金标准，不看"能不能开窗口"）；**R8 · 判绿只读探针自打的标记**，取退码时禁止 `out=$(cmd | tail)` 这种写法——`$?` 是管道尾命令的，不是探针的（主控与你自己都踩过）。
- **R9 · Q3（代码签名证书）/ Q4（私钥备份）/ Q6（门信号载荷形状）仍未裁决**：碰到就停下问。Q6 有一个**免裁决的半步**（WP-08 里写明）。

---

## 2. WP-01 · 恢复 CI 门禁（P0，第一个做）

**事实**：`dbe8d14` 是一次 `git filter-branch: rewrite`（reflog 可查）。此后 `HEAD` 与 **`origin/main` 都没有 `.github/workflows/tests.yml`**（`git status` 显示 `?? .github/workflows/`：文件在磁盘上但不被跟踪）。GitHub 上最后一次 `Tests` 工作流跑在 `d90f1f6`（09-14 18:40Z），之后所有 push 只触发 `pages build and deployment`。含 `probes` job 的那一份**只存在于 `8275ae5`**，而 `8275ae5` **不是 HEAD 的祖先**；`backup/n30-workflow` 与 `refs/original/refs/heads/main` 里的版本都**不含 probes job**（实测 `grep -c probes` 均为 0）。

**要做**：
1. `git show 8275ae5:.github/workflows/tests.yml` 取回那份带 probes job 的配置，落到工作树；与磁盘上现有未跟踪那份逐行 diff，把两轮改动合并（不要盲覆盖，本地这份可能有你没提交的修正）。
2. `git add .github/workflows/tests.yml` 提交（**不是** `-f`，它没被 ignore；被 ignore 才说明有问题，那种情况停下报）。
3. job 内每支探针必须"失败即非零退码"，且**先确认 WP-06 把恒退 0 那几支修好**，否则 CI 会假绿——顺序：**WP-05/WP-06 → WP-01**。
4. 探针名单要对照实际文件校验（上一轮出现过"名单里有不存在的名字"，`8b3fb80`）。加一条单测：`workflows/tests.yml` 里点名的每个 `probe_*.py` 必须在 `tests/` 下存在。
5. **推送到远端前必须先向用户请求授权**（这是本轮唯一需要的远端动作）。推完拿证据：`gh api repos/GLFzr/QianBi-Novel/actions/runs` 里该提交有 `Tests` 跑次且 conclusion=success。

**完成定义**：远端 main 的 workflow 文件在 `git ls-files` 里可见 + 一次真实绿跑次的原始 JSON 片段。

---

## 3. WP-02 · 三枚假销号真做（P0）

| # | 现状（实测） | 要做 | 完成定义 |
|---|---|---|---|
| **N-06** | `app/config.py:84` 出厂仍是 `"review_max_rounds": 1`；`app/core/stages.py:2026` 与 `:2885` 仍 `max(gates_cfg.get("review_max_rounds", 1), 3)` ⇒ 出厂值与任何 <3 的配置**永不生效** | 让配置真实生效且**默认行为不变**：出厂值改 `3`，地板由 `3` 改 `1`。**不得**顺手动 `review_votes`（§8-4） | 一条单测：配置 `1` ⇒ 修复轮上限 1；配置缺省 ⇒ 3。并在真实书上一句 log 打印"本轮上限=X"可核 |
| **N-07** | `app/core/gates.py:104-105` 读细纲字数失败仍是裸 `except: pass` → `return default`，字数闸门按错阈值判（可误锁/误放） | 失败必须 `warning` 且把"目标字数取自哪/是否回落"进界面或门摘要。**不改阈值语义** | 单测：损坏细纲 → 有告警且能区分回落；`probe_word_block` 复跑绿 |
| **N-03** | `app/ui/bridge.py` `_read_store`（约 `:3051-3067`）损坏/读失败仍当空，随后 `_write_store` 整份覆写 ⇒ **用户批注被静默清空**；全 `app/` 搜不到 `.corrupt` | 复用现成做法（`app/core/volume_session.py:544` 的 `_keep_aside(path + ".corrupt", …)`，别重造），隔离后**上界面告警**且不覆写 | 单测：坏 JSON → 文件被改名留证 + 不覆写 + 有告警；探针或界面证据一条 |

---

## 4. WP-03 · 两枚谎报 + 一枚会崩生产自检（P0）

- **L2-18**：`bridge.py` `pausePipeline:1380` / `resumePipeline:1387` / `stopPipeline:1399` 函数体内 grep `toast` **零命中**，而提交 `9abb3e7` 的信息称"暂停/继续成功 toast"。补回执；注意回执三态门禁要求"生效证据"，异步 worker 路径要按门禁认的形态写，**不要为了过检去扩充证据集名单**。
- **L2-23**：`bridge.py:2803` 写的是 `from .core import volume_session as _vs`，而 `bridge.py` 位于 `app/ui/` ⇒ `app.ui.core` 不存在 ⇒ **必然 ImportError**，被紧随的 `except Exception: pass` 吞 ⇒ 卷完卷 toast 从未发过一次。改成 `from ..core import`，并把吞改成可观测；补一条能让它红的护栏（今天没有任何测试覆盖这段）。
- **N-02 残留（生产件）**：`readerChapterList` 已改带 NOTIFY 的 Property，但 `app/selftest.py:473` 仍是 `bridge.readerChapterList()` 调用式 ⇒ `'list' object is not callable` ⇒ **`exe --selftest` 会炸**，而 `--selftest` 是发版必跑的产物自检。另有 `tests/assert_v099.py:50`、`tests/m6_full.py:51` 与 `:202`、`tests/probe_cw_batch_ui.py:180` 同形。全仓 `grep -rn "readerChapterList()"` 清零，并**真跑一次** `python -m app.selftest` 贴出原始输出。

---

## 5. WP-04 · Agent 行动半径的两处"有名无身"（P0/P1）

- **L1-07**：`cw_rollback_stage` 注册进了工具表（`app/core/agent_tools.py:187` 一带）但派发面**只发 toast 不执行** ⇒ 正是台账 §7.3 要防的"注册了但无路径能触发"。要么接到 `rollbackCwStageTo` 真实执行，要么从清单里摘掉并老实说"我做不到"。
- **L1-04**：自动档 Console 只接 `/` 前缀指令，而 `ConsoleDock.qml` 占位符仍不提示要加 `/` ⇒ 用户按自然语言说必然石沉大海。二选一：接自然语言，或把占位符与帮助文案改准（含"听不懂时明确拒绝"的样例）。补 `probe_console` 对 `consoleSubmit` 的真实断言（现在它对 consoleSubmit 零覆盖，而该探针当前 `TOTAL 18 / 19` 是红的）。

---

## 6. WP-05 · 把两台恒真护栏修成真护栏（P0，且必须做 R4 变异验证）

1. **`tests/unit/test_token_coverage.py:12`**：`ROOT = dirname(dirname(__file__))` 少上溯一层 ⇒ glob 指到 `<repo>/tests/app/ui/qml/**/*.qml`，**实测匹配 0 个文件** ⇒ 四类计数恒 0、永远绿。同型 bug 在 `test_config_write_guard.py` 已于 `40a812b` 修过，这个文件漏修。
   - 修完必须打印"扫到 N 个 .qml"，且 N>40；并加一条"目录不存在即 fail(fail-fast)"，防止将来又扫空目录。
   - **当前真值本应立即红**：`Qt.rgba(Theme.x.r` 实测 **5 处 > 基线 3**（净增来自本波次 `AppCard.qml:21,29`）；`font.bold` **53 > 基线 52**（`AboutDialog.qml` 净增 1）。把这两处收干净，或按事实把基线一次记准并说明**为什么允许升**（默认不允许）。
2. **`tests/unit/test_receipt_guards.py:100-113`**：那条"except:pass 后紧跟成功回执即违规"在 `Try.body` 里找 `ast.ExceptHandler` —— 处理器实际挂在 `Try.handlers` ⇒ `st_ is node` 永不成立 ⇒ `parent_body` 恒 `None` ⇒ **该测试不可能失败**。按 `.handlers` 修，并对 `ast.Module` 分支做成真能命中的父节点定位。
3. **同文件 `:19-20` 缺逗号**：`"setText"` 与 `"stage_params"` 粘成 `"setTextstage_params"`、`"normpath"` 与 `"snapshot"` 粘成 `"normpathsnapshot"`（实测集合 61 项）⇒ `setText` 从不在证据集里。补逗号，并**撤销为此登记的 `copyText` 豁免**（那条豁免的理由陈述本就建立在粘连 bug 上）。
4. **收紧证据集**：`EVIDENCE_CALLEES` 里 `read_file` / `list_chapters` / `exists` / `append` / `update` / `start` / `run` 这类**纯读或纯刷新名当"生效证据"**，会把门禁退化成"函数体别是空的"。剔除弱证据名，逐条给留下的名字注明它为什么算写/生效。
5. **补两台根本不存在的仪器**（台账 §7.3 列了、`tests/unit` 里查无此件）：**审计痕可见性**（`forced_locks` / `chapter_need_human` / `is_review_stale` / 归档目录必须有界面读者，缺一即红）、**红线静态扫描**（delegate 内出现 `MultiEffect`/`layer.effect`/`Canvas`/缓存 `property color`/`anchors` 于 Layout 子项即红）。
6. **能力清单↔注册表对账补齐半条**：现在 `:53` 只查 `callable(fn)`。要加"QML 文案/README 提到的动作必须在注册表里"与"注册了但无路径可触发 ⇒ 炸"，否则 L1-04/L1-07 这类还会再长。

---

## 7. WP-06 · 探针舰队返绿（P1，WP-01 的前置）

主控实测（顺序跑、退码取法已修正）当前不绿 6 支，逐支给结论：

| 探针 | 实测现象 | 要做 |
|---|---|---|
| `probe_panel_fit` | 打 `TOTAL_ISSUES = 0` 却 `PROBE_DONE FAIL`，**退码 0**。判据在 `:291` 含 `errs`，而 `:304` 是 `sys.exit(2 if (bad or FAILS) else rc)` ⇒ `errs` 不进退码 | 把 `errs` 纳入退出码；再查那批 QML 错误是什么（`RENDER_FAILS` 在 `:256/:277` 另有两条判定，需逐条判是真缺陷还是判定过期） |
| `probe_settings_tabs` | `RC=1 PROBE_DONE FAIL` | 归因到产品或断言，二选一写明；不许改判定刷绿 |
| `probe_gate_ui` | `RC=1 TOTAL 7 / 8` | 同上（这轮动过门条与两档制，优先怀疑断言过期，参照台账 #62「探针比被测代码年轻」的处理法：改断言要写明契约） |
| `probe_console` | `RC=1 TOTAL 18 / 19` | 与 WP-04 的 consoleSubmit 覆盖同批修 |
| `probe_cw_batch_ui` | **超过 300 秒挂死**；`:180` 还有 `readerChapterList()` 调用式；`:219` 槽内 `sys.exit` + 无看门狗 | 加看门狗与超时退出；修调用式；查挂死根因（疑真回归） |
| `probe_usage_dialog` | `RC=0` 但**无结论行** | 补 `PROBE_DONE PASS/FAIL`，默认失败（上游崩了不许假绿） |
| 同族残留 | `probe_update_ui.py:405`、`probe_about_ui.py:162`、`probe_cw_batch_ui.py:219` | N-31 只修了 6 支，这 3 支同形：槽内 `sys.exit` 不可靠 / 无看门狗 ⇒ 一并修 |

**完成定义**：`python` 顺序跑全部 `tests/probe_*.py`（**需真 Key 的 5 支除外，要在报告里单列"环境不判"**），每支贴原样结论行；`cw_batch_ui` 不再挂死；并把这份名单固化进 CI（WP-01）。

---

## 8. WP-07 · 门禁与文档的自相矛盾（P1）

- **L1-17 没做完**：`docs/release_checklist.md:10` 仍要求跑**已被你删掉的** `dual_sync_check.py`；"七道闸门改六道"的计数在 checklist/README/台账里未同步。全仓 `grep -rn "dual_sync"` 清零（除 CHANGELOG 历史说明），闸门计数三处一致。
- **N-21 的修复被打包顺序吃掉**：`scripts/build_release.py:327-330` 读 `out_dir/{LICENSE,THIRD-PARTY-LICENSES.md,PRIVACY.md}`，而这三份到 `:335-338` 才从 ROOT 拷进 `out_dir`；`out_dir` 按版本号命名 ⇒ **新版本首构建/干净机必然不存在** → `os.path.exists` 假 → 静默少打三份，而《使用说明》`:185,189` 仍承诺"详见包内"。改成先拷再打，或直接从 ROOT 打进 zip；**加一条验包内清单的门禁**（列出 zip 内应有所需文件名，缺即红）。
- **L1-15 的销号不合规**：用了 §10 允许集外的状态值、缺 sha 与护栏名、且"70 章 / marginal 55.7%"零可复查产物。要么把取证材料入库（脚本 + 输出）再按规范销号，要么退回「取证中」。
- **N-05 覆盖面**：回执三态门禁只解析 `app/ui/bridge.py`，`stages.py` 那 4 处站点在扫描面外 ⇒ 扩充到 `app/core/`，或明确写"只覆盖 bridge"并把 stages 侧另立任务。
- 台账里 **L1-03 注记重复两遍**且无护栏名，按 §10 重写。

---

## 9. WP-08 · L2 语义级余项（P1，含 Q6 的免裁决半步）

复核裁定：L2 真绿 14 / ◐ 6 / ✗ 6。以下都要做：

- **L2-13 有回看口但回看不了**：`bridge.py:3017-3023` 取 50 条，`AboutDialog.qml:165-180` 只显 8 条，而 `text: { …forcedLocksList()… }` 是**方法调用式绑定、永不重算**——正是 N-02 的同一种病。改 `forcedLocksChanged` notify 或 Loader 重建。顺带：审计痕现在只挂在「关于」弹窗，是否够，交给用户判（写进汇报让他定）。
- **L2-09 / L2-10 / L2-11 / L2-12（门停靠说不清拦什么）**：`orchestrator.py:43` 的 `sig_gate(str,int,str)` 未动；摘要仍由各调用点硬拼（`stages.py:1633/1891/1950/2127/2832`）；`orchestrator.py:136-137` 在源头 `summary[:200]` 截断，QML 那 4 行换行上限拿不到全量；`bridge.py:4006/4012` 强锁对话框仍只取 `blocking[0]`；G8 的"每行一条阻断问题"落在一个 `onReturnPressed: doNext()` 的单行框里（`StepGateBar.qml:107` vs `:121`）——物理上录不进去。
  - **免裁决半步（现在就做，别等 Q6）**：`GATE_META` + `gateMetaList()`（`bridge.py:1407-1416`）已经有"这个门是干什么"的表，但只有 `PipelinePanel.qml:53/:486` 在用，**门条没接**。把它按 `gateKey` 接进 `StepGateBar` 即补齐"拦哪条 / 能做什么"，零载荷风险。同时把 `[:200]` 的源头截断去掉或提额。
  - **必须裁决才做的半步**：「当前值 vs 阈值」与「违规全清单」要进停靠界面 ⇒ 只能改 `sig_gate`/`lockBlocked` 载荷 ⇒ **停下问用户（Q6）**。
- **L2-21**：`Main.qml:1663-1667` 仍是单条 toast `text=msg` + 2600ms，后进覆盖先进；连发场景会吞掉章完成回执（与 U-13 交叉）。要排队或分级。
- **L2-22 版本历史**：`Main.qml:2088` 仍只列当前章，未做。
- **N-24 补完**：`test_release_readiness.py:34-42` 现在断言的是"手工调 `record(cfg,"app_start")` 后文件里有它"——仍没证明**生产代码会调**。补一条静态断言：`main.py` 必须存在 `record(cfg, "app_start"…)` 与 `record(cfg, "version"…)`。
- **N-29 收尾**：`README.md:465` 的「73%+」还没标旧口径（`:249` 已标）；`:29` 的 96% 是"理论终点"未实测，与 §6.1 口径对齐后再写。

---

## 10. WP-09 · L4 余项（P1-P2，"高级感"还没真正落地）

- **零消费者的令牌与组件**（等于没做）：`AppCard` 的 `elevation`（`AppCard {` 实例化 0 处）、`Theme.focusRing` / `focusRingWidth`（0 引用；`AppField.qml:39`、`AppSelect.qml:20`、`AppSpinBox.qml:29`、`AppButton.qml:69` 仍是 1px 换色）、中性色阶 12 阶×3 主题与 `shE3Alpha`（0 引用）、`AppSwitch {` 0 处。**要判完成，必须让卡片浮起与聚焦环真实出现在界面上**，并说明哪些令牌是"预置未用"（预置可以，但要写明）。
- **U-12 名不符实**：`Main.qml:767-776` 的"贴底才跟随"真落地了，但注释宣称的 `contentY` **Behavior 不存在**（赋值瞬时，无平滑）。补平滑或改注释，二选一。
- **U-13 一次性徽标零命中**：呼吸点有了（`Main.qml:271-289`），"第 N 章 ✓"的一次性完成徽标没有（`QueueRow`/`ChapterPanel` 只有 writing 态 `AppBadge pulse`）。
- **U-20 未完**：完本庆祝 0 命中；`DialogBg.qml:19-23` 阴影仍硬编码 `#2E000000` 未入主题表；9 处滚动条里 6 处仍是原生。
- **红线违规**：④ `fsMicro` 用在整句中文上——`Main.qml:610`「✓ 已确定（终稿锁定）」**是本波次 L2-15 新增的**、`StepGateBar.qml:170` 快捷键整句；⑦ 仍做 relayout 动画的：`StepGateBar.qml:17`（`Behavior on height`）、`ConsoleDock.qml:19`（`Behavior on Layout.preferredWidth`）、`Main.qml:1656-1657`（width+height）、`AppStatTile.qml:55`、`StageStepper.qml:73`。①②③⑤⑨⑩ 未检出违规（阴性也要写进汇报，省得下轮重查）。
- **U-16 门条**：与 WP-08 同一件事，别做两遍。

---

## 11. WP-10 · L3 与 §7.4 判据（大部分需要你在场，先报计划再动手）

- **L3-01 §5b 真机升级矩阵 8 例：①-⑧ 全部无证据**。`docs/release_checklist.md` 现版 **58 框 0 勾**（没有凭空打勾，这点是好的），`dist/release/` 最新仍是 v0.19.9。要求：**先交执行计划**（每例怎么验、验完留什么证），得到用户同意后再逐例执行并把证据落到 checklist（截图/退出码/日志路径），**逐例勾，不许批量打勾**。
- **L3-03 干净机**：新 Windows 用户账户或虚拟机走一次安装向导（安装器 `[Code]` 目录拦截目前只验到"能编译"）。同样先报计划。
- **L3-05 私钥 / L3-02 签名**：`scripts/update_keys.py` 本轮零改动（仍只 print 一句提醒）、`app/update_check.py:45-47` 的 `PUBKEYS` 未被动——**这两个是 Q4/Q3 待裁决，不许自己决定**。你只需把"备份怎么做、要多少时间"写成选项交给用户。
- **§7.4-2 判据（尺 5）零产物**：台账要求自动档与共写档各 ≥10 条**越界话术**语料 + 逐条结果，`tests/` 与 `tests_output/` 里查无此物。没有它，"Agent 帮得到"这句无法验收。请把这批语料做成可重复跑的离线夹具（打桩，不发网络），输出报告入库。
- **§7.4-7 视觉回归**：把 `tests/shot_ui.py` 的 `offscreen` 换成 `QT_QPA_PLATFORM=windows` + 显隐一次强制合成（否则全豆腐块/全上一帧），产出**成套 before/after**，交给用户逐屏点头才算完。
- **§7.4-8 数字诚实**：`CHANGELOG` 称 1100 而实测 `test_` 函数 1084；快照里还留着"932+ 回归"。所有对外计数重新校准一遍。

---

## 12. 建议执行顺序（严格串行，别并行改同一文件）

```
WP-02 假销号 → WP-03 谎报+selftest → WP-05 恒真护栏(含 R4 变异验证)
  → WP-06 探针返绿 → WP-01 恢复 CI(需用户授权推送)
  → WP-04 Agent 有名无身 → WP-08 L2 语义级(免裁决半步先做，Q6 停下问)
  → WP-07 文档与门禁自洽 → WP-09 L4 余项 → WP-10 L3/判据(先交计划，多数需用户在场)
```
理由：CI 必须晚于"探针失败会退非零"，否则接上去的是假绿；护栏必须早于销号，否则又是装饰。

---

## 13. 每轮汇报的固定格式（缺任一项视为未完成）

1. 命令原样 + 输出原样（`N passed` / `PROBE_DONE PASS` / `TOTAL n / m`）；
2. 每条 `✓` 对应的 commit sha 与**护栏文件:行**；
3. 每台新护栏的**变异验证记录**：破坏了什么 → 报了什么错 → 还原后绿；
4. 阴性结论也登记（扫过没有发现的，写"查过、方法、结论=无"），避免下轮重查；
5. 停下来问人的条目：Q3 / Q4 / Q6、L3 需用户在场的执行窗口、任何需要放宽规范才能变绿的地方。
