# 给 zcode 的交接消息（草拟，未发送）

> 状态：**未发送**。目标会话 ID `sess_c70657bd-1938-4a03-a1fb-cc226265b6ae` 未在 Qoder 登记，无法投递。
> 本文是待发正文，自足（不依赖发出方会话的记忆）。

---

## 一、我是谁 / 这条消息从哪来

`G:\ai\酒馆\qianbi-novel`（千笔一文）里的另一个代理，刚做完 W 轮成本止血。你也在同一工作区干活，
所以先讲**协同纪律**，再讲成果，最后讲下一步与要你定的事。

- 共享看板：`docs/测试共享状态.md` §5（追加式，newest 在上）。**改它之前先 `git pull` 再重读**，
  插入时把下面那条锚点行原样带回，别吃掉别人的条目（今晚已发生一次误吞风险，靠这条纪律躲过）。
- 提交纪律：一项一 commit，`git log --stat -1` 对着 `git status --porcelain` 核文件清单
  （今晚我用窄 pathspec 漏了两个文件，本机全绿、**CI 三连红**，已补交修绿 `8d24173`）。
  碰过 `app/` 的必须 push 让 CI 在干净环境跑一遍——**CI 复验的是提交，不是工作树**。
- 推送：全局代理 `127.0.0.1:7897` 今晚会挂（`Could not connect via 127.0.0.1`），直连可用时
  用一次性 `git -c http.proxy= push origin main`；**别把 `http.proxy=""` 固化进配置**。
- 渠道链现值：`scripts/cost_bench.py:89` = `["ocgo-omen","bailian-flash","ds-v41-flash","tr-dsv4f"]`。
  **换渠道＝换模型＝换缓存域**，发跑前必查此行。

## 二、这轮做完的（全部离线代码＋回归，09-09 全天零真机）

| 项 | 一句话 | 位置 |
|---|---|---|
| W-1 | 卷号解析把「第14-25章」的 25 当本卷章数累加 ⇒ **真卷界永不换栈**（栈长到 88 万 tok 的主因）。已按绝对末章号切分 | `app/core/volume_session.py` |
| W-3 | 三处"整栈静默作废"改 fail-loud：坏行**截尾恢复**（旧=全栈弃）＋告警带轮数/tok/一次 miss 的钱＋`save()` 原子写＋**章内三处里程碑落盘**（崩在同一章不再重打整卷） | `volume_session.py`、`stages.py` |
| W-5 | 盲区 token 口径三工具同源（`cost_bench`/`chapter_curve`/`cost_ledger` 过去一个虚高一个虚低）；`hit%` 只在已知行上算；钱给**账面/真实**两数。台账重算 **2479 笔 ¥114.92／¥96.96**，并补录 6 个一直漏账的 V7 取证变体 **¥9.907** | `app/usage.py` 的 `cache_caliber`/`blind_spread` |
| W-4 | 429/5xx 与空内容两条 `continue` **绕过退避睡眠**（T3 被 TR 的 503 三连打死就是这个机制）⇒ 统一到 `_retry_wait`；重试/空流/中止落 `st` 行，台账分列 `retry_spend` | `app/llm/client.py` |
| W-2 量具 | 新脚本按**整条消息**归属逐行判重：独立复算重复率 61.3%／56.8% ⇔ 审计的 60.5% 互证；另给"开幕轮分节"表 | `scripts/stack_redundancy.py` |
| W-7 | 全局摘要已在栈里时改发**引用行**（判定逐字节确证）；清算 triage 不再要求输出；清算终审判档位改**可配且默认不变**；`reasoning_effort` 配了没开思考则告警 | `stages.py`/`canon_audit.py`/`client.py` |
| W-6 | 票指纹仪器（sha1＋六维等级向量＋回声/空转），聚合成 `metrics["vote_iso"]`——**没这个数不许谈降票** | `stages.py`/`cost_bench.py` |
| W-8 | 诊断：**我们固化进栈的是 `clean_llm_output` 之后的字节**，而 DeepSeek 命中要求逐字节完整匹配"模型输出末端"单元 ⇒ 一个换行就可能让该单元之后全部不吃缓存（能同时解释"章界固定 ~32k miss"与"原样重放 99.9%"）。只装仪器 `turn_cleaned`，**行为未改** | `volume_session.py` |

回归基线 **977 passed / 1 skipped**，`scripts/prefix_lint.py` 全绿，CI 已复验到 `45d6a96`。
**尚未做且刻意不做**：②-d 章头去重（实测那五节是当年刻意保留的 recitation，需质量 A/B 才能砍）、
②-a 旧稿不入栈（结构上**不能事后删**，只能"一开始就别整章输出"）。

## 三、下一步（请以此为准，它推翻了 W 轮清单的优先级）

深化版在 `docs/下一步深化计划_v2_20260910.md`，要点：

1. **钱几乎全在输入结构**：瀑布 L3 −¥0.05 → L1 −¥0.09 → L2 −¥0.04 → L6 −¥0.017 → **L4 每章重建 −¥0.15**
   ⇒ 章均从 ¥0.43 有望到 **¥0.09**；而**降票只值 ¥0.007/章、effort 降档实测只差 1.06×**，已降级为次要。
2. **E0 探针是总闸**（明早第一件事，¥2-4）：拿第 53 章 416k 真字节按 8k/16k/32k/48k/64k/128k/256k
   截前缀各发一次看 hit 台阶。台阶只在 32k 整数倍 ⇒ 架构重建作废，改走备择 B'（章界那笔与前缀同构）；
   命中随长度连续增长 ⇒ L4 可直接 A/B。⚠️ 工具缺口：`replay_request_probe.py` 只能按**消息条数**切，
   需要 ~10 行加 `--truncate-tok`（纯离线，可先做）。
3. **排期是一倍现金**：09-10 是周四，峰时 9-12/14-18 **三桶 ×2** ⇒ 同一批 ¥41-50 上午跑会变 ¥82-100。
   离峰窗口 00:00-09:00／12:00-14:00／18:00-24:00；`deepseek-v4.1-flash-expires-on-0910` 当天到期。
   我的建议：**砍臂不砍时段**。
4. 明天要读的仪器格：`metrics["turn_cleaned"]`、`vote_iso`、`session_events`、`retry_spend_cny`、`blind_pct`；
   跑完立刻 `python scripts/stack_redundancy.py --variant <新变体> --top 8` 复量
   （**完成判据：每章新增字/章输出字 从 1.22-1.63 压到 ≤0.6**）。

## 四、要你和用户定的三件事

1. L4 赌注：E0 判定可行后，要不要花一个上午（¥8-12）跑"每章重建"一臂（唯一可能再降 3 倍的杠杆）。
2. 模型到期 vs 离峰省钱的取舍（见上）。
3. 分工：L1/L2 都要碰 `app/core/stages.py` 与 `app/core/shared_prefix.py`——**谁改哪块要先说定**，
   否则会互相覆盖（今晚已因同时改看板踩过一次险）。

## 五、别重提的（都已有反例或被否证）

事后删/改栈历史 · 通用提示压缩（LLMLingua/RECOMP 一类）· 百炼显式 `cache_control` ·
`max_tokens` 当思考帽或流式截断 · 把相位外迁便宜模型 · 为保活缓存发心跳 · 让模型自选"补丁还是全文" ·
把三票改串行（我方 8 变体数据否证：副本票 miss 138～425 tok，首票 3.1k～7.6k）。
