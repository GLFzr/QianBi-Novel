# 下一步优化方案：结构级调整（v16 提案，待用户裁定）

> 性质：深度分析与整体方案，**未做任何代码改动、未发跑**，供用户裁定后执行。
> 数据底稿：v15 完整 30 章逐笔（v15_fresh 1-24 + v15_tail 25-30），全部真机实测。
> 口径：综合命中率 = 命中 ÷（命中 + 未命中 + 输出）。

---

## 一、病根重定位：整个结构里最影响命中率的是什么

### 1.1 实测 X（miss+output）构成，30 章均值 119.7k/章

| 排名 | 相位 | 笔/章 | miss/章 | out/章 | 合计/章 | 性质 |
|---|---|---|---|---|---|---|
| **1** | **canon_audit** | **5.8** | 9,238 | **28,678** | **37,916** | **级联自我放大** |
| 2 | prose | 1.0 | 18,879 | 5,761 | 24,641 | 每章合法新料 |
| 3 | deslop | 0.83 | 14,014 | 1,094 | 15,109 | 材料重贴+卷界 |
| 4 | review | 2.9 | 13,468 | 1,190 | 14,658 | 写延迟+彩票 |
| 5 | trim | 0.23 | 9,179 | 542 | 9,722 | 字数超标链 |
| 6 | tracking | 1.0 | 4,964 | 3,411 | 8,376 | 合法增量 |
| 7 | 其余（enrich/fix/outline/摘要） | — | 5,017 | 1,194 | 6,211 | 小额 |

### 1.2 三个结构性缺陷（按影响排序）

**缺陷一：清算管道的"截断→重试→再截断"自我放大（37.9k/章，第一大鲸鱼）**
- 实测：5.8 笔/章（正常应 1.1），平均每笔输出 4,973 tok——**恰好贴着 5000 上限**。
- 机理：书变稠（30+ 实体）后清算 JSON 变长 → 顶到 max_tokens 截断 → JSON 解析失败 →
  重试（独立单发、全价重发 ~110k 输入）→ 又截断 → 升级 pro → 再花一笔。
  **上限从 8192 砍到 4000/5000 治好了爆炸，却制造了截断级联**——治标引出了新病。
- v11 对照：1.07 笔/章、out 3.7k/章（书稀 + 无截断级联）。

**缺陷二：头部 =「按卷拼接」而非「单一追加流」（卷界塌陷的根因）**
- 每个卷界：设定底册/世界书快照重冻结（卷内反哺使字节必变）+ 语料快照换新文件
  → 头部从变化点起全部失效 → 塌陷章（ch14/15/26/27）X 翻倍至 130-220k。
- 语料滚动（corpus_roll）解决了"卷内"的追加，但**卷界本身仍是头部重置事件**——
  因为基座层（底册/世界书）和语料层（章节全文）被绑在同一个按卷重建的文件里。

**缺陷三：字数超标链（trim 9.7k/章 + 一次彩票抽奖）**
- 细纲「预算合计」行与正文实际产出偏差 → 超标 → trim 独立单发（40k 级 miss/笔）
  → 修完还可能再触发。

### 1.3 为什么"稳态能到 95%"没有意义（用户判断的机理确认）

稳态章 87-91.6% 的曲线，每遇到一个卷界就被打回 70-80% 两章。按 13 章卷长：
**每卷 26% 的章节处于塌陷区**——全窗累计被结构事件锁死在 85-88%。曲线不平滑
的根源不是命中率工程没做好，而是**头部结构把"卷界"定义成了重置事件**。

---

## 二、结构级调整方案（S1-S5）

### S1 清算管道重构（第一优先，-25k/章）

1. **上限与解析匹配**：max_tokens 回到 8192；解析器加**截断 JSON 修复**
   （按最后完整数组元素截断 + 闭合括号），截断不再是失败。
2. **重试入会话**：重试与 pro 终审升级改为会话追加轮（失败轮 rollback 不留史，
   重试措辞扰动保留）——重试从"全价重发 110k"变"会话增量 ~5k"。
3. **输出瘦身 schema**：adoptions 数组 ≤12 条（超收编走下一章）、ledger_updates
   只记 delta——从源头把清算输出压回 3k 内。
- 预期：5.8 → ~1.2 笔/章，37.9k → **~12k/章**。
- 质量门：雷章召回 B01-D01 不降（E9' 教训：清算是最不能弱的闸）。

### S2 书级单一追加流头部（第二优先，卷界税归零 + H 单调）

1. **基座冻结上移到书级**：设定底册/世界书/卷纲在 ch1 冻结一次，全书不再重建；
   世界书卷内反哺走开幕轮近场字段（现状已有，正补偿）。
2. **语料流全书一个文件**：章节全文+细纲+摘要链只追加，卷界零事件。
3. 迁移：已有书首次切换时装配一次书级流（一次性 ~150k miss，此后永不再付）。
- 预期：每卷 2 个塌陷章消失（26% 塌陷占比 → 0）；H 每章严格单调。
- 风险：底册/世界书头部陈旧 → 近场补偿 + 人工检查点重冻（预注册一次性）。

### S3 字数链治理（第三优先，-7k/章）

- 细纲预算行抽检进预生成校验（预算合计 vs 章纲内容密度）；
- prose 首稿字数自检强提示已有 → 追加"超标立即自收束"；
- trim 触发 ≥2 次的章打标（产出质量观测）。

### S4 开幕轮密度压缩（-5~8k/章，第二批次）

- 八节中已被语料库覆盖的成分继续引用行化（corpus_mode 深化）；
- 角色状态/时间线等动态节保留（合法新料）但压缩格式密度。

### S5 调用数合并（-1~2 次抽奖/章）

- 章摘要+全局摘要合并（调整七遗留）；每减一次调用 = 少一次彩票抽奖
  （期望 3-5k/次）+ 少一次头重读。

---

## 三、数学：这些调整能把命中率带到哪

- X 稳态 = 彩票 25-45k（不可控外部项）+ 清算 12k + 开幕新料 12-15k + deslop/review
  材料 12-15k + tracking 5k + 输出残余（audit 治理后）≈ **65-80k/章**
- H = 头部 tok × 每章调用数。成熟书（45+ 章，书级流）头 180-250k tok × 8-9 笔
  ≈ **1.6-2.2M/章**
- **稳态综合 = 1800/(1800+70) ≈ 96%**；30 章新书窗约 88-91%（头部尺寸所限，
  结构性，与调优无关）。
- 新增曲线健康度指标（用户判断的量化）：**单章综合较前章跌幅 ≥5pt 的次数 = 0**
  ——卷界塌陷归零后此指标应恒真。

---

## 四、执行顺序与预算（待裁定）

| 步 | 内容 | 成本 | 判据 |
|---|---|---|---|
| 1 | S1 清算管道重构（离线）+ 单测 | 0 | 截断修复解析单测过；雷章召回不降 |
| 2 | S2 书级流头部（离线）+ 单测 | 0 | 追加前缀性质单测；关态字节一致 |
| 3 | 质量门：13 章盲评 + 雷章召回 | 0（子代理） | D2/D4 ≥ -0.5；召回不降 |
| 4 | 常态跑：v15 书 ch31-56（26 章，含 2 个卷界） | ¥8-10 离峰 | 塌陷指标=0；稳态窗综合 ≥93%（S1+S2 首轮目标）；95% 视彩票表现 |
| 5 | 达标 → 发布 v0.20.0；未达标 → 按 S 项归因回退 | — | — |

**需要用户拍板的三件事**：
1. S1 的清算输出瘦身（adoptions ≤12 条等）——会轻微减少"收录完整性"，是否接受？
2. S2 的书级冻结基座——世界书卷内反哺不再进头部（近场补偿），是否接受？
3. 常态跑授权（¥8-10，离峰）与 95% 判定口径：推荐"成熟书稳态窗 ≥95% 且塌陷=0"，
   全窗累计如实并行报告。

---

## 五、与已否证清单的边界

本方案不触碰：事后删栈/改历史 · 通用提示压缩 · 心跳保活 · 相位外迁便宜模型 ·
让模型自选格式。S1/S2 均为**请求体结构与管道流**的改造，全部旗标化、关态字节
一致、独立可回滚——与 0.19.5 以来的纪律一致。


---

# 修订 v2：质量不变量深研后的修正（同日）

> 用户约束：**结构性调整不能影响输出作品的成熟度和质量**。据此对 S1/S2 做了
> 数据级深研，两处根因判定修正，方案重写如下；v1 中与之冲突的表述以本节为准。

## 一、根因修正一：清算级联 = 思考溢出，不是 JSON 截断

实测（v15_tail 30 份清算报告 + 逐笔 usage）：
- 清算产物极轻：violations **0.3 条/章**、adoptions **0.2 条/章**、ledger **0.3 条/章**
  ——可见 JSON 结论每笔只有几十 token；
- 但 audit 推理/输出比 = **100%**（推理 28,639 tok/章 ≈ 输出 28,678 tok/章），
  每笔输出贴着 5000 上限——**全部是思考，结论还没开始写就到顶**；
- 思考到顶 → 无可见输出 → JSON 解析失败 → 重试再想 5000 → 再失败 → 升级 pro
  → 5.8 笔/章级联。**v13 时代"截断级联"的定性是错的，真正的机制是思考溢出**。
- effort=low 实证无效（ReasonIF：推理模型对软提示的思考控制遵循率低）——
  思考治理必须走 thinking 参数本身。

### S1（修订）：清算思考治理三件套

| 件 | 内容 | 质量风险与门 |
|---|---|---|
| 1a | 清算预扫 **thinking: disabled**（对照检查任务，V4 报告：机械任务无差距） | 雷章召回 B01-D01 实测定案（现成测试资产）；引证验真与 beat_check 程序复核在环；召回降即回退 |
| 1b | 重试/pro 升级**入会话 + 失败轮 rollback**（同前缀命中价重发，无上下文污染——rollback 后上下文与首试逐字一致） | 零语义差异（同提示同上下文），纯缓存路径改造 |
| 1c | max_tokens 维持 5000（思考关后 out=结论 JSON ~1-2k，不再触顶；同时保住 v12 教训的爆炸闸） | 无 |

- 预期：5.8 笔/章 → ~1.2；audit 38k → **8-12k/章**（推理 28.6k→~0.5k）。
- 依据强化：v15 实测 violations 仅 0.3 条/章——当前"思考到顶"并没有换来更多
  发现，说明溢出的思考是无效复查而非深度检出；雷章召回门兜底。

## 二、根因修正二：卷界税实测隔离 + S2 的双写歧义消除

- 卷界章（14/15/26/27）X = **224.6k/章** vs 稳态章 **98.7k/章**——每卷 2 章
  共多花 ~25 万 tok miss，纯结构税。
- S2 质量风险重审：v1 版让"世界书卷首冻结进书级头"——但世界书是**动态文件**
  （卷内反哺持续追加），书级冻结会使头部副本越来越陈旧，与开幕轮近场新鲜副本
  形成**双写歧义**（模型看到两个版本的世界书）。修正：
  **世界书不入书级头，保持开幕轮近场单副本**（现状对 prose 本就双写，
  本修正消除而非引入歧义）；书级冻结只收**真静态**：题材定位底册 + 卷纲 +
  语料流（已锁章节全文/细纲/摘要链）。

### S2（修订）：书级单一追加流（三段真静态）

| 段 | 内容 | 为什么静态安全 |
|---|---|---|
| 基座 | 题材定位（底册）+ 卷纲 | 反哺不写这两文件；作者改动走人工检查点重冻 |
| 语料流 | 已锁章节全文 + 全部细纲 + 章节摘要链 | write-once，追加式 |
| 世界书 | **不入头**——开幕轮/审校/清算近场单副本（现状字段） | 消除双写歧义，陈旧性问题不存在 |

- 预期不变：卷界税归零（每卷 2 塌陷章消失），H 随章数严格单调。

## 三、质量不变量总表（每项调整的"质量为什么不变"）

| 调整 | 质量不变量 | 验证门 |
|---|---|---|
| S1a 思考关 | 清算=对照检查，结论由程序复核（pattern/beat/引证） | 雷章召回 B01-D01 实测不降，降即回退 |
| S1b 重试入会话 | 同提示同上下文（rollback 保真），仅缓存路径不同 | 单测钉 rollback 后上下文一致 |
| S2 书级流 | 章节全文/细纲/摘要逐字入头（内容零损失）；世界书近场单副本 | 快照字节=源文件逐字拼接单测 |
| S3 字数链 | 不触碰正文生成 | trim 触发率观测 |
| S4 开幕轮压缩 | 动态节保留 | 第二批次，盲评 D2/D4 前置 |
| S5 摘要合并 | 全局摘要输入=章摘要（语义不变） | 盲评 |

## 四、修订后数学

X 稳态 98.7k → S1(-26k) + S2 卷界摊销(-8~10k) + S3(-3k) + S4(-5k) ≈ **55-65k/章**；
H（成熟书 45+ 章，头 150-220k tok）× 8-9 笔 ≈ 1.4-1.9M；
**稳态综合 ≈ 95.5-96.5%**，曲线健康度（无 ≥5pt 塌陷）由 S2 结构性保证。


---

# 修订 v3：全量异常扫描的五个新发现（同日二次深挖）

> 方法：v15 全量 173+465 笔逐行异常扫描（独立单发签名/票数分布/时序间隙/冷击精算/
> 大输出→大 miss 紧邻对）。全部有逐笔数据背书。

## F1（最大新发现）：清算会话化旗标静默失效——173 笔全部独立单发

- 实测：173 笔 canon_audit 的输入**全部只有 ~6.3k tok**（独立单发的全量 prompt），
  会话内清算应为 15-20 万 tok 输入。**S2 入会话分支从未触发**（v13 的 47k/笔 同样
  是独立单发签名——该功能可能从未在任何跑次生效过）。
- 代价复算：173 笔 ×（miss 6.3k... 实付 miss 合计 ~143k）+ **思考输出 173 × 5k
  = 865k tok（28.8k/章）**——audit 的输出爆炸主体就是这 173 笔独立单发各自的
  思考溢出，每笔都从零开始想一遍。
- 修复：debug 旗标链（_phase_flags 的 in_session 为何未生效——预设字段已确认
  存在，疑点在 same_domain 判定或 session 传递）+ 重试入会话（S1b）+ 思考治理
  （S1a）。**这是 v16 第一实现项**，也是"静默失效比报错更糟"的又一案例
  （V1-③ 同类）。

## F2：路由冷击实付成本精算 = 16.9k/章（506k tok/30 章）

- 修正 v2 的"彩票 25-45k"高估了：**事件行实付 miss**（真实浪费口径）= 100 次
  506,274 tok = 16.9k/章。分布：audit 143k（实为 F1 的独立单发重付）、trim
  108.8k、deslop 107.6k、prose 103.3k、enrich 43.6k——大请求冷击集中在
  prose→trim/deslop/review 的紧邻转换（prose 刚写完 2-10k 输出，下一笔大请求
  落冷节点全头重付，ch27 trim 单笔 miss 108,754）。
- 可控抓手：**减少紧邻大请求数**（S5 调用合并）+ 紧邻大请求前强制注册窗
  （pace 已有，但 ch27 显示 45s 窗对冷击无效——冷击是节点级不是写延迟级，
  唯一结构抓手是"少抽"）。

## F3：审校快速道失效——24/24 章满投 3 票

- `review_pass_fast` 判据要求首票 verdict ∈ {PASS, PASS_WITH_NOTES} 且零 blocking；
  实测 27 章 3 票满投（1 章 4 票、1 章 6 票=修复环）。首票几乎全是
  PASS_WITH_NOTES（fail=0 但 blocking 非空的场景需复核——marginal 不应进
  blocking）。若判据有 bug，修复 = 每章省 2 票 ≈ -2 次大请求+彩票。

## F4：<10s 紧邻调用 83 次

- 章内相邻调用间隔 <10s 的有 83 次（review→review 副本 1s、tracking→摘要 2s
  等）——写延迟窗口未覆盖的转换。小额但真实。

## F5：世界书/正则块的多相位重复注入（待量化）

- PROSE/REVIEW/TRACKING 模板各携带一份 worldbook_block/regex_block 近场拷贝
  （~2-2.5k tok/份）——同一章内同一字节内容在 3-4 个不同字节位置各付一次 miss。
- 结构性修法（与 S2 修订呼应）：世界书入书级语料流的**追加分**（卷首快照+卷内
  追加登记，append-only 无双写），各相位近场改引用行。预期 -4~6k miss/章。
- 质量风险：审校近场对表的便利性下降——需盲评门。

## 修订后 X 账本（v16 全部落地后的稳态预期）

| 项 | 现状 | v16 后 | 抓手 |
|---|---|---|---|
| audit 全家桶 | 37.9k | **8-12k** | F1 会话化+思考治理+S1b |
| prose 开幕轮 | 18.0k | 13-15k | S4 密度压缩（保守） |
| deslop | 12.2k | 8k | pinned 已有+F2 冷击减少 |
| review | 10.9k | 7k | F3 快速道修复+副本窗 |
| trim | 4.9k | 2k | S3 字数链 |
| tracking | 8.5k | 6k | 上下文字段瘦身 |
| 冷击 | 16.9k | 10-14k | S5 调用合并=少抽奖 |
| 卷界税 | ~8-10k（摊派） | **0** | S2 书级流 |
| 其余 | 3k | 3k | — |
| **合计** | **~119.7k** | **60-72k** | |

H（成熟书 45+ 章，书级流头 150-220k tok）× 8-9 笔 ≈ 1.4-1.9M；
**稳态综合 ≈ 95.4-96.9%**；塌陷指标结构性归零。
