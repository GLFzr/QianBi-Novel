# B-2 切换矩阵 v1（交用户过目后再动手改——本轮只交付不改）

> 用户点名：cw⇄自动 两向各列「保留 / 丢失 / 被静默改写」。数据源：co_writing.migrate_mode、
> state.CW_STAGE_PRODUCTS、orchestrator.run 的产物存在性跳转、bridge._cw_open_product。

## 一、自动 → 共写（新用户主路径）

| 自动档已有 | 进共写后 | 判定 |
|---|---|---|
| 设定/题材定位.md | cw_core 视为已完成（_resume_stage 按产物存在性跳过）——**不会被重问设定** | 保留 ✓ |
| 大纲/大纲.md | cw_outline 视为已完成 | 保留 ✓ |
| 大纲/细纲_第N章.md | 单元细纲阶段按批文件照常可见/可改 | 保留 ✓ |
| （无）设定/世界书.md、正则.md | **停在世界书阶段被要求补做**——自动档从不需要它 | ⚠ 新增一道工序（用户须知情） |
| （无）大纲/单元总纲.md | 世界书确认后要求做单元总纲 | ⚠ 同上 |
| 已定稿章（locked） | cw_prose 可见、锁定语义保留（confirmChapterLocked 拒绝重复锁/改） | 保留 ✓ |
| state["stage"] | 被 cw 接管：机器阶段以 state.cw.stage 为准；_cw_view 回看线不变 | 改写=无害（双向各有指针） |
| state["stage"] 旧值 | migrate_mode 不写 state["stage"]，但自动档回来时 run() 按产物重新推断并覆写 | 静默改写=无害（产物是唯一真相） |

**丢失/断点**：无发现。唯一体验断点 = 世界书/正则/单元总纲三件 cw 专属产物对
自动档转来用户是「新增作业」——建议切换确认弹窗里明示。

## 二、共写 → 自动（批量补章路径）

| 共写已有 | 进自动后 | 判定 |
|---|---|---|
| 题材定位/大纲/世界书/正则 | run() 按存在性跳过 A1/A2；世界书进 rg 块 | 保留 ✓ |
| 单元细纲（细纲_第N章.md） | 自动 A3 按章检查，缺失才生成 | 保留 ✓ |
| 大纲/单元总纲.md | **自动档零消费**（cw 专属）——留着无害但不再被引用 | 静默搁置（写进确认弹窗） |
| 设定/选题信息.md | 自动档零消费 | 静默搁置（同上） |
| **total_chapters** | ⚠ **cw 全程不登记总章数**；自动档 while total==0 ⇒ 无「写完判定」，逐章补细纲直到用户停止——这是最大断点 | ⚠ 断点（修法：切换时若 cw 有单元规划，提示用户先设总章数；本轮先提示不改逻辑） |
| cw.stage 指针 | migrate_mode 只改 mode 不动 stage ⇒ 切回共写时**原地续跑**（含 reopening 清空） | 保留 ✓（可回得去） |
| 共写对话转写 transcript | 保留在 state["cw"]["transcript"]，自动档不读不删 | 保留 ✓ |

## 三、结论（待用户逐行过目）

1. 两向的**文件产物 100% 共享、互不删除**；断点集中在 ①cw 专属三件套的「新增作业感」②total_chapters 未登记的完成判定缺失。
2. 本轮不改切换逻辑；随 B-1 交付的只有：切换确认弹窗文案里追加两条提示
   （cw 专属产物 / 总章数），见 bridge.setCwMode。
3. 若用户裁决 total_chapters 需要自动迁移（cw 单元规划 → 自动档总章数），
   另开一轮实施（涉及单元规划解析，非纯文案）。
