# 2026.9.11 v11/v12 轮工作记录（协同协议第 4 条）

> **§0 v13 轮增补（同日续）**：用户指示"全量实现计划"后，v13 输出侧治理四项已实现
> 并推送（3a43d81 + 69f8527），R-v13 终验跑至第 35 章时 **DeepSeek 账户 402
> Insufficient Balance** 中断。5 章部分数据（30-34）：**综合 90.5%**（provisional，
> 非预注册 13 章口径；语料头随卷界滚动至 225k chars ≈ 169k tok，in/章 2.09M），
> 治理项全部机制性生效：audit out 14.5k→2.4k/章（max_tokens 4000+引用化）、
> outline 入会话实锤（"骑冻结头，miss 仅材料"日志 5 次）、pace_boundaries 落地。
> deslop/review_fix 的 miss 异常（47.9k/43.7k/章）疑似首达深度记账噪声，待续跑复核。
> **恢复命令**（充值后执行）：
> `QIANBI_BENCH_MAX_CHAPTERS=60 .venv/Scripts/python.exe scripts/cost_bench.py --variant v13_long13 --preset-params @tests/bench_variants/v13_stack.json --start 35 --chapters 42 --seed-home v13_long13 --user-id qianbi-bench-v11 --flash-conn ds-official-flash`
> **§-3 v15 轮（2026-09-12，用户令"进行v15的成本"）**：研究文档 §5.1 八项全量实现
> （corpus_roll 滚动快照+前缀守卫 / outline_pregen 预生成入头 / audit 5000 /
> lottery_scan 仪器 / v15_stack 预设；回归 1015 绿）。R-v15 预注册跑次（ch1-30，
> 3 次零成本启动失败修正 + 1 次断点续跑）**收车判定：两项口径均未达成**——
> 口径乙（30 章累计）**85.31%**、口径甲（单章触 95%）未触及（最高 ch28=91.1%）；
> 稳态章 87-91.6%、成本 ¥5.36（¥8 预算内）。机制全过：corpus_rolled 30 次零违例、
> 预生成细纲 28 章、细纲现场调用归零。**两个新认知**：①卷界重冻结税被研究低估
> （ch14-15/ch26-27 四章 X 130-220k）；②仪器定案残余 X 真鲸鱼=**audit 重试循环
> 走独立单发全价重发**（ch15 三连 33 万 miss；v13 的"路由彩票"实为此机制）——
> v16 头号修复：重试改走会话轮。**结构性结论：新书 30 章内 95% 不可达**——
> 头部只能装下书写过的内容，30 章书最多 ~110k tok 头 → 稳态 ~90% 平台；
> 95% 需 60+ 章长跑（v13 路径已证）或头部外部语料源（质量风险未评估）。
> 质量门盲评因成本判据已失败、无决策相关性，未执行（需要时可补）。

> **§-2 终局裁定（2026-09-11，用户）**：「不测了，推送 GitHub」——战役在此收官。
> 最终实测序列（综合命中率 = 命中÷(命中+未命中+输出)）：**72.7%（v10）→ 74.4%
> （v11，13 章）→ 86.5%（v12，13 章）→ 90.5%（v13 部分，5 章 provisional）**。
> 全部代码（v11 四项 + v13 四项 + v14 三项，共十一次调整）以**旗标缺省关、关态请求体
> 逐字节一致**入库，回归 1012 绿；90%/95% 因测量中断**未按预注册口径达成，不发布**，
> 保持 v0.19.5。恢复路径（充值后续测）与全部证据索引见本文 §0/§-1 与看板。

> **§-1 v14 离线迭代（95% 冲刺预备，09889ea，回归 1012 绿）**：真机仍被 402 阻塞，
> 离线完成下一轮全部实现——①corpus_mode 章头瘦身（语料库已含已锁章节全文时，
> 上一章结尾→引用行，文风样本保留；`_chap_header` 接线 19 处调用点，缺省 False
> 字节不变）②`pace_boundaries.vote_replicas` 副本票持久化窗（首票回复写缓存后再
> 发副本，v12/v13 review miss 主要成分）③v14_stack 预设（outline length_budget
> 1200 字 + vote_replicas 12s）④4 件单测。**95% 数学**：X（miss+out）需 ≤ H/19；
> ch39 卷界后头 ~370k chars → H≈3M → 门槛 ≈158k；v13 部分实测 X=201k，v14 预计
> -20~30k，其余靠头部增长摊薄——**95% 的测量窗口在 ch39 之后的整卷**。
> v13 实现细节：①`_pace_seconds` 边界＞全局＞缺省（deslop_review 新边界）；
> ②audit slim prompt（corpus_head 门控，五材料→引用行）+ `_cap_flagged` 上限 8；
> ③`_ensure_outline_in_session`（pre-opening 轮）+ cost_bench `_ensure_outline` 让路；
> ④chapter_curve 按位置索引 + cost_bench 爆炸章告警（out/章 >8k）。单测 7 件
> （test_v13_output_governance.py），回归 **1008 绿**。
> 事故两条：MAX_CHAPTERS=40 夹断 --chapters 42（T3 教训重演，env 已显式设）；
> `_ensure_outline` 驱动层预生成抢占会话内路径（发车 60 秒检查逮住，杀跑补门控重发）。
> 今日真机累计支出 ≈ ¥13.6（含 v13 中断前部分），台账 338 行零污染。

---

# 以下为 v11/v12 轮正文

> 执行线：按《工作指南_v3_综合命中率90总纲》+《seed_下一轮执行代理_20260910》
> 完成 S1-S10 全部十一项中的十项（S11 发布判定见 §8）。渠道全程钉定
> ds-official-flash（deepseek-flash），全部跑次离峰窗口，真机台账 338 行零污染。

---

## 1. 实施层（S1-S5，全部旗标缺省关）

| 项 | 内容 | 落点 | 旗标 |
|---|---|---|---|
| 调整一 | 清算/追踪/去味静态指令库并入冻结头（头 +~7k tok）；清算/追踪/去味会话轮剥除同段 | `canon_audit.py`（模板转义嵌回重构+提取器+轮剥除）、`prompts/memory.py`、`prompts/writing.py`、`volume_session.py::head_rebuild_system_text`、`stages.py` 接线 | `writing.instruction_in_head` |
| 调整三 | 卷内 effort 统一（state 钉住、卷中拒改）+ 清算预扫降档 | `stages.py::_apply_volume_effort/_merge_audit_effort_low` | `writing.volume_effort`（缺省空=关）/ `writing.audit_effort_low` |
| 调整四 | 审校票紧凑 JSON（六维+verdict+confidence），max_tokens 900 由 preset 控；解析失败回退长文重投（max_tokens 1400） | `prompts/review.py`（FINAL_REVIEW_COMPACT=rubric 前缀派生）、`stages.py::parse_compact_review_json/resolve_quote_ref/_stage_param_override` | `writing.review_compact` |
| 调整五 | deslop 命中段 ≤2 处定点修复（⟦Pnn⟧ 纯文本替换对，无 JSON），膨胀 >25%/越界/空合并自动回退整章 | `stages.py::_deslop_pinned_rewrite/_findings_para_map` | `writing.deslop_pinned` |
| 调整七/动作甲 | 卷语料库冻结头：卷首已存在的全部细纲+已锁章节原文入 `追踪/冻结语料快照_卷N.md`（滞后一卷语义，卷内不增） | `volume_session.py::build_corpus_snapshot` | `writing.corpus_head` |

实现注记（与种子/指南的偏差，均有依据）：
1. **追踪协议冻结头用通用措辞版**——冻结头不能带 `{chapter_num}`（卷内逐字节稳定
   优先于逐字复用）；语义等价（章号前缀本来就由程序添加）。
2. **`volume_effort` 缺省改为空串**（种子写"缺省 high"）——缺省 high 会对未配 effort
   的连接加发 `reasoning_effort` 字段，违反"关态请求体逐字节一致"硬规则。
3. **audit 降档走 `router.stage_params` 原位合并**（客户端持有同一 dict 引用，当章
   即生效），不复制 client——copy 会让用量计数器分叉。
4. **AUDIT_PROMPT 重构为转义嵌回**：`.format(双花括号转义段)` 渲染逐字还原，关态
   请求体与改造前逐字节一致（有单测钉住）。
5. deslop 定点修复只走会话路径；单轮路径自动回退整章。

回归：**1001 passed / 1 skipped**（基线 982 + 新增 19），prefix_lint 全绿，
CI 对 2e0f376→d730452 全绿。提交：2e0f376 / 240e6cf / 17a950a / 83c1a18 /
a764955（S1-S5），d730452（S9），另 evidence 3 笔。

## 2. R-v11a 三章冒烟（¥0.415）

五项机制判据全过：头 28,983 chars（新书小底册，绝对值受书规模影响，逐字节稳定✓）、
紧凑票 9/9 解析零回退、`sent` 实证 audit effort=low 与 review max_tokens=900 落地、
pinned 去味单笔输出仅 39 tok（整章重写对照 1,629 tok）、9 调用/章结构不变、零盲区。

## 3. R-v11b 十三章 A/B（¥2.754）

**13/13 完成，零盲区零中断。逐相位对表 vs v10_long13（同渠道同流程基线）：**

| 相位 | v10 | v11 | Δ |
|---|---|---|---|
| 综合 | 72.7% | **74.4%** | +1.7pt |
| ¥/章 | 0.253 | **0.212** | -16% |
| 输出/章 | 24,310 | 19,366 | **-20%** |
| review out/章 | 2,950 | 1,267 | -57%（紧凑票超设计预期） |
| deslop out/章 | 1,428 | 436 | -69%（pinned，设计预期 -67%） |
| deslop in/章 | 23,908 | 9,612 | -60%（定点只带命中段） |
| audit hit | 53.1% | 58.8% | +5.7pt |
| audit out/章 | 3,331 | 3,656 | +10%（effort low 未缩输出，见下） |

**预注册判定：综合 ≥85% 未达（74.4%）、¥/章 ≤0.20 未达（0.212）。**
决策树走"逐相位对表定位"：四项调整**全部正贡献，无一项需要回退**。85% 门槛
本身相对指南第四部分的诚实数学（v11 理论 77.6%）定得偏高——实得 74.4% 与理论
差距主要在：新书底册小（头 21.7k tok 而非 32k）、audit 仍是 miss 鲸（41% miss）、
outline 现场生成每章 4.2k 输出不在本轮治理范围。audit effort=low 参数确认下发
（`sent` 实证）但该样本上推理输出未降（2,382→2,703）——降档对"难对账章"无效，
与 V4 报告"难题不受档位影响"一致，如实记录。

## 4. S8 盲评（子代理双评委，零 API）

**v11_long13 总分 7.27 vs anchor_baseline 7.32（Δ -0.05；门槛 ≥-0.5 ✓）；
D2 爽点 +0.02 ✓；D4 情节一致性 -0.15 ✓（预注册三门槛全过）；零引文违规。**
唯一低于锚点的维度 D3 文笔 -0.72（聚合器单维闸 ≤1.5 内，如实记录——紧凑票与
effort low 对"文笔"无伤但对"文采锐度"或有微贬，后续轮次观察）。

### ⚠️ 第一轮盲评作废与数据事故（重要教训）

首评包 20260911_0430 全部作废，三层叠加事故：
1. **anchor_baseline.chapters 归档副本文件名与内容错位**（既有数据缺陷，非本轮
   代码引入）：锚1 文件里是第2章、锚2 是第1章、锚3-锚6 轮转错位——`_chapters_of`
   按文件名定章号 → 映射整体撒谎。已按正文首行章号矫正归档（commit 9dd9aab）。
2. **子代理 Read 工具读样本文件出现内容-文件名错位**（两名评委独立复现，各自查
   出并自行矫正，但双方对错位样本的票不可信）。
3. 聚合器按磁盘验真 → 错位样本 10 维全判"引文不实"，分数被系统性压低。
处置：重造样本包 20260911_0517（19 份逐份首行验真零错位）+ 评委改用 Bash/Python
读取（绕开 Read 错位）+ 全量重评。**凡子代理读大量本地文件的场景，一律要求
Bash/Python 读取并首行自证**——已回写种子知识。

## 5. R-v12a/b 冻结语料头（¥1.299 + ¥5.541）

- R-v12a（14-16 章续 v11_long13）：头 **133,304 chars（≈100k tok）三章逐字节
  稳定**，语料库=卷一 13 章原文+全部细纲；audit hit 58.8%→80.4%。
- **R-v12b 终局（17-29 章，13/13 完成）：综合 86.5%（输入侧 89.0%）、in/章
  1.19M（语料头兑现——与指南 §5.1"上一卷全文以命中价常驻"的设计量级一致）、
  ¥/章 0.426。预注册 ≥90% 未达、≤0.15 未达。**
- 稳态单章 90-92% 综合多次实测（ch20 92.4%/¥0.251、ch23 90.2%、ch29 91.6%）——
  **90% 在单章上已复现，均值被三类爆炸拖垮**：
  1. **audit 输出爆炸**：vol-2 书累积 30+ 实体后 audit out 14.5k/章（v11 3.7k）、
     31 笔含重试/终审升级——effort low 压不住难对账章（同 §3 发现）；
  2. **outline 现场生成** 8.5k 输出/章（每章现补细纲，不在会话内、近乎全 miss）；
  3. 卷首章冷启（ch17 71.5%）与偶发爆炸章（ch26 ¥1.03）。
- 对照表：v10 72.7% → v11 74.4% → **v12 86.5%**（口径：hit/(hit+miss+out)）。

## 6. 预算与红线核对

真机支出 ≈ ¥10.01（0.415+2.754+1.299+5.541，DS 官方口径离峰），种子预算
¥10-12 内。真机台账 338 行全程未动（每次发车 60 秒双向检查）。全部跑次离峰。

## 7. 遗留与下一步（按钱排序，供下一轮）

1. **输出侧三鲸是唯一主攻**（audit 思考无上限、outline 现场生成、追踪随实体数
   增长）：audit 输出上限 4k + 早停语义复核（指南 §5.2 动作乙的既有条目）；
   outline 预生成进 prepare（离线摊派，省 8.5k out + 全额 miss/章）。
2. ch26 式爆炸章检测：逐章 audit out > 8k 自动告警/降级重试。
3. v12 语料头的**质量面**尚无独立盲评（本轮 S8 评的是 v11）；v12 章样本
   （17-29 章）已归档，补一轮双评委即可闭合。
4. chapter_curve.py 对续跑章号（--start>1）越界崩溃，小修。
5. 盲评工位纪律：样本必须 Bash/Python 读取；归档 .chapters 必须首行-文件名
   一致性校验后再入库。

## 8. 发布判定

按种子 §5.7：综合 ≥90% 方走发布节奏。实测 86.5% 未达 → **不发布 v0.20.0**，
保持 v0.19.5；本轮成果以文档+代码（旗标缺省关）入库。指南第十七部分的
"v0.19.6 综合 85% 线"若采用 v12 配置（86.5% 过 85% 线）可评估发布——留用户裁定。
