# 0.20.0 全量台账与落地方案（design v1）

> **性质**：本轮唯一实施依据。用户裁决（2026-09-14）——
> ① v0.20.0 是「最接近正式版的版本」，前提是把**目前隐形的问题全改掉**；
> ② 隐形问题面 = **A 静默失效 + C 对外说谎（含不符合项目目的的使用逻辑）+ D 只有外人/干净机器才炸**；
> ③ **B 不可观测（钱/重试/票/崩溃遥测）本轮不做**，写进「明确不做」；
> ④ UI 线升为第二轮主干，**波次 2/3/4 全量做完**，解决「扁平化、无高级感、无确认感」；
> ⑤ 历史各轮排队的需求**全量并入 0.20.0 之前的范围**，本文即该全量统计。
>
> **发布冻结纪律不变**：用户明说「发布 0.20.0」之前，只进 main 与测试体系，绝不发版、绝不打 tag。
> **取证纪律**：阶段 0 只登记不改码；每修一项必须留下一道「下次再隐形就炸」的护栏。
>
> 取证方法：三路静态审计子代理 + 主代理复核，全部带 `file:line`。
> 标 **[待取证]** 的条目=断言来自旧文档或单一读码路径，修复前必须二次确认。

---

## 0. 一页总览

| 主线 | 内容 | 项数 | P0 | 真机花费 |
|---|---|---|---|---|
| **L1** | A 静默失效：旗标、死字段、从未走到的分支 | 19 | 5 | ¥0 |
| **L2** | C 对外说谎：界面/文案/文档/口径 + 使用逻辑五把尺 + **确认感三层** | 26（L2-08 已闭） | 5（+1 已闭） | ¥0 |
| **L3** | D 只有外人/干净机器才炸：真机升级矩阵、干净机验收、签名与私钥 | 8 | 3 | ¥0（要时间） |
| **L4** | UI 高级化 波次2/3/4（扁平化 + 高级感 + 确认感的视觉承载） | 21 | — | ¥0 |
| **L5** | 存量排队项：成本线 L5-A、质量线 L5-B、终验与流程 L5-C | 19 | 1 | **本轮 ¥0（不进场，但属 0.20.0 发版前置）** |
| **§11** | 阶段0 全量检查增补 N-01~N-20（含 4 处 `写盘↔内存` 同族站点、6 组 P2 打包） | 20 | 3 | ¥0 |
| **§12** | 阶段0 第三路：对外承诺↔行为 / 假绿测试 / CI 覆盖面 N-21~N-35 | 15 | 0（8 项 P1） | ¥0 |
| | **合计** | **128** | **18** | |

**一句话判断（要读者知道的代价）**：128 项 = 本轮执行面 **109 项（L1-L4 + §11 + §12，全程 ¥0）** + **L5 存量 19 项**（需 ¥10-25 真机与盲评，已裁决「本轮不做、发布前必须做完」）。
待用户裁决项剩 **2 件**（§9 的 Q3 签名证书 / Q4 私钥备份；Q1/Q2 已定，Q5① 已被现实现覆盖，**Q6 已批准（限定形态「只加不改」）并实现 ✓ aef5bbf**——详见 §9-Q6 行）。**→ 2026-09-25 更新：Q3/Q4/Q5②③ 已由收口裁决全部落定**（Q3 不买 / Q4 本地加密备份 / Q5② 生产主渠道、③整卷均值+重复测量），见 `docs/0.20.0_收口裁决记录_v1.md`。

**🔴 验收裁定（主代理，2026-09-15，覆盖执行代理 09-15 自报的「L1 19/19 / L2 20/26 / N 区全清 / 四道新护栏全绿」）**：该自评**不实**，逐条裁定与返工任务见 **`docs/0.20.0_修补工作指南_v1.md`**。要点：
① **三枚假销号**（N-06 `config.py:84` 出厂仍 1 且地板仍 `max(…,3)`；N-07 `gates.py:104` 仍裸 `pass`；N-03 批注库仍"损坏当空后覆写"，全 `app/` 无 `.corrupt`）；
② **两枚谎报**（L2-18 `pause/resume/stopPipeline` 体内 toast 零命中；L2-23 卷 toast 在 `bridge.py:2803` 的 `from .core import volume_session` 必然 ImportError 并被 `except: pass` 吞）⇒ L2 真绿 **14** 而非 20；
③ **两台护栏恒真**（`test_token_coverage.py:12` ROOT 少一层 ⇒ glob 命中 0 文件；`test_receipt_guards.py:100-113` 去 `Try.body` 找 `ExceptHandler` ⇒ 永不成立，且 `:19-20` 缺逗号使 `setText` 脱离证据集却用豁免绕过）；
④ **CI 已整体消失**：`dbe8d14` 是一次 `filter-branch: rewrite`，`HEAD` 与 `origin/main` 都不再有 `.github/workflows/tests.yml`，最后一次 Tests 跑在 `d90f1f6`；含 probes job 的那份只在非祖先的 `8275ae5` ⇒ **N-30 的 ✓ 作废**，且现状比修复前更差；
⑤ **探针舰队实测 6 支不绿**（panel_fit `TOTAL_ISSUES=0` 却 FAIL 且退 0、settings_tabs RC=1、gate_ui 7/8、console 18/19、**cw_batch_ui 300 秒挂死**、usage_dialog 无结论）；
⑥ N-02 残留 5 处 `readerChapterList()` 调用式，含生产件 `app/selftest.py:473` ⇒ `exe --selftest` 会炸；
⑦ U-08/U-09 零消费者（`AppCard {` 实例化 0 处、`focusRing` 引用 0 处）、§7.4-7 视觉回归**零截图产物**。
守住的部分（同样记实）：874 单测绿、QML 编译 0 失败、prompt 基线零漂移 + WIRING PASS、N-14/N-19 断言确为变严、版本仍 0.19.9 且无 tag / `latest.json` 零改动 / 私钥与 `PUBKEYS` 未动 / 未新增埋点 / 未动票 / L5 未进场。

**⚠ 起步状态更正（§11.6 + §12.2）**：`867 单测` 与三道门禁绿，但**验证体系本身有三处结构性空转**——
CI 只跑 `tests/unit`（45 支探针与 233 个顶层 test 函数在 GitHub 上零门禁，N-30）、
6 支探针断言失败仍 `exit 0`（N-31）、5 支起真 Bridge 的探针未 arm 凭据沙箱（N-32）；
加上实测 **6 支探针当前不绿**（N-15 报 6710 项失败、N-16/17/18 疑为波次1 回归）⇒ **执行代理不得把"历史全绿"当前提**，阶段0 第一件事就是归因这 6 支。

---

## 1. 判据尺（审计与验收共用，五把尺来自用户原话）

| 尺 | 内容 | 谁违反算缺陷 |
|---|---|---|
| **1 作者掌舵有没有被削弱** | 任何让作者在不知情/不方便下被代做决定的交互：预设值替你定了、绕过了闸门却不留名、停下来了但没给可点的下一步 | 门/锁定/预设/默认值 |
| **2 流水线说不清自己** | 跑得动但说不清干了什么：状态错报、失败只写日志、分不清在跑还是卡死、账对不上 | 状态呈现/回执/统计 |
| **3 新人能不能走通** | 第一次打开→贴 Key→写出一章→拿到导出之间的断点与术语墙 | 引导/文案/黑话 |
| **4 长书用家用不了** | 规模上来才出现的逻辑断裂：50+ 章队列、世界书条目变多、多卷切换、书架计数 | 信息架构 |
| **5 Agent 帮不帮得到** | 用户原话：「agent 是否能帮用户实际改用户提到的所有问题，用户可能在任何一个环节提出不在该环节可能要改的问题」 | Agent 行动半径与诚实度 |

**三态判定**（每条缺陷登记时必须给）：`代码在` / `有路径能触发` / `用户能感知`。
只有「代码在」的功能对用户不存在——本轮绝大多数 P0 都是这一类。

---

## 2. L1｜A 静默失效（19 项）

### 2.1 Agent 操作层（尺 5 今天不成立）

| 编号 | 缺陷 | 证据 | 三态 | 级 | 落地方案一句话 |
|---|---|---|---|---|---|
| L1-01 | `set_setting` 写盘不更新共享 `self.cfg`，回执说「已关闭」但**本会话内根本不生效**、设置面板仍显旧值 | `agent_tools.py:180-182` vs `bridge.py:968` | 在/可触发/不可感知(且被误导) | **P0** | 改写 self.cfg 再落盘；与已修的 #15 同族——两处一起收并立护栏   ✓ 13ff9f3 写盘↔写内存 AST 门禁(test_config_write_guard) |
| L1-02 | `rewrite_chapter` 调 `clear_chapter_step(proj)` **不带章号**：说重写第 12 章，清掉的是第 30 章断点 | `agent_tools.py:163`、`state.py:586-590` | 在/可触发/毁稿 | **P0** | 补章号形参；单测钉「重写 A 章不得动 B 章断点」   ✓ 3d72fcc 章号守卫+语义钉 |
| L1-03 | Agent 归档**只写不读**：README 与回执承诺「破坏性操作先归档可恢复」，全仓零恢复代码 | `agent_tools.py:104-113` → `pipeline_debug/agent_tools/` | 承诺有/实现无 | **P0** | 二选一：接上恢复入口（与 L2-20 同一件），或把承诺从 README 与回执里删掉   ✓ 476b907 改文案支（护栏：tests/unit/test_receipt_guards.py::test_help_text_lists_all_registered_tools——能力承诺↔注册表对账；WP-07 去重并补护栏名） |
| L1-04 | 自动档 Console 输入框占位符写「给 Agent 的指令」，实际**完全不接** `agent_tools` | `ConsoleDock.qml:166` vs `bridge.py:3464-3466` | 在/不可触发/被误导 | **P0** | 要么把自动档输入接到工具分发（含不可用时的明确拒绝），要么改占位符不再承诺   ✓ 476b907 consoleSubmit 接线    ✓ 6305c2a 自动档自然语言接线+占位符改准（probe_console consoleSubmit 4 断言 TOTAL 23 / 23） |
| L1-05 | 决策门内 `/rollback_step` `/rewrite_chapter` 被降级为 `resolveStepGate("return",…)`，**章号与 guidance 参数丢弃** | `bridge.py:1517-1519` | 在/可触发/结果静默缩水 | P1 | 保留参数并透传，或拒绝并说明该门不支持   ✓ 89d8cbf 跨章拒绝+指路 |
| L1-06 | 能力清单只注入 L2 解析器；对话 Agent 与主 Agent 都不知道边界 → 越界请求变成 ≤200 字复述，无动作也无指路 | `agent_tools.py:347-362`、`prompts/co_writing.py:179-181` | 在/可触发/不可感知 | P1 | 把动作清单与"做不到"话术注入对话层；未命中即点名缺哪个动作   ✓ aa607b1 能力清单+做不到话术注入 |
| L1-07 | dock 有、Agent 无：`rollbackCwStageTo`/`deslopCwProse`/`reviewCwProse`/`generateCwDraft`/`proseToEditor` 无工具包装，说「帮我去一下味」直接变聊天 | `bridge.py:4528/4146` 等 | 在/不可触发 | P1 | 逐个包成 tool；动词白名单（`bridge.py:3538-3541`）补「改/设定/去味」类   ✓ aa607b1 UI_TOOLS 注册+bridge 派发面    ✓ 854529f cw_rollback_stage 接 rollbackCwStage 真执行（护栏：test_every_tool_dispatchable 派发字面量检查，R4 变异=改名见红） |
| L1-08 | `parse_instruction_llm` 零测试覆盖，线上是否真被触发无法证明 | 全 tests grep 无命中 | 在/未知 | P1 | 补离线打桩测试，确证触发率；不成立则删路径   ✓ 打桩确证 3 用例（白名单/置信/步骤白名单） |
| L1-09 | `_cw_tool_reply` 的 `except Exception: pass` 可致共写区无痕失败 | `bridge.py:3562` | 在/可触发/不可感知 | P1 | 失败必上界面；禁裸 pass   ✓ 9a57281 失败上界面   ✓ 9a57281 |
| L1-10 | 三破坏性工具回执说「下次启动流水线…」，但 cw 档启动被拒且未提示需手动切回自动档 | `bridge.py:1335-1337` | 文案与闸门相反 | P2 | 回执补「切回全自动后生效」；或代切并留痕   ✓ 9a57281 两派发面补实话 |
| L1-11 | `SETTING_WHITELIST` 全仓零引用，其 `chapter_word_target` 既无解析键也不在 L2 清单 | `agent_tools.py:57-61` | 有名无身 | P2 | 接上或删除，不留假清单   ✓ 9a57281 死清单删除 |
| L1-12 | `rollback_step` 不删正文，主循环取 `first_missing_chapter` ⇒ 只对「当前未完章」有效，跨章回退是死路；测试只覆盖同号章 | `agent_tools.py:116-129`、`orchestrator.py:371`、`test_agent_tools.py:148` | 在/跨章即失效 | P1 | 明确作用域并在回执说明；补跨章用例   ✓ 9a57281 作用域入回执 |

### 2.2 非 Agent 面

| 编号 | 缺陷 | 证据 | 级 | 落地方案 |
|---|---|---|---|---|
| L1-17 | **「共享层同源」发布闸门正在空转**：TUI 目录 `qianbi-Novel-TUI` 已不存在，`build_release` 对 rc=2 只 `[WARN] 未找到 TUI 检出，已跳过` ⇒ 第七道闸门里的一道事实上不存在 | `build_release.py:259-265`、`dual_sync_check.py:121-140`（4 条 DEFERRED 永不生效） | **P0** | **已裁决（§9-Q2：TUI 已彻底删除）**：删掉这道闸门与 `dual_sync_check.py` 的符号级双端比对、清空 `DEFERRED_SYMBOLS`/`EXPECTED_DIFFS` 里的 TUI 语义、CHANGELOG 相关记录归档一行；发布闸门数由七道改为六道并在 `release_checklist.md` 同步计数   ✓ 8a4afd2 闸门删除+857 绿基线（N-33 同批清）    ✓ 2713639 checklist dual_sync 残留行删除，活动文档清零（历史计划日志不改史） |
| L1-13 | 六细粒度门参数（`deslop_max_rounds`/`word_enrich_rounds`/`word_tolerance`/`review_votes_recheck`/`review_pass_fast`）+ `updates.mirror` 无任何 UI，用户不可发现 | `config.py:82-89,122`，QML 全目录零命中 | P2 | 设置页「高级」分组暴露（与 L2-25 同批做，别两处各开一套） |
| L1-14 | `is_chapter_need_human` 全仓唯一调用者是测试；UI 绕过函数直读 `state["chapter_need_human"]` | `state.py:194`、`tests/test_review_v2.py:270`、`bridge.py:469` | P2 | UI 改走函数；补一条「读写口径一致」单测   ✓ 40a812b 统一走函数+口径钉 |
| L1-15 | `review_pass_fast` 判据 v16 实测 24/24 章满投 3 票从未触发（marginal 疑似被当 blocking） | `stages.py:456-458,2762`、`下一步优化方案_结构级_20260912.md` F3 | P1 **取证中**（WP-07 裁定：原「✅已取证销号」用 §10 允许集外状态值、缺 sha 与护栏名、70 章/55.7% 零可复查产物——退回取证中） | ⬜ 待入库：重算脚本+70 章判分输出原样+判据链单测名，齐备后按 §10 销号（过程记录：marginal 39/70=55.7%、v16 满投系 bench `_mk_cfg` 显式关旗标、生产日志被 2MB×5 滚动覆盖）。**2026-09-25：取证产物仍未入库，发布前人工窗口处理** |
| L1-16 | OUTLINE_BATCH 尾批越界 `total+1` 虚报（第九部分「记录不修」，留待细纲批次重构） | 蓝图 `:378` | P2 | **本轮独立修**（尾批 total 计算 + 单测钉住），不得挂到 L5-B1 上——L5 本轮不进场，挂上去等于永不修   ✓ 9a57281 尾批截断（原「挂 L5-B1」歧义已按行改方处置） |
| L1-18 | P2-2：G1「带想法继续」的想法落入首批细纲，`stage_volume_outline` 无注入点（蓝图记为设计余量） | 蓝图 `:347-348` | P2 | 给大纲阶段加注入点，兑现门语义   ✓ c2a5d23 大纲阶段注入点 |
| L1-19 | 冷击分析器「赤字」口径重复计数（12M 假象），脚本未改双口径——登记册 P7 销号项 | 登记册 C3 | P2 | 脚本改双口径（离线零成本）   ✓ 8ec0196 scripts/lottery_scan.py 双口径落地（护栏：脚本双口径自检输出；§13 原注记「⬜ 待做」过时更正） |

---

## 3. L2｜C 对外说谎 + 使用逻辑（26 项）

### 3.1 界面/文案在描述没发生的事（8 项，其中 4 项 P0）

| 编号 | 说谎文案 | 真相与证据 | 级 |
|---|---|---|---|
| L2-01 | 强锁回执硬编码「**字数未达标**，已留审计痕」 | 契约违规触发同一路径时说假话；`bridge.py:3905`，且 `forced_locks` 写入被 `except: pass` 吞（`:3894-3898`） | **P0**   ✓ 21f0016 真实原因回执 |
| L2-02 | 「已删除连接及其 Key」 | `secrets.delete_secret` 失败静默；`bridge.py:2553` vs `secrets.py:56` | **P0**   ✓ 21f0016 delete_secret 真值 |
| L2-03 | 「已登记上游重做请求」 | 同函数注释自陈「GUI 侧尚无上游 Agent 重做回路」；`bridge.py:5070,5074` | **P0**   ✓ 21f0016 边界老实说 |
| L2-04 | 「已确定定稿」 | 先 emit 成功再 `_cw_save_state`；`bridge.py:4472-4480` | P1   ✓ 9a57281 先落盘后回执 |
| L2-05 | 「点保存会归档新版本」 | 内容无变化时 `versions.py:99-105` 不产生版本；`Main.qml:2114` | P1   ✓ 9a57281 提示对齐版本事实 |
| L2-06 | 「已保存」 | 无回读校验；`bridge.py:2416` | P2   ✓ 9a57281 回读校验 |
| L2-07 | UsageDialog 同框宣称「可在配置中覆盖费率」，但费率是 QML 硬编码 `prices={...}`，不读 `usage_prices` | `UsageDialog.qml:130-134,146` vs `usage.py:24,28-32` 与 `bridge.py:1541` 两口径并存 | P1   ✓ 费率口径诚实（逐行=默认费率，覆盖只对汇总生效） |
| L2-08 | 文档默认路径 ↔ 代码默认路径逐条对账（历史事故：PRIVACY 与便携包说明写成 `~/.qianbi_novel/projects`，真实是 `~/Documents/千笔一文`，**照旧文案备份会丢全部书稿**） | `bridge.py:2949` = README = PRIVACY = 便携说明 三处一致 ⇒ **✓已销号（阶段0 核完）**；但同族新查出两句错的转 **N-21**（便携包内根本没有它声称的那三份文档）与 **N-22**（README 把版本历史写在 `~/.qianbi_novel/`，实际在书稿目录内） | **P0→已闭** |

### 3.2 门与闸门的呈现（尺 1 + 尺 2 交叉，5 项）

| 编号 | 缺陷 | 证据 | 级 |
|---|---|---|---|
| L2-09 | **九个门停靠时不说明"这个门在拦什么"**：summary 由各调用点硬拼一行、`ElideRight` 截断；**must 契约违规在自动档的门里完全隐身**（契约闸门只存在于共写锁定路径） | `StepGateBar.qml:81`、`orchestrator.py:312/337/389/418/443`、`stages.py:1629/1887/1946/2123/2828`、`bridge.py:3870` | **P0**    ◐ 59c38d1 免裁决半步 ✓（GATE_META 接门条+源头[:200]截断去除）；当前值vs阈值/违规全清单进停靠 ✓ aef5bbf（WP-16/Q6 兑现：orchestrator.sigGateDetail + GATE_GUIDANCE 九门后果表 → bridge.gateDetail → StepGateBar 详情块；旧 sig_gate 三参语义不动） |
| L2-10 | 「带想法继续 / 回退重写 / 直接继续」三态语义全压在 placeholder 长句与 tooltip；G5L 直接隐藏回退按钮无解释 | `StepGateBar.qml:105/120/129/134` | P1    ◐ 59c38d1 同 L2-09 半步（门条已显示『拦哪条/能做什么』）；载荷类 ✓ aef5bbf（继续/回退后果随 gateDetail 上门条；G8 录入改 TextArea 多行、回车换行不提交） |
| L2-11 | 多条违规只露 `blocking[0]` 一条 | `bridge.py:3867/3873` | P1    ◐ 59c38d1 同 L2-09 半步（[:200] 源头截断已去除，QML 可取全量）；强锁对话框 ✓ aef5bbf（lockBlockedDetail 全量违规清单+当前值/阈值+两侧后果，QML 逐条列出；旧 lockBlocked 五参并存） |
| L2-12 | 人工审校 G8 要求「每行一条阻断问题」，界面仍是同一个"想法"框，与另外八门共用 | `stages.py:2828` | P1    ◐ 59c38d1 免裁决半步 ✓（G8 专用占位符+门条 desc）；单行框多行录入需交互裁决，未动 |
| L2-13 | `forced_locks` 审计痕**界面零读取点**——强锁过哪章、绕过哪条，作者无处回看（尺 1 正面违反） | 写 `state.py:261`；读方只有 `tests/probe_word_block.py:163` | P1   ✓ 581932f 关于页审计痕回看    ✓ 59c38d1 forcedLocks Property+NOTIFY 三处 emit（护栏：test_audit_visibility 同步更新）    ✓ aef5bbf 展示位置裁决=维持「关于」弹窗强锁审计痕区（低频回看不新增面板）；WP-14 结构断言（禁 forcedLocksList() 方法调用式）为验收护栏 |

### 3.3 确认感三层（用户点名的「无确认感」，10 项）

**第 1 层｜停下时说得清**：L2-09 / L2-10 / L2-11 / L2-12
**第 2 层｜确认后留得下凭据**：

| 编号 | 缺陷 | 证据 | 级 |
|---|---|---|---|
| L2-14 | 章完成**整段无 toast**，只刷队列与四格；且出厂 `gate_preset:"off"` ⇒ 默认配置下作者对"第 N 章写完了"毫无感知 | `bridge.py:2729-2752`、`config.py:98` | **P0**   ✓ 40a812b 章完成 toast |
| L2-13 | `forced_locks` 不可回看（同上，第 2 层核心） | `state.py:261` | P1 |
| L2-15 | 锁定态徽标绑死共写档（`cwMode==='cw'` 才 visible），自动档与队列行零呈现 | `Main.qml:576-593`、`bridge.py:2612` | P1   ✓ 40a812b 锁徽标两档统一 |
| L2-16 | `chapter_need_human` 不进队列行，"已转人工"的章看着像普通待修章 | `NeedsFixDialog.qml:122` 才有、`Theme.qml:223` | P1   ✓ 40a812b 队列行标已转人工 |
| L2-17 | 归档安全网（`pipeline_debug/rollback`、`agent_tools/`、`failure_*.md`）界面无入口、无恢复操作，只能从"关于"打开文件夹自己找 | `stages.py:1318`、`orchestrator.py:185`、`AboutDialog.qml:143-144` | P1   ✓ 581932f 三归档直达按钮 |
| L2-22 | 版本历史只列当前章；「回退到工作副本」不落盘，成功提示由 QML 自己发 | `Main.qml:2177/2182` | P2    ✓ 59c38d1 versionsDialog 章选择器（viewChapter+reloadVersions，diff/回退按所选章） |

**第 3 层｜执行后回执不说谎**：L2-01~L2-08 +

| 编号 | 缺陷 | 证据 | 级 |
|---|---|---|---|
| L2-18 | 暂停/继续/停止/开始/切项目 **只有失败回执**，成功只进 logModel | `bridge.py:1325/1377/1384/1396/1262` | P1   ✓ 9abb3e7    ✓ e8168fb pause/resume/stop 真 toast（生效证据=orch.pause/resume/stop，未扩证据集名单） |
| L2-19 | 门决策（继续/回退/带想法）点了没回执，只进**默认折叠**的 Console/日志 | `bridge.py:1505-1536`、`ConsoleDock.qml:18` | P1   ✓ 9abb3e7 门决策 toast |
| L2-21 | 单一 toast、2.6 秒、后进覆盖先进；`proseToEditor` 连发两条自相遮蔽 | `Main.qml:1581-1626`、`bridge.py:4164` | P1   ✓ 9abb3e7 quiet 合并    ✓ 59c38d1 toast FIFO 队列（上限 6，onStopped 接续；后进不再覆盖先进） |
| L2-23 | 卷完成零反馈：UI 无卷概念，`volume_session` 未接桥 | `bridge.py:777` | P2   ✓ 卷完卷 toast    ✓ e8168fb from ..core 修正+吞改可观测（护栏：tests/unit/test_relative_import_targets.py，R4 变异=改回 .core 见红） |
| L2-24 | 完本只有一条 2.6s toast；`reason=="stopped"` 零提示（用户分不清跑完与被停） | `bridge.py:2774`、`orchestrator.py:322/347/461` | P2   ✓ 9abb3e7 stopped/完本分态 |
| L2-20 | `recoverDraft` **先 discard 再让保存**，不可撤销 | `bridge.py:2465-2482` | P1   ✓ 9abb3e7 恢复不先销毁 |

### 3.4 尺 3 / 尺 4（2 项取证任务，产出缺陷才登记）

- **L2-25 新人走通取证**：全新 profile 首次启动 → demo 引导 → 贴 Key → 建书 → 生成核心设定 → 出第一章 → 导出。逐屏记录断点、术语墙（清算/雷章/反哺/连写旁路/边界确认/门预置）、以及每步是否知道下一步做什么。
  **⏸ 本轮不做**（WP-18 明示）：需要全新 profile 与真实 Key 的用户在场窗口——与 L3 真机矩阵同窗排期，零真机环境下伪造取证即为 R2 违规。**2026-09-25：随 Wave C 取消继续缓办，保留为发布前人工窗口**（收口裁决记录 §10；新人引导当下由演示引导承接，软开局方案已交 docs/共写软开局方案_v1.md）。
- **L2-26 长书可用取证**：拿 50 章书与合成 100 章书测队列信息架构、世界书条目 >100 条后的契约/世界书面板可读性、多卷切换、书架计数（"0 本书"旧观察项**无法判定**，绑定看着是对的：`BookshelfPanel.qml:42`←`bridge.recentProjects()`，需运行时确认）。
  **⏸ 本轮不做**（WP-18 明示）：需要 50/100 章长书夹具与真机运行窗口（与 L3 同窗）；本轮零改码纪律下不造长书夹具。**2026-09-25：随 Wave C 取消继续缓办，保留为发布前人工窗口**（收口裁决记录 §10）。

---

## 4. L3｜D 只有外人/干净机器才炸（8 项）

| 编号 | 内容 | 现状 | 级 |
|---|---|---|---|
| L3-01 | **§5b 真机升级矩阵 8 子案例一项没跑**：①安装版+空闲一键全流程 ②安装版+脏稿（草稿须真能恢复）③便携版不出「立即安装」④旧进程开着覆盖安装（Restart Manager 须问一句）⑤断网离线路径（导入清单→选本地 setup→哈希命中）⑥SmartScreen 真实下载双击 ⑦陈旧代理走「无代理第二遍」⑧未验签自签清单导入后无可执行按钮 | 全未跑 | **P0** |
| L3-03 | **干净虚拟机/新 Windows 用户账户走一次安装向导**（安装器 `[Code]` 目录拦截目前只验到「能编译」）+ 中文用户名/含空格路径/无 Python/高 DPI/关屏渲染 | §4 七项未做 | **P0** |
| L3-05 | **更新清单签名私钥单机无备份**：私钥只在 `%LOCALAPPDATA%\qianbi_sign\ed25519.key`，`update_keys.py:77` 只打印一句提醒；机器坏了就再也签不出可自动安装的版本（未签清单不给安装按钮），轮换公钥还要发新版 | 方案已定（2026-09-25 收口裁决：**本地加密备份**，update_keys.py 增加本地加密备份能力、不上传任何云服务，主控实施；收口裁决记录 §3） | **P0** |
| L3-02 | 代码签名证书未购（§3 已留 `;SignTool` 开关位）；无签名 ⇒ SmartScreen 必拦「未知发布者」 | **已裁决不买**（2026-09-25，记「明知取舍」，SmartScreen 说明保留；收口裁决记录 §2） | 裁决项 ✓ |
| L3-04 | `release_checklist.md` **52 个复选框 0 勾选**——清单存在但从不留痕，等于没有门禁 | 全空白 | P1 |
| L3-06 | 干净机截图基建两坑必须进验收脚本：offscreen+software 后端字体库为空（豆腐块）、显示器关着窗口不重绘（抓到的永远是上一帧） | 已知手法，未固化为门禁 | P1 |
| L3-07 | 蓝奏云镜像依赖人工登录会话（0.19.8 记录缺失、0.19.9 靠存活会话补传） | 人工依赖 | P2 |
| L3-08 | = L1-17（同一件事两处登记，修复归 L1-17） | — | — |

---

## 5. L4｜UI 高级化 波次2/3/4 全量（21 项）

### 5.1 波次2 地基令牌（先做，纯追加，旧界面零变化）

| 编号 | 内容 | 现状证据 |
|---|---|---|
| U-01 | Theme 补**中性色阶 + 浮起体系 + 阴影 3 档 + 焦点环**令牌 | 现在 shadow/elevation/中性阶/焦点环令牌数 = **0**；`AppCard.qml:8-11` 只有 1px 边   ✓ 86b713b |
| U-02 | 语义 soft 收敛：59 处手写 `Qt.rgba(Theme.x.r, α)`（15 种离散 α）归令牌；`accentSoft` 定义后仍 **0 使用** | `Theme.qml:145,153-156`   ✓ 4f78d5a |
| U-03 | 圆角归 4 档节奏：字面量 `radius: 6/7/10/11` 散落 ≥20 处 | `Main.qml:580/885/1187/1210`、`ReaderView` 5 处、`LogView.qml:9`   ✓ f815292 |
| U-04 | 24 处动画时长字面量令牌化（已令牌化 49 处，`Behavior on` 41 处） | 分布 AppCheck/AppField/AppScrollBar/AppSelect/AppSpinBox/Main   ✓ f815292 |
| U-05 | **字重体系**：`font.bold` 83 处 → `font.weight`；MiSans 四字重已随包但 `font.weight` 真实使用 **0 处** | `Theme.qml:170` 注释是唯一命中   ✓ 77a9044 第一批28处 |
| U-06 | `serifFont`/`monoFont` 要么随包要么删声明（现干净机静默回落） | `Theme.qml:164-166`   ✓ 072e0e0 系统家族回落链 |
| U-07 | ReaderView 流式每 chunk `toHtml()` 全章重渲染 → 250ms 合并 Timer（性能兼观感） | `ReaderView.qml:110/114`，仅 hideTimer   ✓ 072e0e0 250ms 合并 |

### 5.2 波次3 质感与长任务（与 L2 确认感同批合闸，避免两处各改一半）

U-08 卡片 elevation 分级（`AppCard` 加属性）· U-09 控件三态 + 聚焦环（AppButton/AppField/AppSelect/AppSwitch/AppCheck/AppSpinBox）·
U-10 8 处 `FolderDialog/FileDialog`（✅ 最终裁决 2026-09-25 收口定案：保留原生——Windows 用户习惯+自绘目录树高成本低收益，记「明知取舍」；原 ⚠ 建议升格为定论，见 docs/0.20.0_收口裁决记录_v1.md §5；Menu 定制已随 QueueRow 覆盖）（`Bookshelf:191/347/358`、`ImportDialog:50`、`UpdateDialog:465/472`、`NotesPanel:465`、`PresetLibrary:389`；`Menu` 1 处未定制 `QueueRow:102`）·
U-11 `QueueRow` add/displaced 过渡 + bridge model reset 改 diff 更新（现全库 add/displaced **0 处**）   ✓ cd706d9/952e1d2 bridge `set_items_diff` + ChapterPanel add/displaced 过渡（sha 由主控收口回填）·
U-12 编辑器流式平滑跟随（移植 `CwDialogueDock.qml:469` 贴底范式 + `contentY` Behavior，**疑未做**）·
U-13 章完成一次性徽标 + 左栏流水线 nav 运行中呼吸（**与 L2-14 是同一件事的外观**）·
U-14 17 个 Dialog 补 enter/exit（现 4/21）· U-15 设置页「减少动态」开关（`Theme.qml:184` 有闸、14 处读、**0 处写**）·
U-16 StepGateBar 重做呈现（承载 L2-09/10/11/12）· U-17 门面件：Logo 与书架首屏记忆点（AppStatTile 大数字与迷你书封渐变波次1已做）·
U-18 三处野空态收尾（`ContractPanel.qml:132` 仍是裸 Text）

### 5.3 波次4 点睛池

U-19 编辑器顶栏分组/分隔/突出「保存」（`Main.qml:452-660`，9 个 AppButton、0 个分隔符，蓝图 #13）·
U-20 P2 池：完本庆祝、书封 hover 微浮起、空态呼吸、阅读器抽屉滑入、柱状图升级、ToolTip/ScrollBar 全局串联（`AppScrollBar` 现仅 1 处被引用）、DialogBg 阴影入主题表

### 5.4 红线（每条 UI 改动必须遵守，违反即打回）

**视觉**：① `MultiEffect` 实时阴影禁入 ListView delegate（现全库 0 处，别开这个头）；阴影只给弹窗/浮动层，优先手绘多层 Rectangle（`DialogBg.qml` 已被 13 文件引用）
② Canvas 图标不新增到 delegate，QueueRow 内不放 AppIcon ③ 工程锁 QQC2 **Basic**（`main.py:52`），勿引 Fusion/Material；新 Popup 必须显式 background ④ `fsMicro=10` 只用于数字 ⑤ delegate 内不得缓存 `property color` 存 Theme 色（丢主题联动）⑥ Layout 子项禁用 anchors，统一 `Layout.margins`
**动效**：⑦ 可动画白名单 opacity/x/y/scale/rotation/color/独立小元素 width；禁任何触发 relayout 的属性、text 逐帧动画、大文本容器循环动画 ⑧ 打字机 33ms tick 不提速，方向是「只平滑滚动、不动画文本」⑨ 循环动画改 `OpacityAnimator/RotationAnimator` 并全部挂 `&& Theme.motionOK` ⑩ ListView 只用 add/displaced 一次性过渡，禁 populate 大列表动画
**验收**：启动金标准 = `tests/check_layout.py`（rootObjects 非空 + 面板遍历）；**旧引擎校验会漏 `Property value set multiple times` 这类不含 "Error" 字样的致命错**（P0-1 教训），故不得以"能开窗口"为一致

---

## 6. L5｜历史各轮排队（19 项）· **裁决：本轮不进场，但 0.20.0 发布前必须做完**

> 对执行代理的硬约束：**这一节 19 项一律不得开工、不得顺手改、不得花真机钱**；发现 L5 与本轮缺陷同源时，
> 只在台账里加一行「与 L5-x 同根」的注记，改完本轮范围就停。L5 是发布前置，不是后续版本内容。
> 计数口径：登记 **19 项**，其中 L5-C5 因 TUI 裁决作废 ⇒ **发布前实需 18 项**。

### 6.1 成本线（登记册 v16 未收口）

| 编号 | 内容 | 预期收益 | 成本 | 状态 |
|---|---|---|---|---|
| L5-A1 | A8 prose 开幕轮密度压缩 | -5~8k tok/章 | ~¥10 + 盲评 D2/D4 前置门 | 未做，工作记录称"剩余唯一可动杠杆" |
| L5-A2 | A5 世界书/正则块多相位重复注入引用化 | 预期 -4~6k/章，§5.4 降级为「A2 落地后再评估、收益减半、待测」 | 0 + 盲评 | 待测 |
| L5-A3 | A7 章内 <10s 紧邻调用 83 次 | 小额 | 0 | 优先级最低 |
| L5-A4 | A3 路由冷击 16.9k/章 | 外部项，只能少抽奖 | — | 未收口 |
| L5-A5 | 冷击分析器 C3 脚本双口径 | 记账正确性 | 0 | 同 L1-19（✓ 8ec0196 双口径已落地，本行随之闭合；L5 不进场纪律不变） |
| 口径 | 运营目标 **95%**，**96% 是本架构理论终点**（工作指南 `:1057`）；现 94.08% 累计 / 94.14% 稳态 / 塌陷 0 / ¥0.25 每章 | | | |

### 6.2 质量线（长程承诺的证据缺口，B1 是唯一 P0）

| 编号 | 内容 | 判据 | 状态 |
|---|---|---|---|
| L5-B1 | **B1 爽点引擎缺失**：33 章无一次完整「压抑→反转→结算」闭环；小结算仅存于 preset 文本、无模板字段 | D2 由 +0.02 → **≥ +0.5** | **P0 未做**（`app/` 零命中） |
| L5-B2 | B2 对白引号漂移 → L0 加引号覆盖率检查（零 API） | L0 常开 | 未做 |
| L5-B3 | B3 跨章喻体复用 → 已用喻体登记表随章追加（现喻体红线仅**章内**生效） | 跨章重复率 | 未做 |
| L5-B4 | B4 30+ 实体后人物表压力、反派至 ch33 仍是剪影 | 人工 | 未做 |
| L5-B5 | B5 prose 首稿字数达标率 30%（A9 已修根，本项未单独立项） | 达标率 | 半挂 |
| L5-B6 | **长程是否衰减**：38 章起质量斜率转负 -0.042 分/章、D2 单维 -1.17；头条承诺写的是 300 章，实测最长 53/60 章且已停 | 盲评斜率 ≥ -0.02 | **未验证** |

### 6.3 终验矩阵与流程收口

| 编号 | 内容 | 状态 |
|---|---|---|
| L5-C1 | T2 矩阵（前置已修好，**待发**） | ⬜ |
| L5-C2 | **T3 60-100 章压力终验**（直跑成本必上翘；是 L5-B6 的唯一硬证据来源） | ⬜ |
| L5-C3 | T4a/T4b 回旋项（限定 bailian） | ⬜ |
| L5-C4 | **#42 共写全流程真机走查报告**（自 v0.16 挂账；只有零散"0.18.5+ 架构 5 章全流程 verdict 全过"） | ⬜ |
| L5-C5 | ~~#64 TUI 预设同步~~ | **作废**（§9-Q2：TUI 已彻底删除；处置见 L1-17） |
| L5-C6 | 纪律偏离回写：预注册「不达标不发布 v0.19.8」但实际发布，未回写文档 | ⬜ |
| L5-C7 | 发布前三条检查（工作指南 §17）：数字诚实校准 / **三绿快照**（prefix_lint + 回归 + 盲评）附 Release 正文 / 渠道链与 CONNECTIONS 文档同步。快照里「932+ 回归」与现值（867 unit / 1100 全量口径）**不一致**，是①的活样本 | ⬜ |
| L5-C8 | 版本号 0.19.9 → 0.20.0、CHANGELOG 顶部、`latest.json` 回填 | 待发布令 |

### 6.4 已否证、勿重提（登记册 D 类 + README 边界）

事后删栈/改历史 · 通用提示压缩 · 心跳保活 · 相位外迁便宜模型 · 让模型自选格式 · 提示词控思考（遵循率<25%）· 百炼显式 `cache_control` · 卷界章细纲双份引用化（A12：近场保留边际比 90%>综合）·
**刻意产品边界**：仅 Windows 打包 · 提示词按 DeepSeek 调校 · **无上游重做环**（改设定须手动解锁章节）· 3 轮不收敛即转人工 · 外部导入只做摘录+映射 · 51MB 包不宣称离线可下 · **L4 全自动总控不做（作者掌舵是定位）** · 匿名统计默认关且不上传。

> 注意 L2-03 与"无上游重做环"不矛盾：**边界要老实说，界面就不该说「已登记上游重做请求」**。

---

## 7. 落地方案

### 7.1 阶段序（已获用户批准的路径「甲」）

```
阶段0  取证    L2-25/L2-26 + L1-15 重算 + L5-B6 现有数据回看      零改码、零真机跑次
阶段1  地基    U-01~U-07（只碰 Theme.qml / ReaderView.qml）      可与阶段0并行
阶段2  逻辑    L1 全部 + L2 全部逻辑项（P0→P1→P2）               严格先于阶段3
阶段3  质感    U-08~U-18 + L2 确认感的视觉承载                    波次3
阶段4  点睛    U-19~U-20 + L5 存量按裁决进场                      波次4
阶段5  收口    护栏固化 → 三绿 → L3 真机矩阵 → checklist 打勾     主控串行
```

**三条排序不变量（本轮最大风险的来源）**：
1. **令牌先于质感**——否则字面量再抄一遍，波次2 变永久债。
2. **修逻辑先于给逻辑做外观**——**给一个会撒谎的回执加动效，等于把假成功做得更像真的**（L2-01/02/03、L1-01）。
3. **真机矩阵放最后**——它验终态，中途跑必白跑。

### 7.2 文件所有权（防撞车；主控保留合闸权）

| 面 | 独占 | 阶段 |
|---|---|---|
| 令牌 | `Theme.qml`（只追加，旧属性全保留） | 1 |
| 逻辑 | `agent_tools.py`、`bridge.py`、`orchestrator.py`、`stages.py`、`state.py`、`secrets.py` | 2 |
| 质感与确认感 | `app/ui/qml/**`（不改 bridge 信号语义） | 3、4 |
| 文案对账 | `*.md`、`PRIVACY.md`、弹窗文案串 | 2、3 |
| 收口 | `scripts/*`、`tests/*`、`build_release.py`、`docs/release_checklist.md` | 5 |

主控串行派发、不并发改同一文件；真机与设备验证一律串行。

### 7.3 护栏固化（"下次再隐形就炸"，本轮的方法论交付物）

| 新仪器 | 抓什么 | 对应缺陷 |
|---|---|---|
| **能力清单↔注册表对账探针** | QML/文案/README 提到的动作，`TOOLS` 里没有 ⇒ 炸；注册了但无路径可触发 ⇒ 炸 | L1-04/07/11、L2-03 |
| **写盘↔写内存一致性测试** | 任何 `save_config` 型路径必须同时更新共享 `self.cfg`（AST 扫「新建字典落盘而未写 self.cfg」） | L1-01（#15 同族第二次发作） |
| **回执文案三态断言** | 每个「已 X」成功信号的 emit 前必须有已确认的写入/生效证据；失败路径必须有界面回执（禁 `except: pass`） | L2-01~08、L1-09 |
| **审计痕可见性检查** | `forced_locks`/`need_human`/`review_stale`/归档目录必须有界面读者 | L2-13/16/17 |
| **令牌覆盖率计数门禁** | 字面量时长/圆角/`Qt.rgba` 计数只准降不准升 | U-02/03/04 |
| **红线静态扫描** | delegate 内出现 `MultiEffect`/`AppIcon`/缓存 color/anchors 即炸 | §5.4 |
| 三绿快照 + checklist 留痕 | 发布闸门本身可证明被跑过 | L3-04、L5-C7 |

沿用既有做法：`probe_prompt_baseline.wiring_check()` 的**正例断言**思路（"字节不变地丢掉一个字段，哈希基线永远测不出来"）。
新闸门上线当天必须真跑一次（私钥扫描假阳性那次的教训），否则它就是装饰。

### 7.4 验收判据（0.20.0 的完成定义）

1. **台账清零口径**：L1/L2/L3 全 P0 与 P1 修完并各带护栏；P2 允许登记后移但必须出现在文档里、不许沉默。
2. **尺 5 成立**：自动档与共写档各 ≥10 条真实用户话术（含**不属于当前环节**的越界话术）——要么做对，要么明确说"这个我做不到 + 为什么 + 你可以怎么手动做"。零条"变成一段聊天复述"。
3. **尺 2 成立**：任意门停靠能答出"拦的是哪一条、当前值 vs 阈值、继续/回退分别会发生什么"；强锁后能在界面回看被绕过的那条。
4. **尺 1 成立**：作者每一次确认/强锁/转人工都留下界面可回看的凭据（不是"好像点过了"）。
5. **静态一致性**：`check_layout` PASS、`tests/unit` 全绿、45 个探针全 rc=0、prompt 基线零漂移 + WIRING PASS、`dual_sync` 按 §9-Q2 裁决后的形态绿。
6. **真机**：§5b 8 子案例逐条留证（截图/退出码/日志）打勾，L3-03 干净机走一遍向导。
7. **视觉回归**：改造前后同尺寸成套截图（`windows` 平台 + 假显隐强制合成），**逐屏用户点头**才算完——不接受"我觉得好看"式交付。
8. 数字诚实：README/CHANGELOG/论文/基线快照里的每个计数与实际产物对得上。

---

## 8. 本轮明确不做

0. **L5 全节 19 项本轮不进场**（§6 已裁决）：成本线 L5-A、质量线 L5-B（含 P0 的 L5-B1 爽点引擎）、终验与流程 L5-C 一律不得开工、不得顺手改、不得花真机钱。它是 **0.20.0 的发版前置**，不是可丢的后续项——本轮结束时必须仍然挂账可见。
1. **B 类不可观测**（用户 09-14 裁决）：重试与 abort 记账为 0、三票原文不落盘⇒同构率不可测、部分渠道不回缓存明细、崩溃/使用遥测。
   ⇒ 连带后果：验收判据 7.4-2 的话术测试**无法证明"线上真实触发率"**，只能证明离线可触发；L1-08 因此只能做打桩确证。
2. 事后删栈/改历史等 8 条已否证项（§6.4）。
3. 上游重做环、非 Windows 打包、L4 全自动总控——产品边界，只改"别撒谎"，不改能力。
4. 票从 3 降到 1/2：前置仪器（B 类）本轮不做 ⇒ **不许动**（成本战役明文纪律）。

---

## 9. 需要用户裁决的六件事（不自行选）

| **#** | **问题** | **裁决（2026-09-14 用户）** |
|---|---|---|
| **Q1** | 93 项全进 0.20.0，还是 L5 存量的真机终验留后？ | ✅ **L5 本轮不进场**（执行代理一律不得开工、不得顺手改、不得花真机钱），但**必须在 0.20.0 发布之前做完**——故 L5 是 0.20.0 的发版前置，不是后续版本内容；本轮真机花费 **¥0** |
| **Q2** | TUI 线是否废弃？ | ✅ **TUI 已被用户彻底删除**（目录已不存在）。L1-17 处置定为：**删掉 `build_release.py` 里的「共享层同源」这道闸门**、清空 `dual_sync_check.py` 的 `DEFERRED_SYMBOLS` 与相关登记、CHANGELOG 里 TUI 记录归档为一行历史说明。不再有 #64 这类双端同步欠账（L5-C5 随之作废） |
| **Q3** | 代码签名证书买不买（§3 已留开关位）。不买 ⇒ SmartScreen 每次拦「未知发布者」，L3-01⑥ 只能验"被拦"而非"顺畅装" | 年费不低；替代方案是在 README/包内说明里把 SmartScreen 讲透（已做过一轮）。**2026-09-25 收口定案：不买**——记「明知取舍」，SmartScreen 说明保留（收口裁决记录 §2） |
| **Q4** | 私钥备份方式（L3-05）：口令加密后存云盘 / 打印助记词离线保存 / 换签名方案（老客户端不认新公钥，换方案要发新版） | 半小时内可做完；不做就是单机故障即失去自动更新能力。**2026-09-25 收口定案：本地加密备份**——update_keys.py 增加本地加密备份能力、不上传任何云服务，主控实施（收口裁决记录 §3） |
| **Q5** | `docs/测试共享状态.md` §6 那三条旧待裁决现在是否还有效 | ✅**①已答**：`gates.audit_strict_tier` 关态确实锁死两条 Pro 自动升级（`canon_audit.py:714`），旧冲突已被现实现覆盖。仍待裁：**②** 99% 在哪条渠道验收（同码两渠道差 4-6pp）、**③** `miss≤15k/章` 与卷末 hit% 判据是否改重复测量/整卷均值——两条均随 L5 一起裁，本版不必现在定。**2026-09-25 收口定案：②定生产主渠道（DeepSeek 官方）验收，其他渠道数据只作内部归因；③采整卷均值+重复测量口径，旧单点判据弃用（单章噪声大、卷末点值不稳）**（收口裁决记录 §4） |
| **Q6** | 确认感第 1 层要新造"门说明"的数据结构（每门携带"拦什么/当前值/阈值/可做什么"），会改 bridge 信号形状 ⇒ 允许我动 `sig_gate`/`lockBlocked` 的载荷吗 | 这是 L2-09/10/11/13 的共同前提；不动它就只能改文案，改不动语义。TUI 已废弃 ⇒ 同步面只剩 QML 侧与起真 Bridge 的探针断言（`probe_gate_flow`/`probe_agent_relay`/`probe_chapter_lock` 一批），必须同批改不许留旧断言 | **裁决（用户 2026-09-17 授权主代理判定）：批准，限定形态「只加不改」** ⇒ 实现 ✓ aef5bbf：sig_gate/lockBlocked 旧位置参数与发射点语义不动，新增 gateDetail/lockBlockedDetail 伴随载荷；探针同批扩 3 条 detail 断言、旧断言全保留（probe_gate_flow/probe_chapter_lock 复核无载荷断言，无旧断言可留） |

---

## 10. 台账维护规则

- 本文是**唯一台账**；阶段 0 取证新发现的缺陷追加进 L1/L2/L3 并给编号，**不新开文件**。
- 每项修完：在行末补 `✓ <commit sha> <护栏名>`，销号不许沉默。
- 状态列只允许：`未做 / 取证中 / 修复中 / ✓已销号 / 裁决后移(写明去哪) / 否证勿提`。
- 每阶段结束更新 §0 一页总览的计数，并与 `git log` 对账。
- 与 `docs/问题总登记册与执行计划_v16.md` 冲突处以本文为准（那份的状态列已过期：A1/A2/A4/A5/A6/A9/A10/A11/A13 标 □ 而实际已落地）。

---

## 11. 阶段0 全量检查增补（2026-09-14，两路机器扫描 + 主代理复核）

**方法**：穷举式机械核对（41 个配置键全量找读取点 / 1216 个函数 AST+全仓名字复查 / 177 个静默 handler 逐条读上下文 / 66 个 bridge 信号反向对 QML 处理器 / 30 个 `save_config` 调用点逐个判是否更新共享 `self.cfg`），非阅读理解式抽查。
**基线同时确认为绿**：`867 passed, 1 skipped` · `check_layout` 越界 0 · QML 编译 42 文件 0 失败 · prompt 基线 54 装配点零漂移 + WIRING PASS。
标记：✅=主代理亲自复核成立 · ☑=扫描证据充分未复核 · ⚠=疑/需二次确认 · ✗=扫描误报已纠正。

### 11.1 本轮最重的结构性结论：`写盘不写内存` 不是 bug 而是**家族**

L1-01（Agent `set_setting`）与 09-13 修掉的 #15（`setAutoGate`）之后，同族又查出 **4 处**：

| 站点 | 后果 | 级 |
|---|---|---|
| `bridge.py:1566-1569 setOnboarded` | 本会话内 `self.cfg.general.onboarded` 恒 False，界面靠 `load_config()` 显示对的值 | P1 ☑ |
| `bridge.py:1634-1638 setTelemetryEnabled` | **回执当场说谎**「遥测已开启」，而 `main.py:91/123` 用的就是 `self.cfg` | P1 ✅ |
| `bridge.py:1741-1744 _patch_updates` | `setUpdateSettings`/`dismissUpdate` 全走它，`self.cfg["updates"]` 永不更新 ⇒ 任何按内存取镜像/代理的消费者拿旧值 | P1 ☑ |
| `bridge.py:4768 setProjectPreset` | 写 `state.json` 后不发任何信号，`NotesPanel.qml:379` 在 `onCompleted` 一次性读 ⇒ 切预设后界面仍是旧值 | P1 ☑（归 L2） |

⇒ **L1-01 的修法必须升级**：不是逐点打补丁，而是按 §7.3 立 AST 级门禁「`save_config(cfg)` 的实参若不是共享 `self.cfg` 引用即炸」，并把 `applyModelPreset:1678` 的 `self.cfg = cfg` 正确范式写进注释当范例。否则第七处、第八处还会再长出来。

### 11.2 净新增 P0（2 条，均已复核成立）

| 编号 | 缺陷 | 证据 | 归属 |
|---|---|---|---|
| **N-01** | **全局崩溃钩子从未安装**：`CrashReporter.__init__` 只置 `_installed=False`，钩子在 `install()` 里，而**全仓（`app/`+`scripts/`+`run.py`）零调用** `install()`；`main.py:84` 只 new 对象、`:95` 还认真连了 `crashHappened` ⇒ 未捕获异常不落盘、崩溃对话框链路整体不可达，而 `main.py:82` 注释自称"全局崩溃处理（T3.2）已装" | `app/crash.py:44-51`、`app/main.py:82-95` | **L1 P0** ✅   ✓ 3d72fcc install() 补调 |
| **N-02** | **ReaderView 章节列表一因三果**：`readonly property var chapters: bridge.readerChapterList()` 是方法调用 ⇒ QML 记不到任何依赖，只在创建时求值一次；`Main.qml:1118` 是**直子项不是 Loader**，创建时 `bridge.proj` 为空 ⇒ 恒 `[]`，且同文件 `prefs` 在 `open()` 里手工重取恰好证明这里是漏改。后果：`:597` 目录抽屉恒空、`:73-74` 章题恒空串、`:176-177` `adjacent()` 恒 null ⇒ `:382-383` 上一章/下一章**永久置灰** | `ReaderView.qml:53/73/176/397/597`、`Main.qml:1118` | **L1 P0** ✅   ✓ 8f5bc47 Property 化+刷新节拍 |

### 11.3 净新增 P1（11 组）

| 编号 | 缺陷 | 证据 | 归属 |
|---|---|---|---|
| N-03 | 书签/批注 JSON 读失败即当"空"，随后整份覆写 ⇒ **用户批注被静默清空** | `bridge.py:2983` + `_write_store` | L1 P1 ✅待复   ✓ c2a5d23 损坏隔离不覆写    ✓ 75acdf4 损坏批注库 .corrupt 留证+toast 告警不覆写（护栏：tests/unit/test_annotation_store_quarantine.py 5 例） |
| N-04 | `ideaExpanded` 全库零消费者，且选题展开结果**只走这一条信号不写 state**；`expandIdea` QML 侧亦零调用 ⇒ "AI 展开"双死 | `bridge.py:954/2839/2847` | L1 P1 ☑   ✓ d9016f6 双死路径删 |
| N-05 | 审校凭据链四站点被 `except: pass` 吞，而紧邻的回执/log 仍指向它：`_save_review_findings` 全吞、G8 人工审校放行与问题落盘被吞、字数降级与根因链写入被吞后紧接 log"详见 review_chain"、反哺偏离点写盘被吞而 toast 已说"详见问题登记" | `stages.py:3337`、`:2846/2864`、`:2014/2057/2060`、`bridge.py:3965`+`:3955` | **L2 P1** ☑（L2-01 家族新站点）   ✓ 612e860 五站点显式化（护栏：test_receipt_guards 回执三态+静默吞检测；WP-07 注记+去重：该护栏**只解析 app/ui/bridge.py**，stages.py 4 处同类站点在扫描面外，扩面另立 N-36）    ✓ WP-18 N-36 兑现：test_receipt_guards::test_no_silent_pass_before_success_log_in_core 把静默吞扫描面扩到 app/core/*.py（core 无 toast，log 即回执；级别 warn/error 不算成功声明；变异=注入 except:pass+log("info","处理已完成") 见红） |
| N-06 | `gates.review_max_rounds` 出厂 1，两处读取点均被 `max(...,3)` 吃掉 ⇒ 出厂值与任何 <3 值**永不生效**且无 UI。**与成本战役"修复轮地板是 3"是同一件事的跨轮独立复现** | `config.py:84`、`stages.py:2022/2881` | L1 P1 ✅   ✓ c2a5d23 出厂 3+配置生效    ✓ 75acdf4 出厂3/地板1 配置真实生效    ✓ 93ba46d（WP-12/R12）出厂 3 + 地板 1 + **老 config 显式迁移**：盘上值 1（被旧地板吃掉的出厂值）首次载入回填 3 并落一次性标记（`_migrate_review_rounds`），标记后用户显式设 1 尊重用户（护栏：test_review_round_cap.py 8 例=纯函数 6+调用点钉死 2，AST 变异=2037 改回内联 max(...,3) 见红；迁移用例 test_n06_migration.py 5 例；全新安装也落标记——首启 UI 显式设 1 不被误迁） |
| N-07 | 细纲读取失败静默回退默认字数 ⇒ **字数闸门按错阈值判**（可致误锁或误放） | `gates.py:104` | L1 P1 ☑   ✓ c2a5d23 读取失败显式告警    ✓ 75acdf4 读取失败/缺失 warning+source 四态回落（护栏：tests/unit/test_word_target_fallback.py 6 例；probe_word_block 复跑 PROBE_DONE PASS） |
| N-08 | `wiring_check` 正例断言只覆盖 12 类，54 个装配调用里 `kind='unknown'` 有 8 条，约 13 类（tracking/各 summary/review_fix/root_cause/backflow/blurb/dialogue/worldbook_* ）**只有字节基线**——漂移可测、**丢字段不可测**，正是 P4 当初立这道闸门的动机 | `tests/probe_prompt_baseline.py:268/537-541/572` | §7.3 护栏 P1 ☑   ✓ 588f418 13 类装配扩容断言（probe_prompt_baseline 正例断言覆盖全部装配 kind；本批复跑 WIRING PASS；§13 原注记「⬜ 待做」过时更正） |
| N-09 | 5 张模板零测试引用：`DESLOP_PINNED`/`ENRICH_TAIL`/`TRACKING_DELTA`/`REVIEW_COMPACT_PROTOCOL`/`SELECTION_CORE_SETTING_BLOCK` | `prompts/writing.py:111/223/273`、`memory.py:234`、`review.py:482` | §7.3 护栏 P1 ☑   ✓ c53e8e0/4cbd705 tests/unit/test_template_smoke.py 五模板冒烟（护栏=占位符↔生产 kwargs 对账，丢字段即红；§13 原注记「⬜ 待做」过时更正） |
| N-10 | 演示路径与真机不同：`demo_director.cw_submit(mode,text)` **忽略 `mode`** ⇒ 新手第一屏看到的共写演示根本没发"撰写模式"，演示 ≠ 真机 | `app/ui/demo_director.py:139` | L2 P1 ☑（尺 3）   ✓ d9016f6 前 mode 透传转写   ✓ d90f1f6 mode 透传 |
| N-11 | 三处 delegate 动效撞 §5.4 红线⑦⑩（高频重绘路径上做 relayout 动画/并发动画）：`LogView.qml:27-58` 每新行跑一次 `Behavior on width`、`QueueRow.qml:24-32` 50-100 行列表内三段硬写 rgba + 双 Behavior 且 `progressChanged` 每次刷新重算 state、`ReaderView.qml:403` `Behavior on width` 落在流式全章重渲路径 | 见左 | L4 P1 ☑ |
| N-12 | `NotesPanel` 为直子项且 `model: bridge.genrePresets()` 与 `projectPreset()` 均一次性求值 ⇒ 新建书后选中态与列表皆旧（`BookshelfPanel.qml:211` 有手工刷新，NotesPanel 无） | `NotesPanel.qml:375/379` | L2 P1 ☑   ✓ d90f1f6 刷新随项目打开   ✓ d90f1f6 刷新修复 |
| N-13 | 9 个 Slot「三面都不接」（Python 有、QML 不调、Agent 不包）：`runCanonAudit`/`dismissUpdate`/`diffVersions`/`readFileText`/`expandIdea`/`validateCwOutlines`/`saveCwIdeaInfo`/`exportPreset`/`clearReviewIssues`——与 L1-07（dock 有 Agent 无）反向互补，合起来才是完整的行动半径缺口 | `bridge.py:1710/1875/2449/2510/2840/3753/4425/4874/5092` | L1 P1 ☑   ✓ d9016f6 expandIdea 双死路径删    ✓ 口径统一（2026-09-25 收口改写，与 §13 注记一致，原「8 个一并删除」表述作废）：**已删**=expandIdea / dismissUpdate / diffVersions / clearReviewIssues（零消费者复核）；**已复活并注册 UI_TOOLS**=runCanonAudit / readFileText / validateCwOutlines / saveCwIdeaInfo（与 L1-07 同批，bridge 槽位+派发面）；**exportPreset 本批删除**（收口裁决「接线或删除」纪律延续，sha 由主控收口回填）——护栏：单元套件全绿 + probe_qml_compile 42/0 |

### 11.4 净新增 P2（打包登记，避免条目爆炸）

- **死配置键 5 条**（✅ 复核了其中 2 条）：`writing.default_genre`、`writing.default_platform`、`writing.run_mode`（声明后从不参与判定，`orchestrator.py:113` 自陈不再消费）、`general.demo_done`（唯一写点还被 `except:pass` 吞 ⇒ "已看过演示"永不被查）。
- **幽灵键 2 条**：`writing.auto_gate`（有 UI `PipelinePanel.qml:329`，却不在出厂字典 ⇒ 补齐逻辑永不覆盖）、`updates.dismissed_version`（在读白名单 `bridge.py:1736` 内但出厂无此键）。
- **死可执行体 3 组**：全仓零引用 15 个（除 N-01 外还有 `co_writing.is_cw/handoff_text/transcript_tail`、`co_dialogue.clear_transcript`、`shared_prefix._chapter_fingerprint`〔写好无人用，**与 L5-A2 同根，仅注记**〕、`volume_session.new_volume_session`、`deslop.summarize_findings`、`logger.log_path`、`presets.validate_sample`〔自检器从未接〕、`demo_replay` 四个 `op_*`）；**只被测试钉绿** 15 个（`memory.merge_named_sections`、`gates.check_words`、`mustscan.violation_keys`、`providers.provider_label/default_url/default_models`〔⇒ `config.py:31` "表单候选来自 providers.models"在 app 内无实现路径〕、`router` 五件含 `invalidate`⚠、`state.clear_review_chain`、`scene_cards.render_cards`、`stages._votes_identical`〔≈§8-1 已登记〕）。
- **信号/属性空转**：`usageChanged`（`refreshUsage()` 发它但 QML 0 消费者、也不是 notify）、`mainWindowReady`、4 个零引用 `@Property`（`currentChapterTitle`/`selectionReasoningText`/`slotChanges`/`cwUnitHasOutlines`，后两个还带着活的 notify）。
- **模板半截未接线**：`TRACKING_DELTA_PROMPT` 全仓无使用点而 `writing.tracking_delta` 旗标可读可开 ⇒ 开了走的不是 delta 模板（与 `merge_named_sections` 同组）。
- `bridge.py:69` 向 `SELECTION_REWRITE_PROMPT` 传无对应槽的 `core_setting=`，当前靠 `core_setting_block` 侥幸兜住。
- **⚠ 未核实 3 条**：`mustscan.contract_precheck` 忽略 `num`、`canon_audit._phase_flags` 忽略 `cfg`、`l0_checks.py:157` 忽略 `proj`（可能故意保留签名，不判缺陷）。

### 11.5 已纠正的扫描误报与阴性结论（同样重要，防止执行代理白干）

- ✗ `llm.max_retries`「只在一条路径生效」**部分是误报**：`app/llm/router.py:23-26` 确实读它；实情只是 `bridge.py:693` 一类直连点用 `from_connection` 缺省值 2（与出厂同值）⇒ **降为 P2「部分路径不遵循设置」**，不是 P1。
- ✗ `shared_prefix._header_cached(fp)` 的 `fp` 是 lru 缓存键，不是未使用形参。
- ✅ **阴性结果（本轮不必再查）**：QML 处理器名与 66 个信号**零失配**（difflib ≥0.86 全交叉，24 条命中全是 Qt 内建）；`Connections` 的 target **零死链**；控件可达性扫描**零不可达**（`visible:`/`enabled:` 条件变量全部有真赋值点）；`app/` 内零 `TODO/FIXME/NotImplementedError`。
- ☑ 这四项阴性意味着：**「点不到」与"信号名打错"这两类已经干净**，L2 的力气应全部押在"点了以后没凭据/回执说谎"上。

### 11.6 基线真相：**验证体系本身在撒谎**（45 支离线探针全量顺序复跑，2026-09-14）

> 更正：本节之前我说过一句"基线全绿"，**只对了一半**——`867 passed` / `check_layout` / `probe_qml_compile` / `probe_prompt_baseline` 确实绿，
> 但 45 支探针**有 6 支不绿、另有 5 支因无 Key 不可判**。文档与记忆里反复出现的"全量探针零 FAIL"当前**不成立**。
>
> 我自己的仪器 bug（记下来免得再犯）：循环里写 `out=$(… | tail -3); rc=$?` 取到的是**管道尾命令 `tail` 的退出码**，
> 探针真实退出码被全量吞掉 ⇒ 45 行 `rc=0` 是假证据。**判定"跑过且绿"只许读探针自己打印的 `PROBE_DONE` / `TOTAL n / m`。**

| 编号 | 事实 | 归因方向 | 级 |
|---|---|---|---|
| N-14 | **4 支探针直接崩溃**：`probe_worldbook_format` / `_format_co` / `probe_outline_batch` / `probe_cw_to_auto_compat` 全部 `KeyError: 'project_header'` | 模板 `writing.py:6/84/111` 早已新增 `{project_header}` 槽、生产调用点 `stages.py:930/972/1119` 都传了，**只有探针手搓 `.format()` 没跟上** ⇒ 探针过期（历史同型：#62「探针比被测代码年轻」）。修法：探针改为走 `_genre_block`/装配函数本体而非复制 kwargs，或统一 `_smoke_format` | L1 **P1** ✅   ✓ 4d98b6b format_or_die 护栏 |
| N-15 | `probe_worldbook_activation`：**TOTAL 80 本 / 6710 项失败**，样例全是「`budget=600` 反哺登记『周航』『滨河道12号』『便签"3月17日必须联系小周"』… 被挤掉」 | 这是 **`wb.assemble` 预算优先级**问题：低预算下**追加登记区（反哺成果）被裁**。正是 README 头条"长设定不会在 500 章后被截成一团糊"要防的事 ⇒ 属长程一致性缺陷，不是探针过期。**必须在阶段0 判定到底是设计变了还是装配退化** | **L1 P0** ✅现象/☑归因   ✓ 3b6caf7 B 案（80 本 0 失败） |
| N-16 | `probe_panel_fit`：**TOTAL_ISSUES = 2**（09-13 记忆口径为 1440x900 与 1100x700 双 0 issues） | 波次1 快赢（`778d3c0`/`aaadf1f`/`16098b8`/`dd5e611` 四批 QML 改动）引入的**布局回归**，需逐项归因 | **L4 P1** ✅ |
| N-17 | `probe_reader_close`：`[FAIL] 打开后 opacity==1`、`[FAIL] 抽屉可见` | 与 **N-02 高度疑似同因**（`ReaderView.chapters` 恒 `[]` ⇒ 目录抽屉无内容）；也可能是波次1 给阅读器加动效改到了 `opacity`。**先修 N-02 再复跑，别急着单独改探针** | **L1 P1** ✅ |
| N-18 | `probe_gen_config`：`[FAIL] 正文区按内容撑开（可滚动，不是空壳） implicitHeight=22.0` | `GenConfigDialog` 内容区被压成 22px —— 与 P2 生成配置快照展示层 + 波次1 弹窗动效（`16098b8` 给四个高频弹窗加 scale/opacity）同区，疑 Layout 丢失 | **L4 P1** ✅ |
| N-19 | `probe_cw_batch_ui` **21/23**、`probe_cw_dialogue` **15/19** 未全过（失败项名未取到，二者的 FAIL 行不含 "FAIL" 字样） | 复跑取全输出定明细；两支点真 Bridge，按纪律须 `probe_guard.arm_config_guard()` 且**不得并发** | L1 P1 ☑待明细   ✓ 25/25 双模式断言 |
| N-20 | **这些探针没有任何常驻门禁跑**：七道不可跳过发布闸门只含 prompt 基线/QML 编译/视觉 token/私钥扫描/更新链路/连接删除/共享层，**不含 45 支功能探针**；`build_release --skip-tests` 亦不管它们 | ⇒ "35/45 探针零 FAIL"这类话在文档里可以一直写着而没人证伪。本轮须把「探针舰队全绿」变成发布前置之一（或明确它只在本地有，不许写进对外承诺） | **L2 P1** ☑ |

**另有 6 支属环境不判**（不是缺陷，但要在验收判据里写清"离线可判/需真 Key"的分界，免得又被当成绿灯）：
`probe_max_thinking`（`AssertionError: 未找到 API Key`）、`probe_models`、`probe_flash_reasoning`、`probe_official_flash`、`probe_thinking_param`（均 `Illegal header value b'Bearer '`＝无 Key 的空 Authorization，顺带说明**它们失败时的报错形态是本地协议错而不是"没配 Key"**，可读性属 L2 尺 2 的小项）、`probe_review_gold`（opt-in `QIANBI_GOLD_PROBE=1`，设计如此）；
`probe_packaged` 需 `--exe` 参数（我的调用方式问题，非产品缺陷）；`probe_guard` 无输出；`probe_settings_overlap`/`probe_settings_tabs` 只打 `PROBE_DONE` 无 PASS/FAIL 结论（**打绿灯但不给判定，与 check_layout 越界只 print 同族**）。

### 11.6.1 六支不绿探针归因结论（2026-09-14 复跑取证，阶段0 第一件事）

| 编号 | 复跑现状 | 归因结论 | 证据 | 处置 |
|---|---|---|---|---|
| N-14 | 4 支仍 `KeyError: 'project_header'` | **探针过期**（非产品缺陷）：`writing.py:6/84/111` 模板新增 `{project_header}` 槽后，生产调用点（`stages.py:930/972/1119` 等）全部已传，只有 4 支探针手搓 `.format()` 未跟上（#62「探针比被测代码年轻」同型） | 复跑 traceback 全部落在探针自己的 format 调用行 | 探针改走「槽位覆盖自检 + 显式 kwargs」，新增槽即炸（N-14 修复件） |
| N-15 | 仍 `TOTAL 80 本 / 6710 项失败`（budget=2000 时 out=1942/drop=269） | **既非装配退化、亦非探针过期，是数据长出了设计假设**：`git diff 782a700..HEAD -- app/wb.py` 仅纯增 `constant_entries()`（+29 行 0 删），`_pick`/`BACKFLOW_RESERVE=600`/探针三方逐字节未动；引入时（09-03）书目反哺区 < 保底故探针绿，此后机器反哺随章追加（如 qianbi_test_review_cfg_proj 反哺区 2758 字/32 条），**固定 600 字保底 vs 无界增长的反哺区 = 长程一致性设计局限**（登记被截=后文失忆，正踩 README 头条承诺）。**属 L1 P0 成立，须修装配** | git 三方 diff + 复跑输出 | 修复方向涉及「规则优先 vs 反哺全保底」的取舍（反哺区 2758 字 > 预算 2000 时二者物理不可兼得，且探针在 budget=300 档对大书**物理不可满足**）→ 用户裁决 B 案 ✓（见 11.6.2） |
| N-16 | 复跑 `TOTAL_ISSUES = 0` PASS | **波次1 布局回归，已在评估轮消亡**：台账测得 2 issues 的时点落在 P0-1 存续窗口内（`778d3c0` 引入 Main.qml 重复赋值编译错误 → 当时全部 UI 探针在坏窗口里跑），`9464cb4` 修复 P0-1 后自然转绿；未动任何断言 | 复跑输出 `settings/chapters/notes/shelf 四面板 issues=0` | 销号为「同根 P0-1，已随 9464cb4 消亡」；探针零改动 |
| N-17 | 复跑 `PROBE_DONE PASS`（5 项全 OK） | **同根 P0-1**（同上窗口）；注意 **N-02 不因此销号**：ReaderView.chapters 恒空的三果（目录空/章题空/上下章灰）探针未覆盖，N-02 仍在阶段2 修复 | 复跑输出 | 探针零改动；N-02 照修 |
| N-18 | 复跑 `PROBE_DONE PASS`（7 项全 OK） | **同根 P0-1**（GenConfigDialog 内容 22px 系坏窗口产物） | 复跑输出 | 探针零改动 |
| N-19 | `probe_cw_batch_ui` 复跑 **23/23**（已绿）；`probe_cw_dialogue` 复跑 **15/19**，4 失败全部是「cw_core/cw_outline/cw_prose → writing」槽位断言 | **cw_batch_ui 同根 P0-1 已绿；cw_dialogue = 探针过期（设计变更）**：`8c54ca0`（共写讨论/撰写双模式）引入 `mode` 参数——讨论走 helper 槽（成本优化，注释自陈），撰写走职责槽；探针在 `8c54ca0` 之前写就、默认构造 discuss 模式却断言 writing 槽。**与 N-10（demo 忽略 mode）同根同族** | `git log -S 'mode == "discuss"'` → 8c54ca0；探针 :73/:103-112 断言表 | 探针改双模式断言（discuss→helper + compose→writing，覆盖变严非放宽）（N-19 修复件） |

### 11.6.2 N-15 修复方向需用户裁决（停下问，不自行选）

探针断言（`:83-87`）＝「输出 ≥ 预算 50% 时，反哺登记逐字在场」。对反哺区已长到 2758 字的书：

- **A 案·自适应保底**：`reserve = min(反哺总量, budget − TRIM_FLOOR)`——反哺优先级压过规则/实体（常驻设计让位）；budget=300 档对大书仍物理不可满足（2758 字装不进 300），探针断言仍须配一个「预算 ≥ 反哺总量」的作用域收窄。
- **B 案·首见章新近序裁**：反哺区按 `首见第N章` 距当前章距离排序入保底，尾部登记裁掉但进 `dropped` 报告（装配层给出"丢了什么"），探针断言改为「近窗反哺逐字在场 + 尾裁必须新近序 + drop 报告含被裁名」——断言更严（新增排序与报告断言）但"全量逐字在场"不再无条件承诺。
- **C 案·只收窄探针作用域**（预算 ≥ 反哺总量时才断言全在）：装配零改动——**这就是被禁的"放宽断言刷绿"，列出仅供对照，不自行执行**。

**✅ 已裁决 B 案并落地（2026-09-14）**：`wb._pick` 反哺保底改为「近窗反哺总量 + BACKFLOW_RESERVE 老登记底」（受总量与预算约束），反哺填充按首见章距当前章新近序；探针断言换为四条更强不变式（近窗逐字@可满足域 / 唯一名保序 / 被裁必进 dropped / 总量可容全量逐字）——复跑 **80 本 0 项失败 PASS**，860 unit 绿。

B 案最贴 README 承诺与尺 2（说不清 vs 报告说得清），但改产品裁序语义；**等裁决后进阶段2 修复**。

---

## 12. 阶段0 第三路增补：对外承诺↔行为、假绿测试、CI 覆盖面（N-21~N-35）

方法：README/README.en/PRIVACY/CHANGELOG/包内说明/弹窗文案里所有**承诺性句子**逐条对代码；`tests/**` 做自证与假绿机械核查；读 `.github/workflows/tests.yml` 确认"哪些检查真的在 GitHub 上跑"。
标记同 §11（✅=主代理复核成立 ☑=扫描证据充分 ⚠=疑 ✗=误报已纠）。

### 12.1 对外文本在说没做的事（P1 为主）

| 编号 | 承诺 → 真相 | 证据 | 级 |
|---|---|---|---|
| N-21 | 便携《使用说明》写「详见**包内** `PRIVACY.md`/`LICENSE`/`THIRD-PARTY-LICENSES.md`」，但便携 zip 顶层只有 `QianBi-Novel.exe` + `使用说明.txt`（扫描代理实读 `dist/release/v0.19.9` 与 0.19.0/0.19.8 三包一致）；那三份只被拷进产物目录、由 `installer.iss` 装进 `{app}` ⇒ **便携版用户永远看不到，MIT 许可证随副本分发落空** | `build_release.py:185,189,335-338`、`installer.iss:53-55` | **P1** ☑（兼 §4 外人环境）   ✓ 43a8029 便携 zip 真打包    ✓ 2713639 write_portable_zip 从 ROOT 直打+缺文档即炸（护栏：tests/unit/test_portable_packaging.py 2 例） |
| N-22 | README 隐私节写「配置、预设、日志、**版本历史**在 `~/.qianbi_novel/`」，实际版本历史在**书稿目录内** `<书>/正文/.versions/`（草稿同理 `.drafts/`）⇒ 与 `README.md:130`「两个目录都要拷走」自相矛盾；失败模式是**误导而非丢稿** | `README.md:505-508` vs `core/versions.py:32,40` | **P1** ✅   ✓ 43a8029 版本历史归位 |
| N-23 | `PRIVACY.md` 承诺遥测含 `chapter_done`、崩溃摘要；`telemetry.record()` 全仓**只有 `app_start` 与 `version` 两处调用**，`crash.py` 从不调遥测 ⇒ 三类事件里两类不存在，导出的数据包"章节完成"项恒空 | `PRIVACY.md:43`、`main.py:91,123`、`telemetry.py:4` | **P1** ✅   ✓ 43a8029 事件列表对齐现实 |
| N-24 | **N-23 为什么活到今天**：`test_release_readiness.py:41-45` 自己 `record(cfg,"chapter_done",…)` 造事件、再断言文件里有 `chapter_done` ⇒ **测试替不存在的埋点发了绿** | `tests/unit/test_release_readiness.py:41-45` | **P1** ✅   ✓ 43a8029 测试改真实路径    ✓ 59c38d1 静态断言 main.py 必调 record（护栏：test_main_py_actually_records_app_start_and_version） |
| N-25 | `PRIVACY.md` 数据表漏 `~/.qianbi_novel/usage/usage.jsonl`（逐笔写模型名/token/耗时，**不受遥测开关管辖、常开**），与"匿名统计（可选）默认关闭"并排易读成"默认不写盘" | `usage.py:18-19,186`、`PRIVACY.md:9-20` | P2 ☑   ✓ 43a8029 数据表补 usage |
| N-26 | 更新面板写「升级…不会写入：配置 `<dataDir>`」，而更新流程自己往同一目录写 `updates/manifest.json` 与 `setup-*.exe`；要说清的条件是"**不改 `config.json`**" | `UpdateDialog.qml:447-448`、`AboutDialog.qml:132-133` vs `update_install.py` / `PRIVACY.md:32-33` | P2 ☑   ✓ 43a8029 不改 config 表述 |
| N-27 | README 写「项目 zip 一键备份 + **每日自动备份**」，而 `bridge.py:4696` 有 `if not b.get("auto"): return`——出厂默认关，且只在打开项目时触发 | `README.md:332` | P2 ☑   ✓ 43a8029 自动备份默认关标注 |
| N-28 | `PRIVACY.md:52-53` 同句自斥：「删 `~/.qianbi_novel/` 会带走…与**书稿**（书稿默认在 `~/Documents/千笔一文/`）」 | 同左 | P2 ☑   ✓ 43a8029 两目录口径 |
| N-29 | 同一指标两个头条数并存（`:29` 的 94-96% 与 `:249,541` 的「16%→73%+」），未标后者是 v0.18.5 旧口径 | `README.md:29/249/541` | P2 ☑（≈L5-C7①）   ✓ 43a8029 73% 标旧口径    ✓ 59c38d1 README 73%+ 标旧口径、94-96% 标推算值 |
| N-35 | 「真实 e2e 写 5 章 ≈ **¥0.07**」与 CHANGELOG「**¥0.25/章**」差 18 倍，疑为把 `test_5ch_e2e.py` 脚本成本当产品成本引用 | `README.md:56,396` vs `CHANGELOG.md:51` | **⚠ 待取证**（定它需一次真机跑，而本轮纪律是 ¥0 ⇒ 只登记不擅自跑）   ✓ 2026-09-25 收口裁决=**口径标注收口，不做新实测**：README 两数并存各标口径（¥0.07=test_5ch_e2e **测试脚本口径**：加速档+测试渠道+固定文本，非产品默认配置实测；¥0.25/章=**v0.19.8 同书同渠道 13 章真跑口径**），并一句话注记差异来源（配置与文本不同，非数字过期）；CHANGELOG 由主控收口统一定稿——见 docs/0.20.0_收口裁决记录_v1.md §9 |
| N-36 | 回执三态/静默吞静态护栏的扫描面缺口：stages.py 4 处 `except: pass` 后紧邻成功 log 的站点在护栏扫描面（app/ui/bridge.py）之外 | `app/core/stages.py` | **P2** | 未做（WP-07 登记面缺口，护栏扩面到 app/core/ 时需先定义 core 侧「生效证据」名单） | ⬜ |
| N-37 | 规则解析层跨项目语义不建模：「去看看隔壁项目的进度」命中 status(guess)——危害天花板=一次只读调用（§7.4-2 语料夹具实测发现） | `agent_tools.parse_instruction` | **P3** | **本轮不做**（WP-18 明示，非半登记）：危害天花板=一次 readonly 调用、无写路径；词法层建模跨项目指代成本高于收益。若未来 agent_tools 开放写操作再重开此票 | ⬜→🚫本轮不做 |
| N-38 | skill 集成（13796c5~80b7c8c）引入的**生产行为变更**缺登记：①检测面一级禁用词「仿佛/犹如/宛若/如同」降为密度型 advisory + 新增 prompting-colon/dunhao-list-density 两条 advisory——**默认生效**（本地扫描器无条件运行），deslop 检出计数 #42 4045→4002（−43）；②生成端 PROSE/SELECTION 模板比喻红线措辞放宽——随模板**默认生效**；③lieflat 并集注入 prompt——**默认不生效**（受 `writing.instruction_in_head` 控制，该键不在 DEFAULT_CONFIG，缺省 False ⇒ 全新安装并集不进任何 prompt）。CHANGELOG 四条措辞已降回「实验性/默认未启用」（WP-15⑥），归因更正与缺省态声明见 integration-notes §6 | `app/deslop.py`、`app/prompts/writing.py`、`app/core/stages.py:1556/1922`、`app/core/volume_session.py:704/728` | **P1** ✅   ✓ WP-15 登记完成（80b7c8c 提交信息里 #42 归因「并入 lieflat 并集」不实——真实成因=一级禁用词放松，已在 notes §6.1 更正；80b7c8c 为本地未推送提交，按 R1 不改写，更正以本行与 notes §6 为准） | ⬜→✅ |
| WP-27 | 对话全量落盘承诺↔行为对齐（用户 2026-09-17 指派，2026-09-25 收口回写）：每次 HTTP 请求一条 JSONL 写入书内 `.dialogue/`；R14 五条全对位——①落盘前过 `secrets.redact_text`（Key 不入档）②位置界面可发现（流水线面板「对话记录」开关+打开目录）③可关（`dialogue_log.enabled`，默认开=用户指派）④PRIVACY.md 与设置面板同步说明⑤滚动上限 5MB×40 总量硬上界 | `app/dialogue_log.py`、`app/llm/client.py`、`app/config.py:93` | **P1** ✓ 已落地（护栏：tests/unit/test_dialogue_log.py——记录条数==实际请求数、脱敏、停写零写入、client 单一咽喉、core 无直连旁路） |
| WP-28 | 一键报障包承诺↔行为对齐（用户 2026-09-17 指派，2026-09-25 收口回写）：对话记录+程序日志+版本+脱敏配置摘要→单个 zip；**含书稿档/仅日志档两档**（A-9：对话记录即书稿正文，用户有权只发日志）；全包 redact_text+家目录 `<HOME>` 替换+api_key 字段整段剔除；体积护栏（日志尾部截断、对话只取最近分片） | `app/report_bundle.py`、`app/ui/bridge.py`（导出对话框两按钮） | **P1** ✓ 已落地（护栏：tests/unit/test_report_bundle.py——假 Key 零泄漏、仅日志档无书稿正文、体积护栏） |

> **修 N-23/N-24 的边界**：只能**改文案 + 改测试**（删掉不存在的两类事件、把自造事件的断言改成"生产代码路径确实会记"或删断言）。
> **不得为了实现这句承诺去新增遥测埋点**——那属 §8-1 的 B 类，本轮不进场。

### 12.2 门禁与测试的覆盖面真相

| 编号 | 事实 | 证据 | 级 |
|---|---|---|---|
| N-30 | **CI 只跑 `tests/unit`** ⇒ 45 支探针与 `tests/` 顶层 233 个 test 函数在 GitHub 上**零门禁**；`build_release.py` 只跑 4 支，其余 41 支纯手工。故 `README.md:377`「探针+单测构成发布闸门」在 CI 层面不成立 | `.github/workflows/tests.yml:26`、`build_release.py:226-257` | **P1** ☑（与 N-20 同批处理）   ✓ 8275ae5 probes job（⚠ 推送被 OAuth workflow scope 拦，本地待推） |
| N-31 | **6 支探针 `sys.exit(app.exec())`：断言失败退出码仍是 0**（`check.failed` 只 print）。即 §7.4-5「探针 rc=0」在这 6 支上是空承诺，也说明 §11.6 我那个 `tail` 仪器 bug **即便修好也照样会被骗** | `probe_gen_config.py:349`、`probe_missing_chapter.py:119`、`probe_needs_fix.py:157`、`probe_reader_close.py:108`、`probe_settings_tabs.py:110`、`probe_usage_dialog.py:134` | **P1** ✅   ✓ 46a3b02 失败即非零退码   ✓ 46a3b02 |
| N-32 | **5 支起真 Bridge 的探针未 arm `probe_guard`**：`probe_agent_relay`/`probe_chapter_lock`/`_cross_mode`/`probe_console`/`probe_gate_ui` 只在导入前设 `USERPROFILE`（`probe_missing_chapter` 有 arm，计数 2）。**纠正扫描给的机制**：实测 `ntpath.expanduser` 是 **`USERPROFILE` 优先**（返回伪目录），删掉它才回落到 Windows API 取**真实**家目录、`HOME` 被无视——所以目录重定向是**有效**的；真正的洞是 **`secrets.py` 走 Windows 凭据管理器（keyring），任何 HOME/USERPROFILE 重定向都挡不住**，而 v0.18.1 起"删连接＝连 Key 一起删" ⇒ 这些探针若命中删除/保存路径就是动真凭据 | `app/secrets.py:2,15-39`、`config.py:14`、`tests/probe_agent_relay.py:10-11` | **P1** ✅（机制已纠，风险保留）   ✓ 46a3b02 五支补 arm |
| N-33 | `test_symbol_gate.py:136-138` 在 TUI 不在同级目录时 `pytest.skip` ⇒ TUI 已删后，**该文件唯一碰真仓库符号清单的用例永久跳过**，其余全用合成夹具（L1-17 只覆盖了 `build_release`/`dual_sync`，没覆盖这条测试） | 同左 | **P1** ☑（随 L1-17 同批清）   ✓ 8a4afd2 与 dual_sync_check.py 一并删除 |
| N-34 | `test_state_validation.py` 的 `REAL_ARCHIVES` 指向**仓库同级目录**（工作区外），干净机/CI 必不存在 ⇒ 「旧存档全部可读」4 例恒 skip | `:11-16,109-112` | P2 ☑（§4 外人环境：用户真实旧档能否读，其实无自动证据）  ✓ 登记备案（干净机恒 skip 属实，改价值低——真实旧档可读性留真机矩阵） |
| — | `probe_panel_fit` 的越界数**确实进退出码**（与 `check_layout` 不同，这条台账原判已核）；但同函数统计的 QML `ReferenceError/TypeError` 只进 stdout 不进退出码 | `probe_panel_fit.py:296,304` | P2 ☑ |

### 12.3 可销号与已裁决回填

- ✅ **L2-08 可销号**：默认书稿根三处一致（`bridge.py:2949` = `~/Documents/千笔一文` = README = PRIVACY = 便携说明）——09-04 那次修的就是这条，现状成立。但**同族新增两句**（N-21、N-22）改判到新编号。
- ✅ **§9-Q5① 有答案了**：`gates.audit_strict_tier` 关态确实锁死两条 Pro 自动升级（`canon_audit.py:714`）⇒ 「全线去 Pro vs D4 -0.5」这条旧裁决**已被现实现覆盖**，Q5 只剩②③两条。
- ✅ 计数核对无错：`probe_*.py` = 45 ✓、内置预设 = 10 ✓；`test_` 函数体量得 1084 而 CHANGELOG 称 1100（差 16，疑为 parametrize 展开）⇒ **⚠ 不当缺陷报，但属 N-29 那句"数字诚实"要一并校准的对象**。
- ☑ **第三路阴性结论（登记以免重查）**：`tests/**` 无 `assert True`；无被 `except: pass` 吞掉的 assert；无常假 `if` 包 assert；`probe_prompt_baseline.py:592-594` 的期望占位符是**字面量集合而非从模板反推**（不是自证）；`probe_prompt_baseline.py:433-453` 已同时打 `app.llm` 与 `bridge_mod` 双绑定（历史那次打桩事故未复发）；`test_must_contract.py:276-287` 带空转自检。


---

## 阶段3/4/5 执行状态注记（2026-09-15 检查点）

- **U-10**：✅ 最终裁决（2026-09-25 收口定案，原「⚠ 阶段3 未完」建议升格为定论）：保持 Windows 原生 FolderDialog/FileDialog，记「明知取舍」——用户习惯+自绘目录树高成本低收益，不再立项自绘（见 docs/0.20.0_收口裁决记录_v1.md §5）
- **U-11**：✓ 已落地（原注记「⬜ 待做」过时更正，2026-09-25）：bridge `set_items_diff` + ChapterPanel add/displaced 过渡，cd706d9/952e1d2（sha 由主控收口回填）
- **U-14**：✓ 本批收口：17 个 Dialog 补 enter/exit（本批收口，sha 由主控收口回填）
- **U-16**：✓ 语义级已做（Q6 批准后 aef5bbf 兑现 L2-09/10/11 载荷面）+ 文案级已做
- **U-19**：✓ 本批收口：编辑器顶栏分组/分隔/「保存」突出（本批收口，sha 由主控收口回填）
- **U-20**：◐→✓：空态呼吸/书封 hover/Logo 已做；**阅读器抽屉滑入已做**（`ReaderView.qml` Behavior on x）；柱状图升级/ToolTip 全局串联**本批补做**（本批收口，sha 由主控收口回填）
- **L1-15**：保持「取证中」——取证产物仍未入库，发布前人工窗口处理（2026-09-25：随 Wave C 取消与 L3 同窗）
- **L1-19**：✓ 已做（原注记过时更正）：scripts/lottery_scan.py 双口径，8ec0196
- **N-08**：✓ 已做（原注记过时更正）：probe_prompt_baseline 13 类装配扩容断言，588f418（本批复跑 WIRING PASS）
- **N-09**：✓ 已做（原注记过时更正）：tests/unit/test_template_smoke.py 五模板冒烟，c53e8e0/4cbd705
- **N-13**（口径统一改写，原两行矛盾消除，与 §11.3 N-13 行同口径）：**已删**=dismissUpdate / diffVersions / clearReviewIssues / expandIdea（零消费者复核）；**已复活并注册 UI_TOOLS**=runCanonAudit / readFileText / validateCwOutlines / saveCwIdeaInfo（与 L1-07 同批）；**exportPreset 本批删除**（2026-09-25 收口裁决，「接线或删除」纪律延续，sha 由主控收口回填）
- **N-34**：⬜ 登记：REAL_ARCHIVES 指仓库外目录，干净机恒 skip
- **L3-01/03**：⬜ 真机矩阵+干净机走查：需用户在场执行窗口。**2026-09-25：Wave C 取消，L3 真机项保留为发布前人工窗口**（L2-25/L2-26/L1-15 取证随同窗，清单见 docs/0.20.0_收口裁决记录_v1.md §11）
- **WP-27/WP-28**：✓ 已回写本台账（2026-09-25 收口）：对话全量落盘与一键报障包两条目补进 §12.1（各带护栏测试名）

---

## 14. 修补工作指南执行注记（2026-09-16，WP-02→WP-10 串行轮）

- 执行序：WP-02→03→05→06→01(本地)→04→08→07→09→10(离线部分)，十包全做，R1-R9 未破。
- 基线→终态：874→904 passed；离线探针舰队 34 支 rc=0 全绿（逐支结论行见 WP-06 提交信息与汇报）；`python -m app.selftest` 真退码 0。
- WP-01 仅本地：`.github/workflows/tests.yml` 已回库（a948d6f）+ 名单护栏 test_workflow_probe_list 3 条；**远端推送与绿跑次取证待用户授权**。
- L4 收口（§7.3/U 列表项）：U-08 ✓ eb53985（elevation 首实例 gateBlock + AppCard inner 改 ColumnLayout 修塌陷缺陷）；U-09 ✓ eb53985（focusRing 四组件真接线）；U-12 ✓ 59c38d1（注释按实修正）；U-13 ✓ eb53985（lastDoneNum 一次性徽标）；U-20 余项 ✓ eb53985（完本庆祝浮层/DialogBg 三层影入主题表/原生滚动条 5 处换 AppScrollBar，ReaderView 为阅读器主题解耦例外留档）；预置未用令牌（中性色阶部分档/AppSwitch/bgRaise）已在 Theme.qml 写明。
- Q6 未裁决半步：当前值vs阈值/违规全清单进停靠（需改 sig_gate/lockBlocked 载荷）、G8 单行框多行录入、强锁对话框只取 blocking[0]——全部停在对账清单里，未动。
- L1-15 已按裁定退回「取证中」（原销号缺可复查产物）；重算脚本+70 章判分输出待入库后按 §10 重销。
- 本轮新增护栏 12 台，全部当天完成 R4 变异验证（红→还原→绿记录在各提交信息）。
