# 0.20.0 修补工作指南 v2（追加任务书 WP-11~WP-20）

> **读者**：执行代理。**前置**：v1 的硬纪律 **R1-R9 全部继续有效**，本文件只追加与更新。
> **来源**：主代理 2026-09-15 复跑与三方复核。你 v1 那一轮**做对的**：`926 passed`、上一轮 6 支不绿探针全部返绿（panel_fit RENDER_FAILS=0 / gate_ui 8/8 / console 23/23 / cw_batch_ui 23/23 不再挂死 / settings_tabs / usage_dialog）、`--selftest errors:[]`、令牌基线一字未抬（`rgba` 5→3 是真把 `AppCard` 迁到 `Theme.shadowSoft1/2`）、豁免从 3 收到 1 且主动撤了 `copyText`、删了死键 `openProjDebugDir`、恢复 CI 后停在推送前。**这几条按 v1 §13 的格式重新贴一次证据即可，不必返工。**
> **范围更新（用户 2026-09-15 明确）**：`lieflat-less-ai-tone` 供应商化那条工作流**是用户亲自指派的**，不算越界；但它的**验证面有实质缺陷**，见 WP-15。

## 追加纪律

- **R10 · 成功必须退 0**：任何打印 `PROBE_DONE PASS` / `LAYOUT_CHECK_DONE` 的脚本，进程退出码必须是 0。判绿口径统一为"**退码 + 自报结论行一致**"，二者不一致即视为缺陷（本仓已有两支这样，见 WP-11）。
- **R11 · 禁止自我加锁的口**：以下三种形态一律算"没有护栏"——① golden 缺失时**自动写入当前产物**再 `pytest.skip`；② 期望清单**手写在测试里**而不是从代码派生；③ `assert X and True` / 条件式断言（上游不满足就整条形同不存在）。已有实例，见 WP-15。
- **R12 · 改变老用户既有行为必须显式迁移 + 留痕**：出厂默认值改了不算迁移；`load_config()` 只补缺失键（`app/config.py:329-340`），盘上已有的值会原样活着。凡"新默认 ≠ 老行为"，必须写迁移函数、加用例、在台账留一行。

---

## WP-11 · 成功却退非零（P0，最先做）

实测：`timeout 200 python tests/check_layout.py` 打印 `边界越界项: 0` + `LAYOUT_CHECK_DONE`，**退出码 = 127**；`tests/probe_gen_config.py` 同样 `PROBE_DONE PASS` 而 RC=127（两支文件里都 grep 不到显式 `sys.exit`）。

做：查明退出码来源（疑 Qt 事件循环返回值透传，或某处 `exit` 码取自子进程），在**结论为 PASS 时显式 `sys.exit(0)`、FAIL 时非零**；然后：
1. 顺序复跑全部 `tests/probe_*.py` + `check_layout.py`，贴出**每一支**的 `RC=` 与自报结论行（需真 Key 的 5 支单列"环境不判"）；
2. 加一条锁：`tests/unit/test_probe_exit_contract.py` —— 静态要求每支 `probe_*.py` 末尾存在显式退出码约定（或统一走一个 `finish()` helper），并把"PASS 却非零"当违规；
3. 变异验证：把某支的 PASS 分支退码改成 1，看这条锁红不红。

**完成定义**：21 支复跑表 + 新锁的变异记录。

---

## WP-12 · N-06 不许静默改变你机器上的行为（P0）

事实：`config.py:84` 出厂已改 3、`stages.py:418` 地板已改 1，代码本身对；但**老用户盘上早就写着 `review_max_rounds: 1`**，`load_config()` 只补齐缺失键 ⇒ 升级后自动修复环上限**从 3 静默掉到 1**。"默认行为不变"只对全新安装成立。这条同时动质量与成本账（成本战役把"修复轮地板 3"登记为 P0 后成本回升的三源头之一），**不许以"让配置真实生效"的名义顺手发生**。

做（**取"保持升级前行为"这一支**；用户若想借机降到 1，需他明令）：
1. 写显式迁移：升级时若盘上 `review_max_rounds == 1`（即被旧地板吃掉的出厂值），改写为 `3` 并留一行迁移日志；
2. 用例：模拟"老 config（值为 1）经 load 后上限 == 3"；再加"用户显式设 1 时上限 == 1"（区分"从未设过"与"主动设小"做不到就用迁移时点解决，别留模糊）；
3. 把该值的作用与代价写进设置面板 tooltip 或文档一句话（它管的是自动修复轮上限）；
4. 台账 N-06 行重写注记：从"配置真实生效"改为"出厂 3 + 地板 1 + 老 config 显式迁移（sha）"。

**连带（同一处必做）**：`tests/unit/test_review_round_cap.py` 六例**只测纯函数，没一条钉调用点**——把 `stages.py:2037/2896` 改回 `max(...,3)`，六例全绿。补一条 AST/行为断言：`stages.py` 里两处修复轮上限必须来自 `_review_round_cap`。变异验证：改掉一处调用点 → 必须红。

---

## WP-13 · 回执门禁的级别盲区（P0）

事实：`test_receipt_guards.py:155/207` 只匹配 `toast.emit("ok", …)`，而你为 L2-18 补的回执是 `self.toast.emit("info", "已继续写作")`（`bridge.py:1395-1397`）——**新回执从头到尾不在门禁射程内**。以后谁想让说谎回执过关，把级别从 `ok` 挪到 `info` 就行。

做：① 门禁扩到"任何带成功语义（`已…`/`完成`/`成功`）的 toast，不论级别，必须有生效证据"，级别只影响颜色不影响检核；② 为 pause/resume/stop 三条补**真会红**的断言（现在它们的存在只靠人眼看）；③ 证据集里的万能名（`save`/`write`/`record`/`append`/`update`/`start`/`run`/`set_items`/`refreshQueue`）逐个判定：纯读或纯刷新的剔除，留下的在名单旁注明"为什么算生效"；④ `except: pass` 那条现在只认"handler 体恰好一条 `Pass`"——扩成"handler 体不含任何上界面/落日志动作"即违规，并用两行 `pass`+`log()` 的样例做变异验证。

---

## WP-14 · 给四台门禁补牙（P1）

逐台给出"我验过它抓不住的情形"，每台都要做到**该情形变红**并贴变异记录：

| 门禁 | 现在抓不住 | 要做 |
|---|---|---|
| `test_relative_import_targets.py` | 只验**包/模块**存在；`from ..core import volume_sessionXX` 不红（`len(parts)==1` 时压根不查 names）。另：`try:` 内的条件导入会**误报红**、`import *` 被跳过=漏判 | 校验被导入符号是否存在于目标模块顶层；条件导入白名单化而非误报；补两条变异（改符号名→红；改包名→红） |
| `test_audit_visibility.py` | 全是**源码子串匹配**：把 `AboutDialog` 改回 `text: { bridge.forcedLocksList() }`（永不重算，正是 L2-13 原病），`"bridge.forcedLocks"` 子串还在 ⇒ 绿；`"note" in ChapterPanel.qml` 近乎恒真；「结论已过期」只验 bridge 侧字符串、**不要求 QML 消费者** | 改成结构断言：审计痕/徽标的**读者必须是绑定到 notify 信号或 Property 的表达式**（禁方法调用式），并逐条要求 QML 侧真实存在消费者 |
| `test_workflow_probe_list.py` | 只验"名单里的名字都存在" + `≥25` 兜底：磁盘 46 支、CI 只列 34 支，**再悄悄摘 9 支也绿**；YAML 折行时 `split("run_probe_fleet.py",1)[-1]` 只取首行 ⇒ 静默缩面 | 双向核对（离线可判集合 ⊆ 名单，名单 ⊆ 磁盘），用 YAML 解析而非字符串切，并显式列出"环境不判"白名单；变异：从名单删一支 → 红 |
| `test_qml_red_lines.py` | ⑤ 只匹 `anchors.x:`，分组式 `anchors: { centerIn: parent }` 漏判；`raw.split("//")[0]` 会把含 `https://` 的整行砍掉；不剥字符串字面量（串里的 `{`/`}` 会错配块栈）；①③④ 只在 delegate 域生效，`Component` 外的 `MultiEffect` 全放行 | 用块/属性解析而非注释裁剪与子串；剥字符串字面量；把"阴影/Canvas 不得进高频重绘路径"的判定扩到 Popup/列表容器 |

---

## WP-15 · skill 线的验证面返工（P1；工作本身用户已授权）

事实（我抽验过 ①②，其余来自复核）：
1. **R11 违规**：`tests/unit/test_skill_rules.py:148` 与 `:181` 都是"golden 不存在 → 写入当前产物 → `pytest.skip`"。删掉快照，它就永远把当下输出当正确答案。
2. **无牙的锁**：`test_deslop_rule_family_snapshot:155-180` 的 28 条规则族是**测试里手写的期望清单**（只有 `CLICHE_PATTERNS==8`、`scan_rule_ids` 真从代码派生）⇒ 删掉整条规则仍全绿。
3. **放松检测没有召回测试**：`app/deslop.py:202-210`（simile-marker-density 整块）**删掉后相关测试全绿**；`test_skill_dualmode_regression.py:93` 还有 `assert skill_rules and True` 这种空断言，`:96-97` 的"成本锁"是条件式（docstring 说"锁死 lean 增量上限"，实际不存在）。
4. **语料门禁 CI 不可判**：`test_skill_advisory_rules.py:33-45` 扫 `tests/corpus_pd/`，该目录被 `.gitignore:19` 忽略 ⇒ 干净机/CI 只能红在"资产缺失"；且语料是**繁体**原文而新规则词表是简体，误报分母不可复核；顿号阈值 2/千字 对人类实测 1.14 只剩 1.75× 余量。
5. **基线归因不实**：`80b7c8c` 的 7 点漂移里 6 点逐字对得上，唯 `deslop #42 4045→4002（−43）`被写成"并入 lieflat 并集"——并集有 8108 字，方向都反了；真实成因是**一级禁用词放松**。另外 `13796c5`/`a01d308` 自称"prompt 基线零漂移"，而漂移在 `0f12215` 已发生 ⇒ R2 类不实自评。
6. **对外措辞超前**：`CHANGELOG.md` 的 `## Unreleased（0.20.0-dev）` 把"未真机验证（自陈环境不判）"的行为写成已交付。
7. **缺省其实不生效**：并集只在 `instruction_in_head` 为真时才进 prompt（`stages.py:1922-1923`、`volume_session.py:704/728`），而该旗标**不在 `DEFAULT_CONFIG`** ⇒ 全新安装下 Phase 1 并集从未进过任何 prompt。

做：① 焊死 auto-skip（golden 缺失即 `fail` 并提示"跑 `--bless` 生成后人工 diff"，且 `--bless` 不得被测试自动调用）；② 规则族快照改**从代码派生**（读 `deslop` 注册表），删规则必须红；③ 为放松后的检测补**召回夹具**：给定含这些模式的样张，必须报出对应 findings；对 `deslop.py:202-210` 做删块变异验证并贴结果；④ 语料门禁改成 CI 可判（把用于判据的最小片段固化为入库夹具，别依赖被 gitignore 的整库；简繁口径统一），或明确降级为"本地可选"并让 CI 不因缺资产而红；⑤ 把 `#42` 的归因重写为"一级禁用词放松的连带后果"，并把这条生产行为变更单独登记进台账（新增 `N-38` 或 L6 区，附影响面与是否默认生效）；⑥ CHANGELOG 四条措辞降回"实验性/默认未启用"；⑦ `instruction_in_head` 的缺省态要在台账里写清（它决定这条工作到底上没上线），不许再靠"不在 DEFAULT_CONFIG"隐式表达。

---

## WP-16 · Q6 批准（限定形态）——把 L2 语义级做完

**裁决（用户授权主代理判定）**：**允许**动 `sig_gate` / `lockBlocked` 的载荷，但必须"**只加不改**"：新增一个结构化 `gateDetail`（字段：`gate_key`、`拦的是哪一条`、`当前值`、`阈值`、`违规全清单`、`继续的后果`、`回退的后果`）随信号一并发出，**旧的位置参数与既有发射点语义保持不动**；QML 与所有起真 Bridge 的探针断言**同批改、不留旧断言**（v1 §L1-17 同型教训）。

做：① 九门停靠时界面能答出"拦哪条 / 当前值 vs 阈值 / 继续与回退分别发生什么"（`orchestrator.py:136-137` 的 `summary[:200]` 源头截断先去掉）；② 强锁对话框列**全部**违规（现在 `bridge.py:4006/4012` 只取 `blocking[0]`），且回执点名被绕过的是哪条闸门；③ G8 人工审校要能录多条阻断问题（`StepGateBar.qml:107` 的单行框 + `:121` 回车即过门，物理上录不进去 → 改 multiline，回车不提交）；④ `L2-13` 的 `forcedLocksList()` 改带 notify 的 Property 或 Loader 重建（现在挂在「关于」里且永不重算），并决定展示位置后告诉用户；⑤ 用 WP-14 补好牙的 `test_audit_visibility.py` 来验收 ④。

---

## WP-17 · 未跟踪却被引用的文档（P1）

`docs/lieflat-less-ai-tone-integration-plan.md`（147 行）未跟踪，但**已提交**的 `app/config.py:91` 注释与 `docs/lieflat-less-ai-tone-integration-notes.md:3` 都在引用它 ⇒ 干净 clone 指向不存在的文件。做：`git add` 入库（或改掉引用）；并跑一次"引用完整性"检查：全仓 md/注释里引用的 `docs/*.md` 与脚本路径必须存在（可做成一条轻量单测）。

---

## WP-18 · 台账余项收口（P1-P2）

- **L1-15**：用了 §10 允许集外的状态值、缺 sha 与护栏名、"三书 70 章 / marginal 55.7%"**零可复查产物** → 把取证脚本与输出入库再按规范销号，否则退回「取证中」。
- **N-05**：回执三态门禁只解析 `app/ui/bridge.py`，`stages.py` 四处站点在扫描面外（你登记为 N-36，合规）→ 本轮扩到 `app/core/`，或把 N-36 做完。
- **N-13**：`runCanonAudit`/`readFileText`/`validateCwOutlines`/`saveCwIdeaInfo`/`exportPreset` 四条缓办 → 逐条判"接线 / 删除"，不许留在"三面都不接"状态。
- **L2-21**：`Main.qml:1663-1667` 仍是单条 toast + 2600ms 后进覆盖先进（与 U-13 章完成回执交叉，连发会吞回执）→ 排队或分级。
- **L2-22**：版本历史只列当前章（`Main.qml:2088`）。
- **N-24 补完**：加一条静态断言——`main.py` 必须存在 `record(cfg,"app_start"…)` 与 `record(cfg,"version"…)`（现在只证明"手工调了会写盘"）。
- **N-29 / §7.4-8**：`README.md:465` 的「73%+」仍未标旧口径；`CHANGELOG` 1084 与快照"932+ 回归"等对外计数全部重校一遍。
- **L1-03**：台账注记重复两遍且无护栏名，按 §10 重写。
- **L2-25 / L2-26**（新人走通、长书可用两项取证）与 **N-37**（越界语料实测发现的 readonly 天花板）：给出产物或明确写"本轮不做"，不许停在半登记状态。

---

## WP-19 · 需要用户在场的两件事（先交计划，别自己跑）

1. **L3-01 §5b 真机升级矩阵 8 例** + **L3-03 干净机/新用户账户走一次安装向导**：`docs/release_checklist.md` 现在 58 框 0 勾（**没有凭空打勾，这点好，继续保持**）。要求先交执行计划（每例怎么验、留什么证：截图/退出码/日志路径），经用户同意后排期，**逐例执行逐例勾，绝不批量打勾**。
2. **§7.4-7 视觉回归**：`tests/shot_ui.py` 已换 `windows` 平台（确认是否同时做了"显隐一次强制合成"，否则显示器关着会抓到上一帧）→ 产出**成套 before/after**交用户逐屏点头。

---

## WP-20 · CI 推送停在授权前（不变）

`.github/workflows/tests.yml` 已重新入库（`git ls-files` 可见），但 **`origin/main` 仍没有它** ⇒ 远端 CI 目前只剩 pages 工作流。R1 不变：**推送需用户明确授权**，他会在本轮指令里给。推完必须贴：`gh api repos/GLFzr/QianBi-Novel/actions/runs` 里该提交的 `Tests` 跑次与 conclusion 原始片段。

---

## 执行顺序

```
WP-11 → WP-12 → WP-13 → WP-14 → WP-15 → WP-16（Q6 已批）→ WP-17 → WP-18 → WP-20
                                                        WP-19 单独排期（用户在场的窗口）
```
理由：退码口径（WP-11）不修，接 CI 就是把红灯写进门禁；WP-12/13/14 都在改护栏与行为，必须早于 WP-16 的语义级扩展，否则又是"给会撒谎的回执加外观"。

## 汇报格式（沿用 v1 §13，追加两条）

1-5 同 v1（命令与输出原样、sha+护栏行、变异验证记录、**阴性结论也要登记**、停下问人的条目）；
6. **每条门禁必须附"我怎么把它弄红的"那一句实际报错文本**，不接受"能抓 XX"的口头描述；
7. 涉及老用户既有行为变更的（WP-12 这类），单列一节「**行为差异表**」：升级前 vs 升级后，用户可感知到什么。
