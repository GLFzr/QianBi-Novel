# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

> 版本号唯一来源为 `app/__init__.py` 的 `__version__` 常量，本文件与 README、git tag 均以其为准。

## [0.18.4] - 2026-09-05（公测版 · 缓存命中架构 · 记忆层 · 相位预算）

> 公测版说明：本版起软件处于**公测阶段**。「关于」对话框可一键导出匿名数据包
> （token 用量/缓存命中率/各步骤耗时/章节清算计数——**不含书稿正文、提示词、API Key**），
> 导出后发到公测群即可。我们用大家的数据来定位成本与质量问题、驱动迭代。
> 本版同时包含写作质量（能力轮 8 轮迭代）与成本（体验轮缓存架构）两大方向。

### 新增

- **稳定前缀架构**（缓存命中主力）：十张大模板（草稿/扩写/去味/压缩/终审/追踪/双摘要/
  设定清算/共写对话与总结）统一前置项目级逐字节稳定头部（题材/核心设定/金手指约束+红线+
  授权清单/must 全文/世界书常驻条目/写作纪律）；**章级共享段**（本章细纲+角色状态+时间线+
  伏笔+近章摘要）同章所有调用逐字节一致。实测：同章复用 96-100%，跨章综合命中率
  16% → **57.8%**（同章改稿场景 100%）；机械相位（追踪/摘要/去味/扩写）关思考后墙钟从
  200-600 秒降到秒级。
- **成本可观测**：usage 记录 DeepSeek 命中/未命中缓存 token 与 phase；新增
  `tests/report_cache.py` 按天/相位报命中率与估算费用（<80% 告警）。
- **公测数据包**：「关于」对话框一键导出匿名数据（不含书稿/提示词/Key）。
- **能力轮记忆层**（写作质量"账房"）：全书连续性台账（人物/物件/制度/时间四表）、
  拍点级对表 beat_check、设定清算定级纪律（引文核验+数值对账+授权豁免纠偏）、
  日历偏差提案（正文历法 vs 案发日历对表，只提案不改写）。
- **原作世界书导入（同人档）**：新建项目可导入酒馆世界书 JSON（自动转条目）或纯文本
  （存档待拆解）；配套拆解工具 `--theme 民生与市场` 专题层 + 分块并发 + 覆盖度报告。
- 共写对话「讨论/撰写」双模式、「生成草案」「提取正文到编辑器」按钮、比对失败
  重试/跳过双出口、审校票并发、连写模式、章内断点续跑、模型策略一键切换
  （详见 0.18.3 与本版合并记录）。

### 修复

- **Key「不见了」的自愈**：连接行丢 key_ref 而凭据管理器同 id Key 还在时自动领养接回；
  keyring 不可用时退役清理整轮跳过（防误删带 Key 的行）。
- 预设相位白名单补四相位与 thinking/reasoning_effort 参数（此前相位表静默失效）；
  内置机械相位表与 genre 显式配置的合并改为键级 setdefault（显式键优先）。
- 设定清算硬伤定级须核验引文真实存在（不得拼接引文）；数值对账规则入清算。

### 变更

- 开局自动检查更新默认「开」（0.18 起）；本版新增公测数据包导出（默认关，导出由用户
  主动触发）。
- 草稿动态尾瘦身（近章摘要 n=3→2、角色状态 3000→1500、时间线 1500→800）——
  是命中率架构的一部分，实测无质量回退。

## [0.18.3] - 2026-09-05（共写双模式 · 断点续跑 · 官方全家桶）

### 新增

- **共写对话「讨论 / 撰写」双模式**（方案 A，作者点名的体验核心）：每个共写 Agent
  两套配置——讨论模式（默认）只做三件事：≤3 句复述确认作者意图、列 ≤2 个待拍板分歧点、
  说清在等什么输入，全文 ≤200 字（短输入 ≤12 字时 ≤80 字），禁止展开成稿；撰写模式或
  「生成草案」按钮才产出结构化草案。讨论走辅助槽（快），机器侧还有超长截断保险丝。
  「发两三个字收七百字」从交互、提示词、保险丝三层根治。
- **章内步骤级断点续跑**（方案 H）：草稿完成即写盘（`正文/.drafts/`），扫描/去味/审校/
  定稿各步持久化到 `pipeline_state.chapter_step`，已投审校票逐票落盘。**停在任何位置
  （含审校第 2 票进行中），重启都从断点继续——草稿保留、已投票数保留、只补投差额**，
  不再「停一次全章白写」。
- **模型策略一键切换**（方案 B）：设置→系统 新增「模型策略」——官方全家桶（三槽全走
  DeepSeek 官方 V4 Flash，实测审校 5-8 分钟 → 约 1 分钟）/ 严格审校（审校槽升官方 Pro，
  flash 做严格判定会字段级退化已实证）。官方连接行自动确保存在，Key 复用同账号行。
- **设定清算**（方案 D1）：每章定稿后自动对账正文 vs 世界观底册（三分类：底册有/
  合理补白/越界自创），报告落 `追踪/设定清算_第NNN.json`；越界与可收编自创当场点名，
  防止「执事堂」式自创制度被后续章节当正典固化。
- **世界观对账按钮**（方案 F2）：设定清算扩展到全书逐章（后台执行），验证①的脚本转正。
- **连写模式**（方案 F3）：流水线面板新增开关——开启后全部决策门自动放行，批量跑批
  不再需要人守门点「继续」。
- 拆解工具专题层与并发（`decompose_worldbook.py`）：`--theme 民生与市场` 专题补
  平民生计/物价货币/市场管理/行会佣金层（验证①定位的拆解结构性空洞），分块 3 路并发
  （25 分钟 → 约 8 分钟），跑完出覆盖度报告（缺失类别红榜提示）。
- **原作世界书导入（同人档）**：新建项目对话框新增「原作世界书（可选）」——写同人时把
  原作世界观带进项目。两种材料两条路：SillyTavern 世界书/角色卡 v2 JSON 转成
  「世界书.md」条目（蓝灯 `constant`→常驻、`key`→关键词触发词、`disable` 跳过、
  内容剥标题/引用符以守 wb.py 条目块契约），立即可被装配层识别；纯文本/Markdown 原样
  存进 `设定/原作世界书.md`，**不进写作 prompt**——拆解成世界书条目与正则是用户自己的活，
  这正是同人流的用意。已有产物一律不覆盖（世界书.md 已有内容时只报不写）。
  编码 utf-8(-sig)/GB18030 自动兜底。`project.import_worldbook()` 独立成函数，
  `bridge.newProject` 加可选 `worldbookFile` 参数（老七参调用不变）。

### 变更

- **审校投票两阶段并发**（方案 E1）：前两票并发下发（早停判据保留），不一致再并发补
  尾票——审校墙钟从 3 票串行 17 分钟降到 ≈ 最慢一票。
- **同人禁则前置注入**（方案 D3）：存在拆解底册的项目，共写 core/outline 参考块与
  自动档设定/大纲 prompt 注入 must 禁则摘要（≤800 字）——前期阶段不再"盲写"，每单元
  要求安排原作专名出场（治"同人标识浓度不足"）。
- 共写正文落稿契约（方案 C1）：prose 草案强制 `<<<正文>>>…<<<正文完>>>` 包裹，
  「提取正文到编辑器」按钮剥掉开场白与元话语后落稿——正文文件不再混入对话杂质。
- 共写定稿锁定链路加固（方案 C2）：衔接比对失败不再静默卡死——点亮「重试比对 /
  跳过比对并锁定（留痕 skipped）」双出口。
- 审校退化检测（方案 F1）：合规判定 JSON 的 why 字段重复率 >50% 判定模板退化，
  自动升 Pro 重试一次。
- 设置页「模型策略」与流水线「连写模式」均持久化到 config，随项目/全局生效。

### 修复

- **Key「不见了」的自愈**：连接行丢了 `key_ref` 指针（退役换血/迁移弄丢）而凭据管理器里
  同 id 的 Key 还在时，`hydrate` 现在会把它领养回来并接回指针——每个 id 每进程只探一次，
  不为热路径买单。本机真实案例：退役换血换丢了 `bl-qwen-max` 的指针，
  117 字符的 Key 在凭据管理器里没人认领。
- **退役清理在 keyring 不可用时整轮跳过**：`get_secret` 把「读不到」和「真没有」都报成
  空串，读不到凭据的环境（keyring 缺失/降级）里删行等于可能连 Key 一起扔。
  宁可这轮少删一张卡，下次在能读到凭据的环境里再清。
- 新增探针 `probe_wb_import.py`（13 项）：JSON 转条目过 `wb.parse`、纯文本不进世界书、
  不导入老路径零变化、坏路径项目照建。

## [0.18.2] - 2026-09-04（一键更新看得见 · 预设只预置提供方）

> 动机：0.18.1 把更新链路建完了，但真机一跑就露出一类新问题——**功能存在，用户看不见**。
> 一键更新按钮被 `canInstall` 门控整个藏掉，于是「已是最新」的面板里看不出这个应用会一键更新，
> 源码/便携态更是连按钮存在过的痕迹都没有；预设卡片又带着模型名，下半截还被裁掉。
> 这一版全部围绕「看得见」：能用的亮出来，不能用的说明为什么，没有的告诉用户什么时候会有。

### 修复

- **更新面板不再「缺少一键更新」**：按钮从「不可用就藏掉」改成「不可用就禁用并写明原因」——
  有新版且清单验签通过、但跑的是便携版/源码态时，按钮显示为
  「一键更新（仅安装版可用）」禁用态，`whyNotInstall` 那行说明照旧在下方。
  「已是最新 / 检查失败」两种状态下，按钮位置改放一句说明：
  「发布新版后，『一键更新』按钮会出现在这里——下载、校验、退出并拉起安装器一次完成」。
  唯一继续不出现的是**清单未过验签**的场景：那不是能力不够，是不被信任（0.18.1 的安全规则原样保留）。
- **连接预设卡片不再被裁掉一半**：卡片容器高 60，装不下「卡片 56 + ListView 上下 margins 各 20」，
  第二行（模型名）恰好整行被剪掉，看起来像「卡片有一半被遮住」。容器加高到 76，卡片完整显示。
- **探针同步钉住新行为**：更新面板探针新增三条断言——dev 态按钮禁用但可见、
  已是最新态出现入口提示且不给任何安装按钮、失败态同样提示一键更新的出现条件。

### 变更

- **出厂连接预设只预置提供方，不再预置模型**（12 家全部）：名称去掉模型后缀
  （「阿里云百炼 · Qwen3.8 Max」→「阿里云百炼」），`model` 置空。模型名是各家变化最快的参数，
  预置进去的症状是过期即误导；候选改由表单在选服务商时给下拉（`providers.py` 的 models），
  「拉取」到的实时列表优先。连接 id 保持不变（凭据存储与槽位绑定都按 id 认）。
- **选服务商即给候选**：设置表单里切换服务商时，模型下拉立刻填入该家的建议模型列表；
  已手填的模型名不被打断。列表只是建议，不替用户选。
- **v0.18.1 旧行自动换血**：带模型名的出厂行纳入退役清理——没改过、没存 Key、没槽位引用的
  换成新的「仅提供方」行；被槽位指着的、存过 Key 的、改过身份字段的照旧保留（护栏原样）。
  为此修正了 `load_config` 的顺序：补全新内置连接模板必须在退役之后算 id，
  否则换血腾出的 id 会被当成「已存在」而漏补。
- 空模型在本层就喊出来：预设不带模型后，`chat`/`chat_stream` 对空模型直接报
  「这条连接还没选模型」，不再变成一串英文 API 400。
- 首启向导的连接配置指引同步改为「选服务商 → 粘 Key → 选模型」三步。

## [0.18.1] - 2026-09-04（更新链路成型 · 验签才准执行 · 12 家连接预设）

> 动机：把「自动检测 → 有新版出现图标 → 点一下就能更新」这条链真的跑通，并且让
> **没有代理、连不上 GitHub 的用户也还有出路**。设计上的核心判断是：**可达性不是难点，信任才是**——
> 一旦允许镜像、自定义源、离线导入一份 1KB 清单，「能给你推新版本」的人就等于「能在你机器上跑
> 任意 exe」的人，而这个应用持有你凭据管理器里的 Key、能写你的书稿目录。所以「多通道」和
> 「签名清单」必须同一轮落地，不能先开洞再补。这与 README 头条那句「会拒绝的流水线」是同一件事。
>
> 注：0.18.0 未单独发布，更新链路与本轮的连接预设改造合并为 0.18.1 出包。

### 新增

- **结构化检查结果 `CheckResult`**（`app/update_check.py` 重写）：状态四态
  `new / latest / failed / skipped`，每条通道带自己的错误原因。原先那个把 404、DNS 污染、
  TLS 失败、验签不过统统吞成 `None` 的函数，症状是「检查失败」被界面说成「已是最新」——假绿。
- **四条清单通道依次回退**：自定义镜像 → GitHub raw → GitHub Pages → jsDelivr，第一条成功即停，
  并记住上次成功的通道（`updates.last_channel`）下次优先试，省掉一整轮必然失败的连接。
  整链全局 deadline 25s、单请求 6s，跑在后台线程里，全挂也只是等满 25 秒，不碰界面。
- **代理解析**：`system` 用 `winreg` 读 `HKCU\...\Internet Settings` 的
  `ProxyEnable/ProxyServer`；`env` / `none` / `custom` 三种模式在面板里可切。
  PAC 自动脚本（`AutoConfigURL`）**刻意不解析**，直接告诉你识别不了、请手填地址。
  另外补一条**陈旧代理兜路**：按解析出的代理跑完整链仍全失败时，**再用无代理跑一遍**——
  一台配错了代理的机器不该因此永远更新不了。
- **更新缓存** `~/.qianbi_novel/updates/manifest.json`：存**原始已签名载荷**而不是解析结果，
  每次读都重新验签；重启后图标立刻亮起来不用等网络。
- **`QIANBI_OFFLINE=1` 杀开关**：单测、打包态自检与所有探针的零网络纪律由它兜底
  （默认开启自动检查之后，这条不再是洁癖而是必需——否则闸门会真发 HTTP）。
- **Ed25519 清单验签**：公钥以列表 pin 在 `app/update_check.py` 的 `PUBKEYS`（带 `kid`，为轮换留位）；
  `scripts/update_keys.py` 生成/查看密钥，**路径落在仓库内则拒绝写入并报错**（防止一次
  `git add -A` 把私钥变成公开）；`scripts/sign_manifest.py` 按 canonical payload
  （去掉 `sig` 后的 JSON，`sort_keys=True`、`separators=(",",":")`、UTF-8）签名并回写。
- **常驻更新图标**：左栏底部新增 `update` glyph（下箭头 + 落定横线），带 accent 角标点。
  **有新版与检查失败两种状态都画边框**（accent / warn 区分），让「查不动」看得见而不是静默。
- **`UpdateDialog.qml` 更新面板**：未检查 / 检查中 / 可更新 / 已最新 / 不可达五态，
  不可达时**逐条列出每条通道的死法**（raw: 连接被重置 / Pages: 404 / 镜像: 超时）——
  这是「没有代理怎么办」的 UI 答案：你据此判断该配代理还是走离线。四条出路同屏：
  ① 在线一键更新；② 复制直链 / 打开发布页 / 显示 SHA-256；③ **导入离线清单**（朋友或手机拷来的
  1KB 文件）；④ **选择已下载的安装包** → 本机算哈希 → 与验签清单比对 → 命中才出现「立即安装」。
- **一键更新**（`app/update_install.py` 新建）：流式下载 + 边下边算 SHA-256 + 进度 + 断点续传
  （服务端不理会 `Range` 返回 200 时从头来），完成后 `QProcess.startDetached()` 拉起
  `setup.exe`、flush 日志、`os._exit(0)`。只对**安装版**开放（靠 `{app}\unins*.dat` 判定），
  便携版与源码态不会去覆盖正在运行的自己。
- **12 家连接预设**替换原三家：国内 DeepSeek / 阿里云百炼 / 智谱 / Kimi / 火山方舟 / 腾讯混元 /
  MiniMax / 硅基流动，国外 OpenRouter / Google Gemini / xAI Grok / Groq。每条 `base_url`
  都从本机实打过一次 `<base>/chat/completions`，各家 hint 里写的就是实测结论
  （401=路径对只差鉴权、403=CDN 挡裸探测、本机直连超时）。**刻意没预置** Azure OpenAI
  （要 `deployment_id`）、Cloudflare Workers AI（URL 带 `{account_id}`）、百炼新版业务空间域名
  （URL 带 `{WorkspaceId}`）——它们没法用「地址+Key+模型」表达，塞进去只会给用户一堆调不通的连接。
  三个槽位出厂都指向 `deepseek` 的 `ds-v4-pro`：内置提示词按 V4 系调校，且填一把 Key 就能跑通全流程。
- **退役出厂行自动清理**：换成 12 家后，`ds-v4-flash` 与 `ocgo-flash` 不在出厂表里了，
  但配置合并是**按键补齐、从不删**，于是老用户设置里留着两张点不动的僵尸卡——
  它们的 `provider` 已经从 `PROVIDERS` 里移除，界面上连「属于哪家」都显示不出来。
  新增 `_retire_builtin_connections`，三条护栏缺一不删：
  ① 身份字段（名字 / provider / 地址 / 模型名）仍与当年出厂那行**逐字相等**——
  改过的已经是他的连接了（`max_tokens`/`temperature`/`timeout` 不参与比对，
  因为参数升级会正当地上抬它们，比对就会永远删不掉）；
  ② 没有任何槽位指着它——删掉正被引用的连接，等于悄悄换掉「用哪个模型写我的书」；
  ③ 凭据管理器里没有以它 id 存着的 Key——连接删了 Key 就变孤儿，比留一张卡更糟。
  外加一个进程内的记忆集合：`load_config` 几乎每个界面动作都要跑一次，
  不能每次都回凭据管理器读一遍。**硬规矩：这段迁移只读凭据，绝不写也绝不删。**
- **代码签名接口**：`scripts/build_release.py` 检测到 `QIANBI_SIGN_PFX` / `QIANBI_SIGN_SHA1`
  且找得到 `signtool.exe` 就自动签主程序与安装包（SHA-256 摘要 + RFC3161 时间戳，
  无戳签名会随证书到期一起失效）；没配证书照旧产出未签名包并打一行 `[SKIP]` 说明原因。

### 变更

- **开机自动检查更新默认改为「开」**（推翻 0.16/0.17 写进 PRIVACY 的默认关）：默认关的话，
  「自动检测 + 有新版出现图标」这条链就名不副实。边界同步改口——只有一个下载请求、
  24h 限流、不带书名/配置/Key/设备标识，关掉后启动阶段零请求。
- **`updates.auto_check_chosen` 标记**：只有用户在界面上真的拨过开关才会写它。版本迁移只认这个
  标记，所以「抄来的出厂 false」会被翻成新的默认 true，而**显式关过的人不会被翻回来**；
  面板上那条「这一版起默认是开的」的告知读的也是它，用户一旦亲自表过态就自然消失。
- 死键 `updates.check_on_start`（0.15 起从没被任何调用点读到）直接丢弃。
- 更新状态从四个字符串的 `updateFound(str,str,str,str)` 信号换成一份 `QVariantMap` 属性
  `bridge.updateState`：旧信号加一个字段就得同步改 QML 处理函数，漏改的症状是
  「新版本静默显示不出来」。
- 更新 UI 从「关于」对话框迁到独立面板；「关于」只留一个「更新…」入口。
- `requirements.txt` 补 `cryptography`（原先只因孤儿安装存在）并加进打包自检的导入目标——
  PyInstaller 漏收 `_rust.pyd` 时闸门直接红，而不是静默失去更新能力。

### 修复

- **载荷地址其实允许了明文**：批准的计划要求「清单里所有 URL 强制 https」，代码放行了 `http`。
  现在两条通道分开定规矩——**清单那一侧允许 http**（它带签名，走明文最坏是晚一点拿到或拿不到），
  **51MB 载荷与面板上「打开发布页 / 复制直链」拿到的地址只认 https**（明文链路上谁都能试着换包，
  哈希会拦住但代价是一次「更新失败」，还能看清谁在何时拉了哪个版本）。
  用户自己填的镜像前缀刻意豁免：局域网 `http://192.168.1.20/` 是明确意图，且照样过 sha256。
- **删连接不删 Key，一次点击就删**：为了清掉三张残留的预设卡才撞出来——
  `secrets.delete_secret` 定义了却**零调用**，所以每删一张卡，那把 Key 就永久留在
  Windows 凭据管理器里没人认领（只进不出）。现在 `deleteConnection` 在 `save_config`
  **成功之后**才删凭据（写盘失败不该连用户的 Key 一起毁），提示也写明「已删除连接与其 Key」。
  同时这个动作改成两步确认：第一次点击按钮换成「确认删除（含 Key）」，第二次才真删；
  **换选另一条连接时确认自动解除**，免得留着一次武装状态误删下一条。
- **探针以前在动真凭据**：`dehydrate()` 每次 `save_config` 都会把明文 Key 写进凭据管理器，
  而真 Bridge 探针照样保存配置——以前只是覆盖成相同的值所以看不出来，
  加上「删连接」这条路径之后，探针就真的能删掉用户的 Key。
  `probe_guard` 因此加了第三道沙箱：把 `secrets` 的四个函数换成进程内字典，
  探针读写自己的 vault，进程退出即蒸发。
- **`ProxyServer` 只写 `http=…` 的机器等于没配代理**：解析只认 `https=` 那一行，局域网代理
  （WinINET 常见写法）被整个丢掉；`ProxyEnable=1` 而 `ProxyServer` 为空时还会断言「只给了 PAC 脚本」。
  现在优先 `https=`、退回 `http=`、再退回裸 `host:port`，报告只说自己真看到的东西（带上 PAC 地址）。
  另补 6 项注册表解析单测，**假 `winreg` 而不是读本机设置**——否则换台机器测的就不是同一段逻辑。
- **「检查间隔」是个没接线的旋钮**：`interval_hours` 在白名单里、限流函数也读它，界面却没有任何控件，
  和 v0.15 那个从没被读过的 `check_on_start` 是同一类缺陷（只是方向相反：一个读了没人改，
  一个能改没人读）。面板现在给 6/12/24/72 小时四档，边界由 Python 侧兜：
  非法值退回 24、下限 0.5 小时、上限 720 小时——0 等于绕开限流，不能让它在任何人手上成立。
- **私钥泄漏扫描命中了自己**：`scripts/update_keys.py` 的源码里写着它要找的那串 PEM 字面量，
  于是这个新闸门第一次真跑就报「被跟踪的文件里出现了私钥材料」并拦死发版。假阳性的害处不是
  挡路，是让人去关掉闸门。改成运行时拼出的 `BEGIN … PRIVATE KEY` 形状正则（覆盖裸形式与
  `X9.42 EC` 这类带点的多词标签），本文件不再自命中，另补三条单测同时钉住真阳性与假阳性。
- **死代理被说成 DNS 污染**：把自动检查真机对着 GitHub 跑了一遍就撞出来了——配一个死掉的
  本地代理，httpx 抛 `ConnectError`，面板于是报「连接失败（DNS 被污染或被重置）」。
  逐条列出每条通道怎么死，全部意义是让用户判断该修代理还是走离线；诊断指错方向等于把这条
  设计反过来用。现在只要这一遍是走代理发的，连接类失败就点名是哪个代理连不上，
  并提示可以选「不使用代理」；**连接超时**算代理的账，**读取超时**不算（代理显然通了）。
  真机复跑：第一遍四条通道逐条写明「代理 http://127.0.0.1:7997 连不上」，
  第二遍无代理照常取到并验签通过。
- **脏稿否决会卡死一键更新**：`Main.qml` 在 `editorDirty` 时 `close.accepted = false`，
  会让 Inno 的关闭请求卡住、安装器报「需要重启计算机」。新增 `bridge.quittingForUpdate` 显式旁路，
  确认步同时写明「未保存草稿会保留，下次启动可恢复」。
- **安装版升级时程序还开着**：`tools/installer.iss` 加 `CloseApplications=yes` +
  `RestartApplications=no`，手动双击升级时至少问一句，而不是静默排到下次重启。
  **刻意没加 `AppMutex`**：Inno 对无前缀名按 `Global\` 找，而 per-user 非管理员会话没有
  `SeCreateGlobalPrivilege`，互斥量会静默落在 `Local\` 永远匹配不上——纯死重量。
- GitHub Pages 通道的主机名与路径大小写：Pages 必须小写、jsDelivr 的 `gh` 路径区分大小写，
  出厂默认仓库名 `QianBi-Novel` 与用户磁盘上可能的小写 `qianbi-novel` 都归一到正确形态。
- `cached_result` 遇到损坏缓存改为返回 `None`：原先会返回一个 FAILED 结果，
  症状是界面把「本地文件读坏了」说成「更新源不可达」。
- `AppField` 补 `editingFinished` 转发信号（回车或失焦即提交），代理/镜像地址不必再手动点别处。

### 安全

- **规则表（本轮核心）**：验签通过 + SHA-256 命中 + 安装版 → 才允许下载与执行；
  验签失败或清单没有 `sig` → 只显示版本号/说明/直链/哈希，**「立即安装」按钮根本不出现**；
  本机文件哈希 ≠ 清单 → 只报「不匹配：清单 X / 实际 Y」。应用永不替用户执行未经签名清单背书的字节。
- **载荷地址只认 `https:`**（安装包地址与面板替你打开的链接），`file://`（本地 PE）与网络共享路径
  （NTLM 泄漏）一律拒绝；清单那一侧允许 http，因为兜底的是签名而不是传输层，
  用户自填的局域网镜像同理，但照样过 sha256。`version` 字段清洗（`../../` 塞不进下载文件名），
  所有落盘名过 `basename()`；`notes` 强制 `Text.PlainText`，否则清单里的 HTML 能伪造对话框。
- 删连接会连带删掉它在凭据管理器里的 Key——一张没人认领的卡不该留着能用的秘密。
- `setUpdateSettings` 的补丁键走白名单（这个口子不做过滤就能从 QML 写 `connections`/`slots`）。
- 发布闸门 += 私钥泄漏扫描与清单验签检查。

### 测试与闸门

- 新增 `tests/unit/test_update_channels.py`（42 项）、`tests/unit/test_providers.py`（23 项）、
  `tests/probe_update_ui.py`（46 项，零真网络，假 httpx transport 跑完整条通道与代理回退）。
- 新增 `tests/probe_conn_delete.py`（20 项）：真 Bridge + 真 QML 驱动两步删除，钉住
  「Key 跟着连接一起消失」「槽位不指向已删连接」「只剩一条不许删」，
  以及退役出厂行的三条护栏**走真 save/load 各验一遍**（填过 Key / 被槽位用着 / 改过模型名都不许删）。
- **探针从此碰不到真凭据**：`probe_guard` 第三道沙箱把 `secrets` 换成进程内字典；
  以前 `save_config` 就会把明文 Key 写进凭据管理器（值相同所以看不出来），
  加了删连接这条路径之后，探针真的能删掉用户的 Key。
- `probe_panel_fit.py` 现在把设置页切进「确认删除」态再量溢出——那是那一行最宽的文案；
  打包态自检 += `deleteConnButton` 对象名（包里少了这个按钮就红，而不是等用户升级那天发现）。
- 不可跳过的发布闸门 += `probe_update_ui.py` 与私钥泄漏扫描。
- 本机已验证：单测 494 项、`probe_qml_compile` 41/41、`probe_prompt_baseline` 零漂移
  （更新功能不碰 LLM prompt）、`probe_update_ui` 46/46、`probe_conn_delete` 20/20、
  `probe_about_ui` 9/9、`probe_panel_fit` 零溢出、
  `dual_sync_check` 无意外漂移（`app/llm/providers.py` 登记为 GUI 先行的受控差异——
  真同步要连带 TUI 自己的连接预设与槽位默认值一起改，只拷这张表会留悬空 id）、
  `run.py --selftest` 通过。
- **对真网络跑过（不是假 transport）**：GitHub Pages 已开启并实测能吐清单——raw 近乎即时、
  Pages 一两分钟重建完、jsDelivr 仍缓存着 0.17.0（约 12h 边缘 TTL）。拿 0.16.0 当本地版本
  实跑一遍真实路径：`state=new`、**源码里 pin 的公钥 b75dcbf8 真的验过了 GitHub 上那份清单**、
  `can_install` 由验签放行而界面仍被 `install_mode=dev` 挡住。配一个死代理实跑回退：
  第一遍四条通道逐条报代理连不上，第二遍无代理取到并验签通过。
- 单测覆盖的反向用例：篡改签名/字段即验签失败、未 pinned 密钥签的清单被拒、未验签清单不给安装按钮、
  `file:` 与 UNC 地址被拒、`version: "../../x"` 被清洗、全链失败不等于「已最新」、
  显式 `auto_check:false` 不被默认值覆盖、缺 `cryptography` 时降级为「无法验证」而非崩溃。
- 清单反向验证已实跑：手改 `version` 一个字节 → `--verify` 拒签；原清单 → VERIFY OK。

### 已知不足

- **安装包无代码签名证书**：SmartScreen「Windows 已保护你的电脑」必然出现，需点
  「更多信息 → 仍要运行」。这不是 bug，是缺证书；对话框、README、Release 说明都主动写明。
- **51MB 载荷无法凭空翻墙**：客户端能做的只有把检测降到 1KB 多通道、把离线路径做成一等公民、
  把完整性钉死。不宣称「连不上也能下包」。
- 三条 GitHub 通道本机实测全通（raw / Pages / jsDelivr，2026-09-04），但**「GitHub 被完全
  挡死」那台机器上到底走到哪一步，仍然没有证据**——离线路径已实现、离线单测覆盖，
  可它在国内真实网络下好不好用，需要一台真在那种环境里的机器实跑一次。
- **真机矩阵仍欠跑**：安装版空闲/脏稿下一键升级、便携版只出链接、v0.17→v0.18.1 覆盖安装且旧进程
  仍开着、断网下离线导入清单 + 本地包校验安装。这些离线证明不了。
- TUI 侧仍是旧三家连接预设，`providers.py` 已登记为受控差异（GUI 先行）；真同步要在 TUI 同一个 commit 里改这张表 + 它自己的连接预设与槽位默认值。
- OpenRouter 的建议模型名里 `x-ai/grok-4` 在它公开的 `/models`（427 条）中并不存在，已删；
  它是**唯一能免 Key 核验模型名的平台**，所以别把这份确信推广到别家——其余模型名要 Key 才核得了。

## [0.17.0] - 2026-09-04（UI 成熟化 · 面板呼吸感 · 阅读排版升级）

> 动机：功能已经齐了，这一版解决「看起来不像成品」——
> 以 ZCode 桌面端与 Apple 呈现方式为设计基准，做了一轮全量 UI 深度优化：
> 高级化（明度阶梯/发丝线/克制的强调色）、风格化（统一控件系统）、成熟化（三轮截图验收零硬伤）。

### 新增

- **设计系统 2.0**：明度阶梯重校准（夜间主题 bgPanel<bgPage<bgCard 相邻层差 ≥8，区块不再糊成一片）；
  字号 6 档（补 fsMicro）、圆角 4 档、间距 6 档全部 token 化；语义色降饱和保色相，只用于小面积
  （数值/细条/徽章），禁止大色块；新增 `warn` 语义色、`overlay` 弹窗遮罩、accent 三态衍生色
- **六个新控件**：AppSelect（自绘下拉，替换全部原生 ComboBox 的系统箭头/白底弹层）、
  AppSwitch（布尔偏好开关）、AppScrollBar（统一滚动条）、AppEmptyState（空态三件套：
  图标+标题+弱说明）、AppStatTile（指标瓦片，语义色只落在细条与数值上）、AppDialogTitle
- **模态遮罩**：全部弹窗统一获得全屏变暗遮罩（Apple modal 行为），不再裸浮在页面上；
  ApplicationWindow 设全局 palette，原生控件缝隙不再漏白
- **阅读器排版**：Markdown 标题（`# 第X章 …`）渲染为真标题层级（更大字号加粗、节间距），
  不再与正文同字号；直引号按配对自动转中文引号「""」；阅读列宽约束与行距校准
- **UI 图库探针** `probe_ui_gallery.py`：逐面板 + 逐 Dialog + 三主题自动截图 34 张，
  配合三轮视觉验收流程（文字重叠=0、糊边=0、风格一致性）
- **视觉 token 审计** `tools/audit_tokens.py` 并编入发布流水线第四道不可跳过闸门：
  拦裸字号 10、锚点 footer 反模式、业务代码裸 hex 色、无描边卡片回潮

### 修复

- **面板内容零边距贴边**（长期未解的「中列遮挡」真凶）：SettingsPanel/PipelinePanel/NotesPanel
  的 ScrollView 内容用了 `Layout.margins`，但 ScrollView 的 contentItem 不是 Layout，
  **该属性被 Qt 静默忽略**——表单、卡片、按钮左右顶死面板边缘，控件贴着边框像被遮挡；
  Console 展开把面板推向编辑器后视觉更甚。改为真实 x/y 偏移 + 收窄宽度，全部面板两侧各留 12px
- **弹窗字体重叠五连**：终稿锁定徽章宽度为 0 文字溢出压工具栏（Main.qml）；NeedsFix/ReviewIssue
  双标题（QQC2 隐式 header + 自绘标题条叠加）；Toast 固定 36px 长文换行溢出；forceLock/stats
  弹窗 header 无 wrap 长标题画出边缘；版本行/队列行/文件列表多处无 elide 水平溢出
- **禁用主按钮退化成裸文字**：`!enabled` 时主按钮只剩文字、层级反被描边次按钮倒挂；
  现禁用态保留外形（主=灰底、次=描边）
- **行首标点悬挂**：全部 CJK 说明文字 wrapMode 从 `WrapAnywhere`（允许标点起行）改为 `Wrap`
  （遵循 Unicode 断行规则），行首不再出现「。」「）」
- **AppButton 高度下限 28**：52 处被压到 20/22px 的按钮文字垂直溢出压描边；补 `success` kind
  （此前 ReviewIssueDialog 传的 "success" 静默落入 secondary，强调丢失）
- 压住 41 处主题外硬编码色：AppButton accent 衍生色、StageStepper/StepPills 夜间几乎不可见的
  死色、未保存圆点、选中文本色等全部入库；AboutDialog 原生 CheckBox → AppCheck
- 去emoji/字符图标（⚡✦❖☰✍📋✨🎯💡▲▼×）：统一 AppIcon 线性图标；
  Console 折叠把手的竖排"Console"残留改为图标+tooltip

### 变更

- 流水线质量趋势柱改中性色+阻断红点（原高饱和红绿大色块）；阶段卡字符图标换线性图标；
  面板头统一规格（66px + fsTitle 粗标题 + 弱化元信息）；质量四格换 AppStatTile
- 预设库空详情区、审校 0 项、生成配置空壳、项目文件空列表全部补空态设计，消灭死黑大区
- 弹窗 footer 从锚点定位改为 RowLayout+弹性占位（9 处）；「关闭」按钮形态统一
  （唯一出口=描边次级，伴随主操作=幽灵）

### 测试与闸门

- 离线单测 421 项全绿；无头探针 41 → **42 个**；QML 34 → **40 个文件**全部可编译
- 发布流水线闸门 3 → **4 道**（+视觉 token 审计）；`docs/v0.17_status_report.md` 附三轮
  截图验收全记录（面板 9/9、弹窗 21/21 通过）

---

## [0.16.0] - 2026-09-04（契约有牙齿 · 同人文导入 · 升级不吃数据）

> 动机：v0.15 把「设定怎么进 prompt」做扎实了，这一版解决「说了不算」——
> 硬规则要能拦、借来的世界观要能进得可撤、升级要能不动你的稿子。

### 新增

- **must 契约的确定性层**：规则里的 `` `pattern` `` + `mode：require/forbid` 由 `app/mustscan.py`
  零成本本地判定，不再依赖模型自觉；三层处置接线——审校里可见、终稿锁定硬拦、
  你选择强锁则**连同原因署名留痕**（不禁止绕过，但绕过不可匿名）
- **扩写/压缩/去味/局部改写四张模板补上 must 契约槽**：这四步都在整章或整段重写正文，
  只给初稿注入契约等于让中间步骤裸写
- **本书契约面板**：正则规则逐条可见、可改、可删；导入批次在同一页整批撤销
- **外部文档导入按同人文重做**：11 个落点（原作/核心设定/世界书/正则/分歧点/大纲/细纲/正文/
  伏笔/原作进程/角色状态），只拆文档里真写得有的内容——逐字落点做字节比对、改写型落点逐条引证验真，
  **未验真的条目默认不勾选**；确认前给出完整映射预览；借来的原作条目自动 `[常驻]` 免被预算裁掉，
  分歧点落成 must 契约拦住 OOC，`设定/导入记录.json` 支持整批撤销（作者改过的内容不动）
- **更新通道补齐**：开机自动检查真正接线（此前是死键）、发现结果三态区分、
  清单里的 SHA-256 随新版提示显示、书稿/配置/日志三个目录一键直达
- **Agent 思考过程可见**：四类成因（thinking 未回传 / 相位未开思考 / 端点不支持 / 增量被吞）分别定位

### 修复

- **升级不吃用户数据**（两条路径各自的问题）：
  - 覆盖安装先清空旧程序树，不再让已删除/改名的模块残留在 `_internal` 里与新版本混拼
  - 安装位置若落在书稿目录或含 `config.json` 的目录上，目录页当场拦下；升级跳过目录页时在准备页提醒
  - `config.json` 解析失败不再静默回落默认值（下一次保存就会吃掉连接档案），先另存为
    `config.json.broken-<时间戳>`
  - 检查更新不再用裸线程跨线程 emit；网络失败不再被报成「已是最新版本」
- **停止真正打断流式生成**并给出可感知反馈；停止不再被误报为细纲生成失败
- 流式期间滚动跟随改为条件化（你在往回看时不再被拽走）；共写输入框长文本折行与自增高
- 「确定细纲」与「继续生成下一批」拆成两个动作；细纲批次可定位、跟随最新、正文/细纲可切
- 同色卡片补 1px 描边，容器不再与背景糊成一片；日志目录改与配置目录同源

### 变更

- **开机检查更新默认关闭**：`updates.check_on_start` 改名为 `updates.auto_check`，需在「关于」里
  显式勾选。旧配置里那个 True 属于一个从未接线的开关，不代表用户选择，故不自动继承
- 版本清单 URL 的仓库名大小写与仓库真实拼写对齐（两种都能取到，但两处写法曾不一致）

### 测试与闸门

- 离线单测 316 → **421 项**；无头探针 35 → **41 个**
- 新增 `probe_import_ui.py`（导入全链路 33 项）、`probe_about_ui.py`（更新通道 16 项）
- 新增 **`probe_qml_compile.py`** 并编进发布流水线第三道不可跳过闸门：QML 属性写错会让整棵
  界面树静默加载失败，而引擎一个字都不打印——单测与 prompt 基线都看不见它
- 装配基线补钉外部导入模板的结构契约（该模板不在流水线装配点上，字节基线管不住）

---

## [0.15.0] - 2026-09-03（三件套深化 · 世界书条目化 + 预设组装层）

> 动机：生成稿「整体还没到优秀网文的行列」。路线：学 SillyTavern 的 worldbook / regex / preset 三件套深化，
> 而不是盲目对比调参。本轮 GUI 先行，TUI 侧同步延后。

### 新增

- **条目化世界书引擎 `app/wb.py`**：替换整本字符串注入——按锚点激活、分档预算、`activated/dropped` 留痕，
  长设定不再被截断成一团糊（A1/A2/A3/A5 + W1/W2）
- **世界书权威语义双轨**：写作侧升格为强约束、审校侧产出修正提案（A2）；正则多行与分节预算修复（A4/A5）
- **工艺卡注入 + author_note**：6 类场景卡真正接到装配链（battle/payoff/emotion/dialogue/mystery/lowkey），
  题材 `genre_block_for(preset, stage)` 6 阶段特化提示全接线（P0b/P4）
- **单一 payload 构造 + phase 分相位采样**：`LLMClient._build_payload` 一处生成请求体，11 相位各自档位，
  审校相位温度锁定（P0a/P1）
- **章级生成配置快照**：落 `.annotations`，可查看每章实际用了什么参数（`GenConfigDialog.qml`），
  并支持「固化为模板」复用（P2/P3）
- **审核三层强化**：`app/core/scan.py` L0 确定性预检（零成本先拦硬伤）+ 引证验真（无登记即死锁已修）
  + 多轮投票降噪（P1/P2/P4/#37/#47）
- **剧情反哺**：新设定回写细纲/设定并约束后文，扩充实体演进与世界观揭示类目，世界书首版自动生成（A1/A3/#40）
- **字数闸门与陈旧防护**：低字数不再静默过审，未达标拦截并给出可解释报告（#41）
- **待修章节汇总 + 一键修复**、一键生成简介与标签、共写 must 级自检（#39/#40/P5）
- **提示词装配基线探针** `tests/probe_prompt_baseline.py` + `evals/prompt_baseline.json`：43 个装配点摘要，
  改装配必留痕（W0a）；金标回归评测台 `evals/gold_set.json`（P4）
- **打包态一致性自检** `app/selftest.py` + `QianBi-Novel.exe --selftest`：导入/资源清单/装配摘要/QML 装载
  四段摘要，开发态与打包态逐字节对拍（见「变更」）

### 修复

- 符号级 `dual_sync` 门禁：双端同步检查从整文件比对升级到符号指纹，允许有登记的功能节奏差异、
  静默漂移即红（#61）
- 两处过期探针断言（`probe_agent_relay` / `probe_chapter_lock`）：#41 闸门落地后锁定语义变了，
  断言未跟上；改用配置绕值并桩掉反哺线程，新增「已比对但字数未达标 → 拦截而非静默锁定」回归（#62）
- 预设库图标缺失、通用面板过窄遮挡、设置页文字重叠、导出问题、科学计数法显示（#34/#38/#33/#31）
- 修复 Agent 改为定向最小改动 + 同一性验收，无引证登记时不再死锁（#36/#37）
- `probe_word_block` 收尾原生 abort（Qt 静态析构），统一 `os._exit` 收尾（#63）

### 变更

- 世界书调用点分批迁移到 `app/wb.py`，`FINAL_REVIEW` 双份装配合一（W0c/W2）
- 发版流水线新增 `--skip-qml`；打包冒烟探针补上「静态清单审计 + 动态摘要对拍」两道门禁，
  杜绝「包里少一个 .qml 而流水线照样绿」

### 已知遗留

- **#42 共写全流程真机走查**与执灯人真机验收未做：本包含未经真机验证的功能
- 安装包**未做代码签名**（无证书）：首次运行会遇到 SmartScreen「未知发布者」
- TUI 侧同步延后（`app/wb.py` 已有双端约定，但 GUI 新增功能未回填）

## [0.14.0] - 2026-08-29（GUI 商业化封装 · 首个安装版）

> 封装计划 [`docs/plan_gui_packaging_v1.md`](docs/plan_gui_packaging_v1.md) 全量落地：从「跑源码的开发工具」到「可安装、可更新、可信赖的发行版」。开源路线（MIT + 免费 + BYOK）。

### 新增
- **安装器与便携包**：Inno Setup per-user 安装（卸载保留书稿）+ 便携 zip + SHA256SUMS；一键发布流水线 `scripts/build_release.py`（版本资源注入/质量闸门/打包冒烟门禁）
- **单实例锁**：二次启动唤起既有窗口，杜绝多开写坏配置
- **全局崩溃处理**：未捕获异常脱敏落盘 + 主线程对话框（打开日志/复制详情）
- **API Key 安全存储**：迁移至 Windows 凭据管理器，config.json 不再落明文（旧配置自动迁移）
- **应用内检查更新**：GitHub Releases 清单主通道（URL 可配置），新版提示 + 直达下载
- **首启向导**：两步引导（开源声明/数据目录 → 连接配置指引），F1 呼出「关于」
- **关于对话框**：版本/检查更新/日志与数据目录/遥测开关
- **遥测（opt-in，默认关）**：仅本地文件落点，不上传
- **第三方声明与隐私说明**：THIRD-PARTY-LICENSES.md / docs/PRIVACY.md
- 打包冒烟探针 `tests/probe_packaged.py`；发版 checklist `docs/release_checklist.md`

### 修复
- 运行日志与崩溃现场统一脱敏（api_key/sk-/Bearer 模式）

### 变更
- 打包规格重写：onedir 路线、UPX 关闭（杀软误报）、依赖裁剪、Windows 版本资源注入
- `build_exe.py`（旧 onefile 脚本）废弃，统一入口 `scripts/build_release.py`

## [0.13.0] - 2026-08-28（TUI 优势功能完整移植 · 定版）

> 把同源 TUI 项目中优于 GUI 的核心功能完整移植到 GUI 端，并通过 5 章真实 LLM 共写 e2e 验证。
> 详细升级报告见 [`docs/v0.13_gui_update_report.md`](docs/v0.13_gui_update_report.md)。

### 新增功能

- **10 套 v2 题材预设**：修仙·凡人流/都市改命/都市异能/克苏鲁·收容档案/规则怪谈/无限流/末日废土/历史权谋/电竞系统/童话寓言，
  含 `stage_hints` 6 阶段特化提示（`genre_block_for(preset, stage)`）+ 独立预设库面板（`PresetLibraryPanel.qml`）
- **6 维最终审核 + 反馈环**：`FINAL_REVIEW_PROMPT`（黄金开章/爽点闭环/金手指/因果对账/人物弧光/钩子）
  + `ROOT_CAUSE_PROMPT`（5+2 根因溯源）+ `REVISION_TARGETS_PROMPT`（定向改稿）；审校结果落盘（`save_review_findings`）
  + `chapter_need_human` 熔断；共写档三选一弹窗（`ReviewIssueDialog.qml`）
- **6 类场景卡**：`scene_cards.py`（battle/payoff/emotion/dialogue/mystery/lowkey，含汉化关键词路由）
- **字数双界闸门**：`gates.check_word_bounds` 扩写 + 压缩双向（新增 `TRIM_PROMPT` 超长压缩）
- **3 主题切换**：夜间/羊皮纸/纯白（`Theme.qml` 三套 Singleton，Ctrl+T 循环）
- **5 章 e2e 双层测试**：`test_5ch_mock.py`（mock 快速回归）+ `test_5ch_e2e.py`（真实 LLM 全流程）

### 变更

- 正文/审校/细纲/大纲 prompt 全面增强：数字铁律、金手指约束条款、金手指禁人格化、旧秩序统治逻辑、关系一致性自检、字数自测
- 版本单一来源：`app/__init__.py: __version__`，Qt `applicationVersion` 接入

### 修复（真机鲁棒性三修）

- `QIANBI_CONFIG_DIR` 环境变量隔离配置 + `recent_projects` 路径规范化（防混合斜杠/重复）
- 流式空内容时 thinking 模式自动降级重试
- `shot_hwnd.py` 截图 BGRA→RGB 色序修正

### 已知遗留

- 场景卡仅移植未接线（`scene_card_hint` 暂无消费者，与 TUI 源一致），接线列为后续候选

### 验证

- 42 项新单测全过（预设 13 / 场景卡 15 / 审校 14）｜ 5 章真实 LLM 共写 e2e 完整跑通（DeepSeek V4 Flash）
- 详见 `docs/v0.13_gui_update_report.md` 与 `tests_output/5ch_e2e/`

## [0.12.0] - 2026-08-19（共写工作流 2.0 真机测试迭代 · 已合并提交）

> 本轮为「共写档真机全量测试」第一轮：用 ocgo-flash（deepseek-v4-flash，三槽位）真实模型，
> 通过 GUI 驱动（tests/ui_drive.py：UIA + PostMessage 点击 + PrintWindow 截图）在台前逐步操作应用，
> 按用户使用逻辑走共写六阶段。测试于「单元细纲」阶段暂停，发现 9 项问题；**本轮已全部修复**，
> 修复后全量回归 13 脚本全绿；已合并提交（见提交 `a19b640`）。

### 问题修复（9 项 · 对应真机测试问题清单）

| # | 问题 | 修复 |
|---|------|------|
| 1 | config run_mode 与项目 cw.mode 脱同步 | runMode() 只认项目状态；config 'cw' 未迁移时按 auto 显示 |
| 2 | cw_project 阶段无题材预设选择 UI | dock 新增预设 chips 行（复用 genrePresets + cwPreset 属性 + setCwPreset） |
| 3 | 「确定」无防抖/连锁保护 | confirmCwStage 加重入锁 `_cw_confirming`（try/finally 全路径覆盖） |
| 4 | 空转写可点确定 | 总结类阶段先校验转写非空，空则 toast 拦截 |
| 5 | 打回只能打回当前阶段 | `rollbackCwStageTo(key)` + `cwReachedStages` + dock 打回目标 chips 选择器 |
| 6 | 阶段 Agent 边界不拦截 | CO_DIALOGUE_PROMPT 加「阶段边界」段（{stage_label} 注入） |
| 7 | 交接块脑补未讨论的开放问题 | CO_SUMMARIZE_PROMPT 加「交接小节只许来源于转写、禁编造」约束 |
| 8 | 对话无超时反馈/无法取消 | busy 秒数计时显示（cwBusySeconds）+ 取消按钮（cancelCwWorker：requestInterruption + 结果丢弃，done/sum/batch 回调加取消守卫） |
| 9 | GateBanner 按钮溢出 dock | dock 加宽 400→480（maximumWidth 560），按钮行回归校验 |

### 本次迭代做了什么

- **真机测试驱动基建**：tests/ui_drive.py 新增文本输入辅助（type_text/press/set_field_text，SendInput 支持中文）；
  tests/cw_live_drive.py 新增磁盘状态轮询（阶段/转写/产物文件等待）；建立「截图 → 像素扫描/UIA 枚举 → 坐标表 → 点击 → 磁盘校验」驱动循环
- **控件坐标表**：tests_output/cw_live/控件坐标表.md——已实测确认 9 个控件坐标（确定/打回/输入框/发送/单元表单/编辑器）与定位方法（蓝色扫描/亮像素聚簇/UIA 锚点），后续驱动直接查表
- **六阶段实机走查（到单元细纲）**：
  - 创建项目 → 核心设定（3 轮对话收敛：金手指限制/角色表/突破代价/灵痕伏笔 → 确定定稿 题材定位.md 3106 字节，跨档契约结构完整）
  - 剧情总大纲（3 轮对话：三卷九单元框架 → 压缩杂役期至 110 章 → 裁决道德底线与第 8 章落点 → 定稿 大纲.md，角色表/区间块/预计总字数保真）
  - 世界书与正则（2 轮对话：世界书 5 板块 + 正则 9 条 → 用户增删裁决 → 定稿 世界书.md 1827B + 正则.md 1308B，9 条 must）
  - 单元细纲（登记单元 1-30 章·杂役院开篇 → 2 轮对话（灵感细纲 5 章+压抑三层+落点）→ 确定 → 单元总纲.md + OutlineBatchWorker 滚动生成 1-5 章细纲（≈200 字/章）→ 误触「打回」实测打回级联（细纲全部失效归档，符合矩阵）→ 重确定再生成）
  - 暂停于：正文写作 5 章未开始
- **测试产物**（tests_output/cw_live/）：转写_*.md、产物_*.md、交接块_*.md、截图 00-42、问题清单.md、控件坐标表.md、审稿任务.md（子代理审稿 prompt 已备好）
- **测试中发现并在现场处置**：确定按钮连点误触发（改用单击）；单元对话 20 分钟未回复（重启应用清理）；误触打回导致细纲失效（重确定恢复）

### 真机测试发现的问题（9 项 · 已全部修复）

| # | 严重度 | 问题 | 一句话 |
|---|--------|------|--------|
| 1 | 中 | config run_mode 与项目 cw.mode 脱同步 | 芯片显示「共写」但 dock 不出现 |
| 2 | 中 | cw_project 阶段无题材预设选择 UI | setCwPreset 有槽无控件 |
| 3 | 高 | 「确定」无防抖/连锁保护 | 连点两次 → 空转写总结 + 跳阶段 |
| 4 | 高 | 空转写可点确定 | 产出 81 字节垃圾产物仍推进状态机 |
| 5 | 高 | 打回只能打回当前阶段 | 垃圾产物在后一阶段无法跨阶段重做 |
| 6 | 低 | 阶段 Agent 边界不拦截 | 大纲阶段问设定话题，Agent 照答 |
| 7 | 高 | 交接块生成器脑补未讨论的开放问题 | 「三条铁律」「A/B/C」凭空出现并污染接力上下文 |
| 8 | 高 | 对话 20+ 分钟无回复、无超时反馈、无取消 | 用户无法区分「慢」与「坏了」 |
| 9 | 高 | GateBanner 按钮布局溢出 dock | 四按钮总宽超内宽，右侧按钮位置偏移（确定实测 x 631-719） |

## [0.11.4] - 2026-08-19

### 新增功能

- **共写档 M5：主 Agent（Supervisor）接力 + 报告区（共写工作流 2.0 收官）**：
  - SupervisorWorker（复用审校槽，上下文 ≤6k 字）：触发点① 每章定稿前衔接比对——上一章结尾 ↔ 本章 ↔ 下一章细纲 + 范围控制 + 章间逻辑一致性 + 全书级 AI 味 + 世界书/正则契约，只出报告绝不产正文（报告体格式，与审校槽 ===BLOCKING=== Findings 明确去重）
  - 定稿流程：cw_prose「确定」= 先跑主 Agent 比对（supervised 留痕）→ 报告进对话区报告区 → 再次点「确定」即终稿锁定
  - 触发点②：世界书 reopen 写回后自动影响提示——存在 locked 章时提示「已锁定章节不会自动修改，建议显式解锁后重核」（纯逻辑，不发 LLM）
  - CwDialogueDock 主 Agent 报告区（时间戳 + 可关闭；与审校槽 Findings 分开展示）
- **回归探针**：tests/probe_agent_relay.py（12 项：参考块只注入上环节/上限、supervisor 槽位/上下文/不产正文/非审校格式、触发点①②）

### 变更

- cw_prose「确定」两段式：先衔接比对报告 → 确认后锁定

### 修复

- （无功能修复；本轮为共写 M5 收官增量）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8（无新增 QML 告警）
- 接力编排探针 12/12 ｜ 章节锁定 15/15 ｜ 跨档锁定 5/5 ｜ 单元细纲 14/14
- 世界书格式 16/16 ｜ 共写世界书 14/14 ｜ 互续 7/7 ｜ 状态机 23/23 ｜ 对话 19/19（全量 13 脚本全绿）

## [0.11.3] - 2026-08-19

### 新增功能

- **共写档 M4：章节确定锁定（两级提交）+ 读改揣摩**：
  - 两级提交语义落地：保存=临时草稿（原语义不变，`.drafts/` 防抖）；「✓ 章节内容确定」=终稿锁定——locked 标记存 `正文/.annotations/第N章.json`（批注/书签/位置字段保留），编辑器只读 + 顶栏「✓ 已确定（终稿锁定）」徽章 + 解锁按钮
  - 锁守卫全部下沉 project.py（is_chapter_locked / set_chapter_locked / attempt_unlock）：worker 与 UI 同进程读取；显式解锁唯一放行通道，解锁前终稿仍留 `.versions/` 版本历史
  - 跨档互斥：自动档 G9 回退直接拒绝 locked 章（「该章已锁定，请先在共写档显式解锁」）；regenerateStage('ch_outline') 不删 locked 章细纲（锁定细纲=已定契约）；cw↔自动档迁移双向保持锁定；自动档定稿本身不产 locked
  - 读改揣摩：保存正文有改动 → ReadbackWorker（复用审校槽）通读改动、揣摩修改意图进对话区；readback_on_save 默认开 + readback_min_diff 最小改动量阈值节流（0=每次都触发）+ 编辑器「读一遍」手动按钮；设置面板可开关/调阈值
- **回归探针**：tests/probe_chapter_lock.py（15 项：锁守卫/标注仓兼容/版本保留/bridge 语义/迁移保持）+ probe_chapter_lock_cross_mode.py（5 项：G9 拒 locked/解锁放行/细纲跳过/迁移保持）

### 变更

- cw_prose「确定」按钮语义 = 锁定当前章（终态不前进）；共写档锁定章保存被拒

### 修复

- （无功能修复；本轮为共写 M4 增量落地）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8（无新增 QML 告警）
- 章节锁定探针 15/15 ｜ 跨档锁定探针 5/5 ｜ 单元细纲探针 14/14
- 世界书格式探针 16/16 ｜ 共写世界书探针 14/14 ｜ 互续探针 7/7 ｜ 状态机 23/23 ｜ 对话 19/19

## [0.11.2] - 2026-08-19

### 新增功能

- **共写档 M3：细纲二级结构（单元 → 章）**：
  - 单元登记表单（cw_unit 阶段对话区：起始章 / 目标完结章 / 单元主题 → state['cw']['unit']）
  - 确定单元 = 总结定稿落盘 `大纲/单元总纲.md` + 自动滚动生成下一批 5 章细纲（OutlineBatchWorker，辅助槽，每章 ≈200 字：核心事件+故事内容；±10 章上限：批次完结章 ≤ 目标完结章+10；已写正文/已有细纲自动跳过）
  - 「校验细纲」按钮：ReviewOutlinesWorker 重读校验（与既定剧情/世界书/正则/上文结尾的衔接、单元范围），无阻塞自动进入正文写作，有阻塞在对话区指出问题、修改后重试
  - 细纲可直接在编辑器修改（共写档保存=产物保存）；CO_UNIT_OUTLINE_PROMPT / CO_OUTLINE_REVIEW_PROMPT 落地
  - 单元总纲属主保持 = cw_unit（worldbook 阶段只产世界书+正则两文件）
- **回归探针**：tests/probe_co_outline_parse.py（滚动批次/±10 上限/200 字解析/单元属主/批次落盘/校验两态 14 项）

### 变更

- cw_unit「确定」不再直接前进到正文：确定单元 → 滚动生成细纲 → 校验通过后才进入 cw_prose

### 修复

- （无功能修复；本轮为共写 M3 增量落地）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8（无新增 QML 告警）
- 单元细纲探针 14/14 ｜ 世界书格式探针 16/16 ｜ 共写世界书探针 14/14 ｜ 互续探针 7/7
- 共写状态机探针 23/23 ｜ 共写对话探针 19/19（M1/M2 探针保持全绿）

## [0.11.1] - 2026-08-19

### 新增功能

- **共写档 M2：预设 grow_* 参考字段 + 世界书/正则落地**：
  - 预设新增 5 个共写参考字段（grow_core_template / grow_outline_template / grow_worldbook_direction / grow_unit_logic / grow_regex_direction），内置仙侠/都市两套补齐；`presets.grow_block()` 为共写档阶段 Agent 唯一读取入口——仅参考、不得锁定死，不进 genre_block（六字段回归不破），旧 JSON 缺字段返回占位不报错
  - 「正则」抽象接口 `project.regex_rules(proj)`：默认「逻辑约束规则集」（每条 rule/level: must/should/scope），字面正则样本为备选结构；设置面板「正则语义」单选（logic/regex），只影响解析与写入结构
  - 世界书/正则注入正文、审校、细纲三段 prompt：`.format` 组装 + 空串回退占位（旧项目无文件不抛 KeyError）
  - 跨档契约强化：CO_PRODUCT_STRUCTURES 强制保留 `## 主要角色表` 表头、`第N章/N-~M章` 章节区间块、`预计总字数` 字段（cw↔自动档互续）
  - 共写参考块升级：世界书/正则 + grow_* 方向注入各阶段 Agent；世界书总结产物经 `project.split_worldbook_product` 拆分为 设定/世界书.md + 设定/正则.md
- **回归探针**：tests/probe_worldbook_format.py（三段 prompt 组装/空串回退/缺参对照/规则解析）+ probe_worldbook_format_co.py（共写三 prompt + grow_block）+ probe_cw_to_auto_compat.py（cw 产物喂自动档读取器）

### 变更

- 正文/审校/细纲 prompt 增加世界书与正则约束块（占位注入，空串回退，不改变既有参数）

### 修复

- （无功能修复；本轮为共写 M2 增量落地）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8（无新增 QML 告警）
- 世界书格式探针 16/16 ｜ 共写世界书探针 14/14 ｜ cw↔自动档互续探针 7/7
- 共写状态机探针 23/23 ｜ 共写对话探针 19/19（M1 探针保持全绿）

## [0.11.0] - 2026-08-19

### 新增功能

- **共写档（co-write · M1）**：与自动流水线并存的六阶段人机共写（创建项目→核心设定→剧情总大纲→世界书与正则→单元细纲→正文写作），每阶段在对话区与对应子 Agent 讨论后点「✓确定」总结定稿、可「↩打回」重议。
  - 六阶段状态机（`state['cw']` 键分离，绝不碰自动档 `state['stage']`）+ 打回级联矩阵（core→大纲+全部细纲+世界书+正则+单元总纲；outline/worldbook→细纲+单元总纲；unit→细纲），失效产物归档 `pipeline_debug/rollback/`
  - 世界书回看软切（reopen）：cw_unit/cw_prose 阶段入口，不级联删除，重确定写回后返回原阶段
  - 档位粘性按项目判定（`state['cw']['mode']`），旧项目进入共写档按产物推断续跑阶段；流水线运行中不可切换，共写档内 startPipeline 互斥
  - run_mode 新增 'cw'（流水线面板模式芯片循环切换）
- **共写对话机制**：CwDialogueDock 对话区（Agent 头部 + GateBanner 确定/打回/回看世界书 + 消息流 + 流式回复 + 输入行）+ StageStepperCW 六卡导航（已到达阶段可回看）
  - DialogueWorker：每轮输入启动一次性 worker，提示词方案①（角色职责 + 上一环节交接块 ≤800 字 + 本阶段参考块 + 转写 ≤4k 字拼进 user body，零回归）
  - SummarizeWorker：确定 = 对已收敛讨论做一次总结定稿调用；build_handoff 为「→ 下阶段交接」小节唯一生成者，下一阶段只读交接块
  - 对话转写按阶段存储、截断保尾（≤4k 字）
- **回归探针**：tests/probe_cw_state.py（六阶段推进/打回矩阵/reopen 回边/迁移/转写截断）+ tests/probe_cw_dialogue.py（router 打桩：注入方式/槽位映射/总结定稿/交接块）

### 变更

- 运行模式切换改受控：cw ↔ 自动档同步项目级粘性（仅阶段空闲可切）
- 编辑器「保存」按钮按档位分流：共写档=产物保存（不走版本快照），自动档=正文保存（保存驱动版本）

### 修复

- （无功能修复；本轮为共写档增量落地，既有契约未动）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8（无新增 QML 告警）
- 共写状态机探针 23/23 ｜ 共写对话探针 19/19 ｜ bridge 冒烟（档位切换/六卡/回看/预设/确定推进/reopen/互续）全过

## [0.10.0] - 2026-08-19

### 新增功能

- **步骤级决策门（Step Gates）**：把「章末确认」升级为「每步决策门」——每一步完成后停在决策条：
  产物摘要 + 想法输入（继续 / 带想法继续 / 带想法回退）+ 快捷键（回车 / Ctrl+回车 / R）。
  - 默认 border 边界确认：G2 大纲 / G5L 开写前 / G9 定稿后硬停；其余 G1/G3/G4/G6/G7/G8 软门可开关
  - 回退语义：G2 回退=归档清空大纲与全部细纲重拟；G9 回退=归档本章重写（版本历史保留）
  - 想法注入：门内想法写入指令队列，注入下一步执行上下文；章间可携带（G9 想法进下一章）
  - 运行模式三态：自动 / 逐步确认 / 边界确认（流水线面板循环切换 + 门清单开关对话框）
- **题材预设系统**：内置仙侠 / 都市天命两套（文风/世界规则/套路/禁忌/去味黑名单/审校专项），
  用户预设目录 ~/.qianbi_novel/presets/，项目级 genre_preset 随时切换、下一章生效
- **门机制回归测试**：tests/probe_gate_flow.py（4 用例）+ tests/probe_gate_ui.py（离屏 UI 8 项）

### 变更

- 旧「逐步确认」章末暂停（step_confirm）仅 auto 模式生效，新模式下由 G9 决策门承担

### 修复

- （无功能修复；本轮主要为机制落地与回归）

### 验证

- assert_v099 18/18 ｜ smoke_func ALL_FUNC_OK ｜ 门流回归 4/4 ｜ 门 UI 探针 8/8 ｜ 真机启动正常

## [0.9.9] - 2026-08-17

V1.0.0 正式版前最后一个版本：人 AI 共写工作台整体升级（六大成熟体系）。

### 新增功能

- **阅读器体系**：全屏沉浸阅读（F5）、三主题（夜间/羊皮纸/纯白）、字号 5 档/行距 3 档/宋黑、滚动与左右翻页、抽屉目录、选中标注（三色高亮/批注/灵感直通创作笔记）、标注管理与回跳、位置记忆、多书签、未定稿徽章（流式实时更新）、首行缩进排版；标注存 `正文/.annotations/`
- **共写体系**：创作驾驶舱（四阶段卡片：查看/带指导重生成/自动-逐步模式切换）、创作笔记面板（想法 CRUD + 注入范围 下一章/通用/指定章 + 全局写作偏好注入所有章节）、选区浮动工具栏（改写/扩写/精简/按想法改）、改写上下文四档、多段连改、整章重写确认 + 重写前备份版本、质量趋势图（近 20 章）
- **数据体系**：每日自动备份 + 一键 zip、导出预览对话框（章节分隔/标题格式 + 前两章实时预览）、统计面板（章节/字数/今日/本周增量/token/成本）
- **设置体系**：四标签页（连接与模型/写作偏好/外观/系统）、快捷键（Ctrl+S/F5/Esc/←→/Ctrl+B/Ctrl+E）、编辑器偏好（字号/限宽/流式速度打字机）
- **流式体验**：thinking 呼吸动画、打字机/即时流式速度
- **写作质量（prompt 工程）**：文风锚定样本（上一章开头注入防漂移）、章末钩子铁律（禁止平稳/静止收尾）、反复述红线、人物温度约束、信息增量铁律 + 开篇期主角占优时刻、扩写防注水、审校新增商业质量建议（钩子/爽点/节奏，仅 ADVISORY）

### 变更

- **UI 全面重构（ZCode 风格）**：中性灰阶基底（#161616 系）+ 白色主操作 + 实色发丝线 + 控件 4px 圆角；全新 Canvas 矢量线性图标系统（18 枚）替换全部字符图标；48px 纯图标导航栏；48px 紧凑面板头；44px 编辑器顶栏（按钮图标化）；装饰性卡片全面去描边（表面色阶分层）
- **服务商预设收敛**：内置仅 DeepSeek 官方与 OpenCode Go 官方两家（提示词完整适配 DeepSeek API）；移除 OpenAI 预设，第三方经「自定义」接入，连接页明示适配范围
- **想法数据结构化**：`pending_ideas` 由纯字符串升级为结构化（状态/注入范围/时间），旧数据自动兼容迁移；消费后标记 applied 保留历史

### 修复

- `openChapter()` 打开章节未更新内部章号，导致版本历史/保存快照作用在旧章号上
- 重写章节前旧正文自动归档「重写前备份」版本（此前直接删除无安全网）

### 验证

- M6 Phase A 本地全量清单 38/38；结构化断言 18/18；16 步真机走查；功能冒烟 ALL_FUNC_OK；M1 回归通过
- 真实 LLM 全量测试（OpenCode Go · deepseek-v4-flash）：4 章成稿，双闸门全过，成熟度评估 76/100（达签约线，详见 tests_output/m6_成熟度分析.md）
- exe 打包冒烟通过（58.7 MB）

## [0.2.1] - 2026-08-16

### 修复

- **UI 重叠（重要修复）**：连接页三张卡片（连接信息/模型参数/槽位绑定）、流水线页当前章状态卡、书架新建对话框三个分组——卡片容器 `Rectangle` 无显式高度导致内容全部溢出重叠。修复为高度绑定内部内容列（实测卡片高度 300/194/194px 正常）
- **书架清理**：清除历史测试残留的 recent_projects（"诡异复苏"等临时项目记录），内置示例小说《改命笔记》进入书架并作为默认打开项目
- 新增 `examples/改命笔记/` 完整示例（详见 [README](README.md#使用指南)）

### 验证

- `shot_ui.py` 四页渲染无 QML 警告；`check_layout.py` 0 越界
- 连接页卡片级元素实测：三卡高度正常，无塌陷
- `smoke_func.py` / `test_quality.py` 全绿

## [0.2.0] - 2026-08-16

### 新增功能

- **OpenCode Go 深度集成**
  - 内置连接模板 `ocgo-flash`（OpenCode Go · V4 Flash，Base URL `https://opencode.ai/zen/go/v1`），新老用户配置自动补全
  - 新增服务商预设「OpenCode Go」，连接页可直接选择并一键填入 Base URL 与模型列表
  - 连接档案支持**思考模式**（默认/禁用/启用）与**思考强度**（low/high/max）：启用时发送 `thinking: {"type":"enabled"}`，强度作为顶层 `reasoning_effort` 参数（参考 DeepSeek 官方 Thinking Mode 文档与 opencode-go provider 的 effort 映射）
  - 内置 ocgo-flash 模板默认 `thinking=enabled + reasoning_effort=max + max_tokens=65536`
- **带指导重写（human-in-the-loop 关键落地）**
  - 章节队列右键菜单与流水线工作台新增「带指导重写…」：输入重写要求（如"这章别写死女主"），删除该章正文并登记指导语，续跑时注入正文生成 prompt
  - 指导语经 `pipeline_state.json` 的 `pending_guidance` 字段持久化（消费即删除，崩溃/重启不丢失）
- **项目文件浏览/编辑**
  - 流水线顶栏「📂 项目文件」：应用内浏览与编辑 设定 / 大纲 / 追踪 目录下全部 md 文件（左文件列表 + 右编辑区 + 保存），改完保存即可，下游续跑自动读取
- **选题 AI 展开**
  - 新建项目对话框「✨ AI 展开」：把一句话灵感交给辅助槽模型，展开成 3 个可开书的选题方向（核心情绪/梗概/读者契约/对标/差异化）后填入灵感框
- **UI 布局重构（v0.2.0 第二轮）**
  - 流水线页改**三栏工作台**：左 300px 章节队列（含全书进度条）｜ 中写作工作区（当前章状态卡 + 微循环步骤条 + 质量四格 + 快捷操作 + 运行引导）｜ 右 300px 运行日志（可清空）
  - 章节详情改**左右双栏**：左 70% 正文编辑区，右 300px AI 味扫描结果侧栏（卡片化条目/空状态引导/点击定位）
  - 书架：封面双色渐变 + 悬停进入提示，空状态居中引导，新建对话框三段分组（保存命名/创作设定/灵感）
  - 连接页：左列表显示服务商标签与槽位徽章，右表单卡片化四组（连接信息/模型参数/操作/槽位绑定）
  - 主框架：导航图标化 + 当前项目名摘要贴底
  - 修复 3 处 QML 布局缺陷（渐变 parent 引用、Column 锚定冲突、非法 fillHeight 赋值）

### 优化改进

- **细纲批量生成修复**（原"每批 5 章只出 1 章"）
  - 实测根因：max 思考下 5 章批的输出预算被推理吃光（`content=0, finish=length`）
  - `OUTLINE_BATCH 5→2`：2 章批实测稳定（约 160 秒/批），失败自动拆半重试兜底
  - 细纲解析器兼容 `=== 第N章 ===`（带空格）与 `### 第N章` markdown 标题两种降级格式
  - 细纲批 prompt 明确"为每一章各输出一个完整小节，一章不漏、不合并"
  - 解析失败日志携带模型输出长度与已解析章号（诊断友好）
- **审校不再越修越糟**
  - 分级标准收紧：阻塞级只留给读者会明显出戏的硬冲突（角色已死又出场/能力前后矛盾/伏笔提前回收等），分钟级时间偏差等可商榷项一律归 ADVISORY
  - **回滚保护**：审校修复轮后复扫，若阻塞数未减少则保留原稿并标「待修」（实测第 3 章 1→1 处时正确回滚）
- **全书大纲防超限**
  - 核心设定截断至 4000 字符再入大纲 prompt（修复前 13.5 分钟失败返回空，修复后 6 分钟成功）
- **LLM 客户端输出预算提升**：e2e 与内置模板 max_tokens 提升至 32768/65536，适配 max 思考档
- **API Key 安全清理**：从 3 个 probe 脚本移除硬编码 Key（曾随历史提交进入公开仓库，见下方安全公告）
- **测试脚本健壮性**：smoke 槽位断言不再假设连接顺序；e2e 支持思考模式/强度环境变量注入、停止信号直连（避免停止请求后下一章继续开跑）、项目名带时间戳避免目录冲突、Windows 控制台 UTF-8 输出

### 测试新增

- `tests/probe_max_thinking.py`：max 思考长输出预算实验（16384 截断 / 32768 完整 / 65536 可用）
- `tests/probe_outline_batch.py`：细纲批次参数化实验（章数/思考强度/max_tokens 组合）
- `tests/check_layout.py`：离屏渲染后遍历可见元素做窗口边界越界检查（0 越界为通过）

### 验证记录（OpenCode Go · deepseek-v4-flash · thinking=enabled · effort=max）

- 端到端 3 章实测：核心设定 49 秒 / 全书大纲 6 分钟 / 细纲 2 章批 160 秒 / 章节微循环全通过
- 3 章累计 377,383 tokens，总耗时约 60 分钟
- 第 1、2 章全闸门通过（pass），第 3 章审校抓到真实逻辑矛盾（"上午手术成功、下午要求二次手术押金"）→ 回滚保护保留原稿标「待修」
- 离线测试：`test_quality.py` / `smoke_func.py` 全绿；UI 渲染 `shot_ui.py` 四页无 QML 警告；布局检查 0 越界

### 安全公告

- ⚠️ **API Key 泄露**：旧测试脚本（`tests/probe_flash_reasoning.py`、`tests/probe_models.py`、`tests/probe_thinking_param.py`）曾硬编码 OpenCode Go API Key 并随 v0.1.0 提交进入公开仓库。**工作区已清除**，但历史提交中仍存在。**请在 OpenCode Zen 后台吊销该 Key 并重新签发**，不要依赖代码清理。

## [0.1.0] - 2026-08-15

首个公开版本。

### 新增功能

- **写作流水线**：立项（题材/平台/灵感/字数）→ 核心设定 → 全书大纲 → 章节细纲（分块生成 + 拆半降级）→ 章节微循环（上下文组装 → 草稿 → AI味扫描 → 去味改写 → 审校 → 定稿落库）
- **本地 AI 味扫描器**（`app/deslop.py`）：正则规则集，阻断/建议两级——不是A而是B、音量反差腔、否定排比、章末预告、一级禁用词表、认知直述、比喻密度、微动作复读、碎句号、长段落等
- **三槽位路由**：写作槽 / 辅助槽 / 审校槽，独立绑定连接与模型
- **断点续跑**：`pipeline_state.json` 状态机，暂停（checkpoint 语义）/ 停止 / 失败现场 dump
- **记忆链 L1**：追踪四文件（伏笔/时间线/角色状态/上下文）+ 全局摘要 + 章节摘要链，每章定稿自动更新
- **质量闸门策略**：`mark_continue`（默认，标待修继续写）/ `strict`（自动暂停等人）
- **QML「深夜编辑部」界面**：书架 / 流水线监控台 / 章节详情 / 连接与模型
- **打包脚本**：PyInstaller 单文件 exe + 桌面快捷方式安装

### 技术细节

- Python 3.13 + PySide6（Qt Quick / QML）+ httpx
- 连接管理：DeepSeek / OpenAI / 自定义 OpenAI 兼容（含中转网关），可测试连接、拉取模型列表
- LLM 分级重试：网络/超时/429/5xx 指数退避，空内容按可重试处理
- 配置自动迁移：`.oh_story_desktop` 旧目录/旧格式 → `~/.qianbi_novel`

---

## 发布说明

- 本仓库不包含任何真实 API Key；所有测试凭据经环境变量注入
- 打包产物（exe）不纳入版本控制，由 `build_exe.py` 本地生成
