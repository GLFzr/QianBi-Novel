# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.2.1] - 2026-08-16

### 修复

- **UI 重叠（重要修复）**：连接页三张卡片（连接信息/模型参数/槽位绑定）、流水线页当前章状态卡、书架新建对话框三个分组——卡片容器 `Rectangle` 无显式高度导致内容全部溢出重叠。修复为高度绑定内部内容列（实测卡片高度 300/194/194px 正常）
- **书架清理**：清除历史测试残留的 recent_projects（"诡异复苏"等临时项目记录），内置示例小说《改命笔记》进入书架并作为默认打开项目
- 新增 `examples/改命笔记/` 完整示例（详见 [README](README.md#使用指南)）

### 验证

- `shot_ui.py` 四页渲染无 QML 警告；`check_layout.py` 0 越界
- 连接页卡片级元素实测：三卡高度正常，无塌陷
- `smoke_func.py` / `test_quality.py` 全绿

## [0.2.0] - 2026-08-16

### 新增功能

- **OpenCode Go 深度集成**
  - 内置连接模板 `ocgo-flash`（OpenCode Go · V4 Flash，Base URL `https://opencode.ai/zen/go/v1`），新老用户配置自动补全
  - 新增服务商预设「OpenCode Go」，连接页可直接选择并一键填入 Base URL 与模型列表
  - 连接档案支持**思考模式**（默认/禁用/启用）与**思考强度**（low/high/max）：启用时发送 `thinking: {"type":"enabled"}`，强度作为顶层 `reasoning_effort` 参数（参考 DeepSeek 官方 Thinking Mode 文档与 opencode-go provider 的 effort 映射）
  - 内置 ocgo-flash 模板默认 `thinking=enabled + reasoning_effort=max + max_tokens=65536`
- **带指导重写（human-in-the-loop 关键落地）**
  - 章节队列右键菜单与流水线工作台新增「带指导重写…」：输入重写要求（如"这章别写死女主"），删除该章正文并登记指导语，续跑时注入正文生成 prompt
  - 指导语经 `pipeline_state.json` 的 `pending_guidance` 字段持久化（消费即删除，崩溃/重启不丢失）
- **项目文件浏览/编辑**
  - 流水线顶栏「📂 项目文件」：应用内浏览与编辑 设定 / 大纲 / 追踪 目录下全部 md 文件（左文件列表 + 右编辑区 + 保存），改完保存即可，下游续跑自动读取
- **选题 AI 展开**
  - 新建项目对话框「✨ AI 展开」：把一句话灵感交给辅助槽模型，展开成 3 个可开书的选题方向（核心情绪/梗概/读者契约/对标/差异化）后填入灵感框
- **UI 布局重构（v0.2.0 第二轮）**
  - 流水线页改**三栏工作台**：左 300px 章节队列（含全书进度条）｜ 中写作工作区（当前章状态卡 + 微循环步骤条 + 质量四格 + 快捷操作 + 运行引导）｜ 右 300px 运行日志（可清空）
  - 章节详情改**左右双栏**：左 70% 正文编辑区，右 300px AI 味扫描结果侧栏（卡片化条目/空状态引导/点击定位）
  - 书架：封面双色渐变 + 悬停进入提示，空状态居中引导，新建对话框三段分组（保存命名/创作设定/灵感）
  - 连接页：左列表显示服务商标签与槽位徽章，右表单卡片化四组（连接信息/模型参数/操作/槽位绑定）
  - 主框架：导航图标化 + 当前项目名摘要贴底
  - 修复 3 处 QML 布局缺陷（渐变 parent 引用、Column 锚定冲突、非法 fillHeight 赋值）

### 优化改进

- **细纲批量生成修复**（原"每批 5 章只出 1 章"）
  - 实测根因：max 思考下 5 章批的输出预算被推理吃光（`content=0, finish=length`）
  - `OUTLINE_BATCH 5→2`：2 章批实测稳定（约 160 秒/批），失败自动拆半重试兜底
  - 细纲解析器兼容 `=== 第N章 ===`（带空格）与 `### 第N章` markdown 标题两种降级格式
  - 细纲批 prompt 明确"为每一章各输出一个完整小节，一章不漏、不合并"
  - 解析失败日志携带模型输出长度与已解析章号（诊断友好）
- **审校不再越修越糟**
  - 分级标准收紧：阻塞级只留给读者会明显出戏的硬冲突（角色已死又出场/能力前后矛盾/伏笔提前回收等），分钟级时间偏差等可商榷项一律归 ADVISORY
  - **回滚保护**：审校修复轮后复扫，若阻塞数未减少则保留原稿并标「待修」（实测第 3 章 1→1 处时正确回滚）
- **全书大纲防超限**
  - 核心设定截断至 4000 字符再入大纲 prompt（修复前 13.5 分钟失败返回空，修复后 6 分钟成功）
- **LLM 客户端输出预算提升**：e2e 与内置模板 max_tokens 提升至 32768/65536，适配 max 思考档
- **API Key 安全清理**：从 3 个 probe 脚本移除硬编码 Key（曾随历史提交进入公开仓库，见下方安全公告）
- **测试脚本健壮性**：smoke 槽位断言不再假设连接顺序；e2e 支持思考模式/强度环境变量注入、停止信号直连（避免停止请求后下一章继续开跑）、项目名带时间戳避免目录冲突、Windows 控制台 UTF-8 输出

### 测试新增

- `tests/probe_max_thinking.py`：max 思考长输出预算实验（16384 截断 / 32768 完整 / 65536 可用）
- `tests/probe_outline_batch.py`：细纲批次参数化实验（章数/思考强度/max_tokens 组合）
- `tests/check_layout.py`：离屏渲染后遍历可见元素做窗口边界越界检查（0 越界为通过）

### 验证记录（OpenCode Go · deepseek-v4-flash · thinking=enabled · effort=max）

- 端到端 3 章实测：核心设定 49 秒 / 全书大纲 6 分钟 / 细纲 2 章批 160 秒 / 章节微循环全通过
- 3 章累计 377,383 tokens，总耗时约 60 分钟
- 第 1、2 章全闸门通过（pass），第 3 章审校抓到真实逻辑矛盾（"上午手术成功、下午要求二次手术押金"）→ 回滚保护保留原稿标「待修」
- 离线测试：`test_quality.py` / `smoke_func.py` 全绿；UI 渲染 `shot_ui.py` 四页无 QML 警告；布局检查 0 越界

### 安全公告

- ⚠️ **API Key 泄露**：旧测试脚本（`tests/probe_flash_reasoning.py`、`tests/probe_models.py`、`tests/probe_thinking_param.py`）曾硬编码 OpenCode Go API Key 并随 v0.1.0 提交进入公开仓库。**工作区已清除**，但历史提交中仍存在。**请在 OpenCode Zen 后台吊销该 Key 并重新签发**，不要依赖代码清理。

## [0.1.0] - 2026-08-15

首个公开版本。

### 新增功能

- **写作流水线**：立项（题材/平台/灵感/字数）→ 核心设定 → 全书大纲 → 章节细纲（分块生成 + 拆半降级）→ 章节微循环（上下文组装 → 草稿 → AI味扫描 → 去味改写 → 审校 → 定稿落库）
- **本地 AI 味扫描器**（`app/deslop.py`）：正则规则集，阻断/建议两级——不是A而是B、音量反差腔、否定排比、章末预告、一级禁用词表、认知直述、比喻密度、微动作复读、碎句号、长段落等
- **三槽位路由**：写作槽 / 辅助槽 / 审校槽，独立绑定连接与模型
- **断点续跑**：`pipeline_state.json` 状态机，暂停（checkpoint 语义）/ 停止 / 失败现场 dump
- **记忆链 L1**：追踪四文件（伏笔/时间线/角色状态/上下文）+ 全局摘要 + 章节摘要链，每章定稿自动更新
- **质量闸门策略**：`mark_continue`（默认，标待修继续写）/ `strict`（自动暂停等人）
- **QML「深夜编辑部」界面**：书架 / 流水线监控台 / 章节详情 / 连接与模型
- **打包脚本**：PyInstaller 单文件 exe + 桌面快捷方式安装

### 技术细节

- Python 3.13 + PySide6（Qt Quick / QML）+ httpx
- 连接管理：DeepSeek / OpenAI / 自定义 OpenAI 兼容（含中转网关），可测试连接、拉取模型列表
- LLM 分级重试：网络/超时/429/5xx 指数退避，空内容按可重试处理
- 配置自动迁移：`.oh_story_desktop` 旧目录/旧格式 → `~/.qianbi_novel`

---

## 发布说明

- 本仓库不包含任何真实 API Key；所有测试凭据经环境变量注入
- 打包产物（exe）不纳入版本控制，由 `build_exe.py` 本地生成
