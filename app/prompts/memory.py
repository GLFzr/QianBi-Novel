# -*- coding: utf-8 -*-
"""记忆层 prompt：追踪更新 / 章节摘要 / 全局摘要链"""

# ========== 写后：追踪四文件更新（微循环第⑥步）==========

TRACKING_UPDATE_PROMPT = """{project_header}

你是连续性管理员。刚写完第 {chapter_num} 章，请根据正文更新追踪文件。

## 本章正文
{prose}

## 当前追踪文件

### 角色状态
{character_state}

### 伏笔表
{foreshadow_table}

### 时间线
{timeline}

### 写作上下文（既有）
{old_context}

## 核心设定·主要角色表（花名册基准）
{roster}

## 世界书（已登记基准——已收录的实体/规则不得重复登记）
{worldbook}

## 任务
输出八个部分，用 `===XXX===` 分隔：

===角色状态===
（更新后的完整文件。必须为上方「主要角色表」中的**全部角色**各保留一个小节，长期无变化写"无变化（最近出场第N章）"；
本卷新增戏份≥3章的配角也须建节。不得删减已有小节。格式：
## 角色名
- **当前身份**：
- **当前能力**：
- **关键关系**：
- **公众形象**：
- **待回收伏笔**：
- **状态变更记录**：（追加"第N章：变化描述"））

**数字铁律（必须遵守）**：
- 角色状态中的**一切数字**（余额/次数/期限/年龄/日期/金钱/数量）必须**逐字引用本章正文原文**，只能登记"正文中确实出现过的数字"。
- **严禁自行推算、心算或编造**：正文写"消耗了1分"，只能登记"-1 分"的**变化事实**；正文未给余额，就写"（存疑：正文未给出余额）"，绝不写"余额 96.5"这类正文里不存在的数字。
- 若发现本章数字与【角色状态】既有记录对不上，**如实照抄正文数字**，并在该行末尾标注"⚠️ 与前文不一致（前文记录：XX）"——把矛盾暴露出来，不要"修正"成你以为对的值。

===伏笔===
（更新后的完整伏笔表 markdown。格式：
| 伏笔 | 类别 | 埋设章节 | 状态 | 计划回收 | 备注 |
类别三选一：道具谜团 / 规则契约 / 数字倒计时。
- 本章正文出现任何**次数上限、期限、账目数字、规则承诺**（如"还剩七次"），必须登记为规则契约或数字倒计时
- 计划回收必须写具体章节窗口（如"第60-75章卷末"），**禁止写"后续章节"**
- 连续10章未推进的伏笔在备注标「濒危」并写处置建议（排期回收/显式砍掉））

===时间线===
（追加本章事件后的完整时间线 markdown 表格：| 故事内时间 | 章节 | 事件 |）

===上下文===
（更新写作上下文文件：当前进度、最近3章摘要（每章一句话）、待处理线索、
**已用名场面清单**（整街灯灭/全城停电/万人围观等大意象，登记章号，供正文避免复用）。
必须基于上方「写作上下文（既有）」增量更新：既有已登记的名场面章号与待处理线索不得丢失，只追加/更新本章带来的变化）

===新实体===
（本章正文中**实际登场或被正面描写**、且上方「主要角色表」「世界书」均**未收录**的新人物/地名/物品/组织。
每行一条，格式：名称｜类别｜一句话描述（类别限：人物/地名/物品/组织/其他）。
- 已收录的一律不列（世界书「追加登记」里出现过的名字视为已收录）
- 路人甲、一次性背景板（"街边小贩"这类无名角色）不列
- 没有则只写一行：（无））

===新规则===
（本章正文**明确陈述**的新规则/契约/限制/代价（如"每月只能点灯三次"），且世界书未收录。
每行一条，格式：名称｜规则描述。没有则只写一行：（无））

===实体演进===
（已在「世界书」或「主要角色表」登记的实体，本章正文**实际写出**的属性变化
（修为/职位/余额/伤势/身份/持有物/关键关系等）。
每行一条，格式：名称｜字段｜旧值→新值。
- 只登记本文明写的变化，禁止推算心算；旧值正文没写就写"旧值未知"
- 未登记的实体不列（归 ===新实体===）；同一实体多个字段变化分多行
- 没有则只写一行：（无））

===世界观揭示===
（本章正文借叙述交代的**世界事实**（历史/地理/势力格局/力量体系常识），且世界书未收录。
与 ===新规则=== 的区别：规则是角色明说的约束，揭示是叙述交代的事实。
每行一条，格式：名称｜描述。没有则只写一行：（无））

只输出这八部分，不要解释。"""


# ========== 章节一句话摘要（微循环第⑥步）==========

CHAPTER_SUMMARY_PROMPT = """{project_header}

你是剧情速记员。请把以下章节正文压缩成一句话摘要（80 字以内）。

要求：包含本章核心事件 + 关键状态变化 + 结尾落点；写具体，不用"发生了一些事"式空泛表达。

## 第 {chapter_num} 章《{title}》正文（节选）
{prose_excerpt}

只输出摘要本身，不要任何前缀。"""


# ========== 全局摘要链滚动更新（微循环第⑥步）==========

GLOBAL_SUMMARY_PROMPT = """{project_header}

你是长篇小说的剧情档案管理员。基于【既有全局摘要】和【最新一章摘要】，输出更新后的全局摘要（500 字以内）。

要求：
- 记录主角当前处境、主线进展到哪一步、主要矛盾如何演化
- 保留未回收的关键伏笔线索
- 丢弃已解决的细枝末节
- 按时间线组织，陈述句，不做评论

## 既有全局摘要
{old_summary}

## 最新一章
第 {chapter_num} 章：{chapter_summary}

只输出更新后的全局摘要正文。"""


# ========== 剧情反哺提取（章节锁定/补跑：正文里的新东西回写世界书·伏笔表）==========

MEMORY_BACKFLOW_PROMPT = """你是长篇小说的连续性审计员。下面是第 {chapter_num} 章的定稿正文，请提取其中**必须回写进设定档案**的内容，防止后文遗忘或前后矛盾。

## 第 {chapter_num} 章正文
{prose}

## 已登记基准（以下视为"已知"，不得重复登记）

### 世界书（含「追加登记」分区）
{worldbook}

### 核心设定·主要角色表
{roster}

### 角色状态
{character_state}

### 伏笔表（现状）
{foreshadows}

## 本章细纲（对照基准）
{outline}

## 任务
严格只基于**本章正文实际写出的内容**提取，禁止脑补正文没写的东西。输出七个部分，用 `===XXX===` 分隔：

===新实体===
（本章正文中**实际登场或被正面描写**、且上方「世界书」「主要角色表」均**未收录**的新人物/地名/物品/组织。
每行一条，格式：名称｜类别｜一句话描述（类别限：人物/地名/物品/组织/其他）。
- 已收录的一律不列；「追加登记」里出现过的名字视为已收录
- 无名路人、一次性背景板不列
- 没有则只写一行：（无））

===新规则===
（本章正文**明确陈述**的新规则/契约/限制/代价（如"每月只能点灯三次""欠债者不得入庙"），且世界书未收录。
每行一条，格式：名称｜规则描述。没有则只写一行：（无））

===实体演进===
（已在上方「世界书」「主要角色表」「角色状态」登记的实体，本章正文**实际写出**的属性变化
（修为/职位/余额/伤势/身份/持有物/关键关系等）。
每行一条，格式：名称｜字段｜旧值→新值。
- 只登记本文明写的变化，禁止推算心算；旧值正文没写就写"旧值未知"
- 未登记的实体不列（归 ===新实体===）；同一实体多个字段变化分多行
- 没有则只写一行：（无））

===世界观揭示===
（本章正文借叙述交代的**世界事实**（历史/地理/势力格局/力量体系常识），且世界书未收录。
与 ===新规则=== 的区别：规则是角色明说的约束，揭示是叙述交代的事实。
每行一条，格式：名称｜描述。没有则只写一行：（无））

===伏笔变动===
（对照伏笔表现状，只登记正文里确实发生的变动，每行一条：
- 本章**埋下**的新伏笔（悬念/承诺/未兑现之物）：新增｜伏笔内容｜类别｜计划回收（类别限：道具谜团/规则契约/数字倒计时；计划回收写具体章节窗口，禁止"后续章节"）
- 本章**兑现/揭晓**的既有伏笔：回收｜伏笔内容（必须能在上方伏笔表中找到对应行；表里没有的不要写）
- 没有变动则只写一行：（无））

===偏离点===
（本章正文与细纲安排不一致的地方：细纲写了但正文没落地的情节点、正文新增但细纲没有的重大情节、人设/数字/时间与角色状态矛盾。
每行一条，格式：偏离描述（细纲：XX → 正文：YY）。无则写：（无））

===一句话摘要===
（本章一句话摘要，80 字以内：核心事件 + 关键状态变化 + 结尾落点。）

只输出这七部分，不要解释。"""


# ========== 世界书首版生成（自动档：核心设定+大纲完成后，仅当世界书缺失/为空）==========

WORLDBOOK_GEN_PROMPT = """你是本书的设定档案管理员。请基于下方核心设定与全书大纲，生成世界书**首版**。

## 核心设定
{core_setting}

## 全书大纲
{outline}

## 题材预设（只作登记口径：类型归类、数值与条件的写法按这些专项要求；不是可入册的材料来源）
{genre_block}

## 硬性要求
1. **只登记材料里有的**：核心设定/大纲中出现的实体、规则、数值、势力、地点、时间点才可入册；禁止自创新设定
2. 数值类规则（金手指消耗/上限/期限/金额）必须写清数值与条件——这是后文对账基准
3. 每节没有内容就保留表头空表，不要编造凑数
4. 输出从 `## 世界书` 开始的完整 markdown，四个小节结构如下（表头照抄，逐行填充）：

## 世界书

### 1. 实体登记

| 实体名称 | 类型 | 描述 | 关联规则 |
|---------|------|------|---------|

（类型限：主角/配角/物品/能力/数值资源/场景/组织/其他；实体名加粗）

### 2. 规则与数值基准

| 规则名称 | 数值/条件 | 描述 | 影响 |
|---------|-----------|------|------|

（金手指激活条件/消耗/上限/代价、契约限制等；数值必须与核心设定逐字一致）

### 3. 势力与地理

| 势力/地理 | 类型 | 描述 | 关联实体 |
|-----------|------|------|---------|

### 4. 时间锚点

| 时间 | 事件 | 触发条件 | 影响 |
|------|------|---------|------|

（故事开始前已发生的关键事件 + 大纲明示的时间节点）

只输出世界书 markdown 本身，不要解释。"""
