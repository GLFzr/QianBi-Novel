# -*- coding: utf-8 -*-
"""记忆层 prompt：追踪更新 / 章节摘要 / 全局摘要链"""

# ========== 写后：追踪四文件更新（微循环第⑥步）==========

TRACKING_UPDATE_PROMPT = """你是连续性管理员。刚写完第 {chapter_num} 章，请根据正文更新追踪文件。

## 本章正文
{prose}

## 当前追踪文件

### 角色状态
{character_state}

### 伏笔表
{foreshadow_table}

### 时间线
{timeline}

## 任务
输出四个部分，用 `===XXX===` 分隔：

===角色状态===
（仅当本章引起主要角色状态变化时输出更新后的完整文件；无变化则输出"无变化"。格式：
## 角色名
- **当前身份**：
- **当前能力**：
- **关键关系**：
- **公众形象**：
- **待回收伏笔**：
- **状态变更记录**：（追加"第N章：变化描述"））

===伏笔===
（更新后的完整伏笔表 markdown。新埋的伏笔登记为"已埋"，回收的标记为"已回收"。格式：
| 伏笔 | 埋设章节 | 状态 | 计划回收 | 备注 |）

===时间线===
（追加本章事件后的完整时间线 markdown 表格：| 故事内时间 | 章节 | 事件 |）

===上下文===
（更新写作上下文文件：当前进度、最近3章摘要（每章一句话）、待处理线索）

只输出这四部分，不要解释。"""


# ========== 章节一句话摘要（微循环第⑥步）==========

CHAPTER_SUMMARY_PROMPT = """你是剧情速记员。请把以下章节正文压缩成一句话摘要（80 字以内）。

要求：包含本章核心事件 + 关键状态变化 + 结尾落点；写具体，不用"发生了一些事"式空泛表达。

## 第 {chapter_num} 章《{title}》正文（节选）
{prose_excerpt}

只输出摘要本身，不要任何前缀。"""


# ========== 全局摘要链滚动更新（微循环第⑥步）==========

GLOBAL_SUMMARY_PROMPT = """你是长篇小说的剧情档案管理员。基于【既有全局摘要】和【最新一章摘要】，输出更新后的全局摘要（500 字以内）。

要求：
- 记录主角当前处境、主线进展到哪一步、主要矛盾如何演化
- 保留未回收的关键伏笔线索
- 丢弃已解决的细枝末节
- 按时间线组织，陈述句，不做评论

## 既有全局摘要
{old_summary}

## 最新一章
第 {chapter_num} 章：{chapter_summary}

只输出更新后的全局摘要正文。"""
