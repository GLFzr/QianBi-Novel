# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 0.19.9 | 安全修复仅同步到最新版（不单独发旧版补丁） |
| 0.20.0（待发布） | **当前开发线，安全修复的目标版本** |

独立项目没有精力维护多条支持线：发现安全问题后，修复随下一个版本发布，
并在 CHANGELOG 中如实记录。

## 如何报告漏洞

- **首选：GitHub Issue**（[新建 Issue](https://github.com/GLFzr/QianBi-Novel/issues/new/choose)，
  选对应模板）。本仓库是公开的 MIT 项目、且不持有任何用户数据，没有设立私下通报
  通道的必要——但也**请不要**在公开 Issue 里粘贴能复现盗刷的完整 Key 或他人书稿。
- **报障包是黄金证据**：应用内「导出」对话框可生成报障包（对话记录 + 程序日志 +
  版本 + 脱敏配置摘要，Key 与家目录路径自动脱敏），附在 Issue 里能让我们直接看到现场。
  涉及书稿隐私就选「仅日志」档。
- 影响更新的安全事件（例如签名/验签链路问题）请在 Issue 标题标注 `security:` 前缀，
  会优先处理。

## 安全设计要点（这些是承诺，欢迎按条验证）

- **API Key 只进 Windows 凭据管理器**（`app/secrets.py`）。磁盘上的 `config.json`
  只存引用（key_ref）与指纹，不落明文；旧版明文 Key 在首次保存时自动迁移进凭据库。
  日志、崩溃现场、报障包、对话记录统一过脱敏出口。
- **删除连接 = 删除凭据**：从应用里删掉一条连接，它在凭据管理器里的 Key 一并删除
  （两步确认）；退役出厂预设行的自动清理有三重护栏（没改过、没在用、没存过 Key）。
- **遥测默认关闭且不上传**：匿名统计是 opt-in，开启后也只写本地文件
  （`~/.qianbi_novel/telemetry/`），当前版本没有任何统计上传通路。
- **对话记录本地落盘且可关**：Agent 对话全量落盘在书内 `.dialogue/`（WP-27），
  默认开、仅本地、不上网，设置面板一键停写；内容入档前统一脱敏。
- **更新链路只信任签名与哈希**：版本清单必须通过源码里 pin 住的 Ed25519 公钥验签
  （`app/update_check.py` 的 `PUBKEYS`），安装包 SHA-256 必须与清单一致；两条任一不过，
  「立即安装」按钮不会出现。安装包地址只认 https；你自填的镜像下回来的每个字节照样过哈希。
  私钥只在发布机（`%LOCALAPPDATA%\qianbi_sign`），仓库内零私钥材料有静态闸门把关
  （`scripts/update_keys.py --check-repo`）。
- **数据不出本机**：除你配置的模型 API 调用外没有云端；唯一的自动对外请求是
  检查更新（默认开、24h 限流、只下载约 1KB 公开清单、不上传任何内容，可关）。
  细节见 [docs/PRIVACY.md](docs/PRIVACY.md)。

## 已知取舍（不是漏洞，但你应该知道）

- 安装包**没有代码签名证书**（独立项目年费不划算的明说取舍），首次运行会撞
  SmartScreen「已保护你的电脑」——用 Release 页的 SHA-256 对账后再放行。
- 检查更新的**清单通道**允许 http 与用户自填镜像（真正的兜底是验签与哈希，
  明文至多让攻击者晚一步拿到清单，伪造不了可执行的更新）。

## 依赖漏洞

依赖清单见 `requirements.txt`，第三方声明见
[THIRD-PARTY-LICENSES.md](THIRD-PARTY-LICENSES.md)。依赖层面的 CVE 请同样开 Issue，
注明影响的包与版本。
